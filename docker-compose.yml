services:
  energia:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - energia_data:/app/data
    environment:
      - DB_PATH=/app/data/energia.db
    restart: unless-stopped

volumes:
  energia_data:
