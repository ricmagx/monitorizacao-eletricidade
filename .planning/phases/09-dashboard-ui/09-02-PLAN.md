---
phase: 09-dashboard-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/web/services/rankings.py
  - src/web/templates/partials/custo_chart.html
  - src/web/templates/partials/ranking_table.html
  - src/web/templates/partials/recomendacao_banner.html
  - src/web/templates/partials/dashboard_content.html
  - tests/test_web_rankings.py
  - tests/test_web_dashboard.py
autonomous: true
requirements:
  - UI-03
  - UI-05

must_haves:
  truths:
    - "Ranking de fornecedores mostra coluna de poupanca potencial em EUR/ano"
    - "Grafico de custo usa dois datasets bar lado a lado (nao line chart para custo real)"
    - "Banner de recomendacao mostra nome do plano alem do fornecedor"
    - "Upload PDF integrado no layout do dashboard"
    - "Formulario manual de custo removido do layout (endpoint mantido)"
  artifacts:
    - path: "src/web/services/rankings.py"
      provides: "calculate_annual_ranking com campo poupanca_potencial, build_recommendation com campo plan"
    - path: "src/web/templates/partials/custo_chart.html"
      provides: "Dois datasets type bar lado a lado (sem line chart)"
    - path: "src/web/templates/partials/ranking_table.html"
      provides: "Coluna Poupanca Potencial (EUR/ano)"
    - path: "src/web/templates/partials/dashboard_content.html"
      provides: "Layout com upload_xlsx + upload_pdf, sem custo_form"
  key_links:
    - from: "src/web/services/rankings.py"
      to: "src/web/templates/partials/ranking_table.html"
      via: "campo poupanca_potencial no dict de ranking"
      pattern: "poupanca_potencial"
    - from: "src/web/services/rankings.py"
      to: "src/web/templates/partials/recomendacao_banner.html"
      via: "campo plan no dict de recommendation"
      pattern: "recommendation\\.plan"
---

<objective>
Alinhar os componentes visuais do dashboard com a UI-SPEC aprovada na Phase 6: corrigir grafico de custo (line -> bar), adicionar coluna de poupanca ao ranking, actualizar banner de recomendacao, e integrar upload PDF no layout.

Purpose: As tres divergencias concretas entre o estado actual e a UI-SPEC devem ser corrigidas para cumprir UI-03 (ranking com poupanca) e UI-05 (grafico de custo bar+bar). O layout tambem precisa de integrar o upload PDF da Phase 8.

Output: Componentes visuais alinhados com UI-SPEC, testes actualizados.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-ui/09-RESEARCH.md
@.planning/phases/06-ui-design-ui-phase/06-UI-SPEC.md
@.planning/phases/09-dashboard-ui/09-01-SUMMARY.md

Source files to modify:
@src/web/services/rankings.py
@src/web/templates/partials/custo_chart.html
@src/web/templates/partials/ranking_table.html
@src/web/templates/partials/recomendacao_banner.html
@src/web/templates/partials/dashboard_content.html
@tests/test_web_rankings.py
@tests/test_web_dashboard.py

<interfaces>
<!-- Contracts from Plan 01 that this plan uses -->

From src/web/services/rankings.py (current):
```python
def calculate_annual_ranking(analysis: dict | None, current_supplier_name: str) -> list[dict]:
    # Returns list of dicts with: supplier, plan, custo_anual_estimado, is_current
    # Phase 9 Plan 02 must ADD: poupanca_potencial field

def build_recommendation(analysis: dict | None) -> dict:
    # Returns {show, supplier, saving_eur, message}
    # Phase 9 Plan 02 must ADD: plan field
```

From 06-UI-SPEC.md Chart.js Contract:
```
Grafico de custo (estimativa vs real):
- Tipo: bar com dois datasets lado a lado (nao empilhados)
- Dataset "Estimativa (EUR)": rgba(54, 162, 235, 0.8) — azul
- Dataset "Custo Real (EUR)": rgba(255, 99, 132, 0.8) — vermelho/rosa
- Legenda: position: 'bottom'
- Eixo Y: titulo "EUR"
```

From 06-UI-SPEC.md Copywriting Contract:
```
Recomendacao activa: "Mudando para [fornecedor] — plano [plano], poupa cerca de [X] EUR/ano"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Poupanca potencial no ranking + plan no recommendation</name>
  <files>src/web/services/rankings.py, tests/test_web_rankings.py</files>
  <action>
1. Em `rankings.py`, modificar `calculate_annual_ranking()`:
   - Apos construir e ordenar `top5`, calcular poupanca potencial:
   ```python
   current_annual = next(
       (r["custo_anual_estimado"] for r in top5 if r["is_current"]), None
   )
   for r in top5:
       if current_annual is not None:
           r["poupanca_potencial"] = round(current_annual - r["custo_anual_estimado"], 2)
       else:
           r["poupanca_potencial"] = None
   ```
   - Poupanca do fornecedor actual sera 0.0 (ou valor muito baixo por arredondamento)
   - Poupanca negativa (fornecedores mais caros que actual) tambem e valida — template decidira como mostrar

2. Em `rankings.py`, modificar `build_recommendation()`:
   - Apos obter `best_supplier`, tambem extrair `best_plan`:
   ```python
   best_plan = top3[0].get("plan", "") if top3 else ""
   ```
   - Adicionar ao dict de retorno:
   ```python
   return {
       "show": True,
       "supplier": best_supplier,
       "plan": best_plan,
       "saving_eur": float(saving_rounded),
       "message": f"Mudando para {best_supplier} — plano {best_plan}, poupa cerca de {saving_rounded} EUR/ano",
   }
   ```
   - Actualizar a mensagem para seguir o copywriting da UI-SPEC: "Mudando para [fornecedor] — plano [plano], poupa cerca de [X] EUR/ano"

3. Em `tests/test_web_rankings.py`, adicionar:
   - `test_annual_ranking_poupanca_potencial`: verifica que cada entry tem key `poupanca_potencial`, que o fornecedor actual tem poupanca 0 (ou ~0), e que o fornecedor mais barato tem poupanca positiva
   - `test_annual_ranking_poupanca_no_current`: quando current_supplier_name nao corresponde a nenhum fornecedor, poupanca_potencial e None para todos
   - Actualizar `test_build_recommendation_significant`: verificar que result tem key "plan" com valor nao vazio, e que mensagem segue formato UI-SPEC "Mudando para"
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -m pytest tests/test_web_rankings.py -x -q</automated>
  </verify>
  <done>calculate_annual_ranking retorna poupanca_potencial em cada entry. build_recommendation retorna plan e mensagem no formato UI-SPEC. Testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: Alinhar templates com UI-SPEC (custo chart, ranking, banner, layout)</name>
  <files>src/web/templates/partials/custo_chart.html, src/web/templates/partials/ranking_table.html, src/web/templates/partials/recomendacao_banner.html, src/web/templates/partials/dashboard_content.html, tests/test_web_dashboard.py</files>
  <action>
1. **custo_chart.html** — Corrigir grafico de custo (UI-05):
   - Mudar o segundo dataset de `type: 'line'` para `type: 'bar'`
   - Remover `tension`, `spanGaps`, `borderColor`, `order` do dataset de custo real
   - Actualizar cores para as da UI-SPEC:
     - Estimativa: `backgroundColor: 'rgba(54, 162, 235, 0.8)'` (azul UI-SPEC)
     - Custo Real: `backgroundColor: 'rgba(255, 99, 132, 0.8)'` (vermelho/rosa UI-SPEC)
   - Remover `order` do dataset de estimativa tambem
   - Chart.js com dois bar datasets sem `stack` renderiza lado a lado por default — comportamento correcto
   - Manter `if (window._custoChart) window._custoChart.destroy();` para evitar memory leak HTMX
   - Labels do dataset: "Estimativa (EUR)" e "Custo Real (EUR)" per UI-SPEC

2. **ranking_table.html** — Adicionar coluna poupanca (UI-03):
   - Adicionar `<th>Poupanca Potencial (EUR/ano)</th>` no thead
   - No tbody, para cada row:
     - Se `r.is_current`: mostrar "—"
     - Se `r.poupanca_potencial` nao e none e > 0: mostrar valor formatado com cor success (`color: var(--success); font-weight: 600`)
     - Caso contrario: "—"
   - Actualizar empty state text para: "Sem dados de comparacao disponiveis. Importe um XLSX para gerar o ranking." (per UI-SPEC copywriting)

3. **recomendacao_banner.html** — Actualizar mensagem (UI-SPEC copywriting):
   - Mudar de `{{ recommendation.message }}` para template inline que inclui o plano:
   ```html
   {% if recommendation.show %}
   <div class="banner banner-success">
     Mudando para {{ recommendation.supplier }}{% if recommendation.plan %} — plano {{ recommendation.plan }}{% endif %}, poupa cerca de {{ recommendation.saving_eur | int }} EUR/ano
   </div>
   {% endif %}
   ```
   - A mensagem ja vem formatada de build_recommendation(), mas o template deve funcionar mesmo que `plan` esteja vazio

4. **dashboard_content.html** — Reorganizar layout:
   - Remover `{% include "partials/custo_form.html" %}` (formulario manual de custo substituido por upload PDF per UI-SPEC)
   - Adicionar seccao de upload apos os graficos e antes do ranking:
   ```html
   {% include "partials/recomendacao_banner.html" %}
   <div class="grid-2">
     {% include "partials/consumo_chart.html" %}
     <div id="custo-section">
       {% include "partials/custo_section.html" %}
     </div>
   </div>
   {% include "partials/upload_xlsx.html" %}
   {% include "partials/upload_pdf.html" %}
   <div id="ranking-section">
     {% include "partials/ranking_table.html" %}
   </div>
   ```
   - Verificar que `upload_pdf.html` existe em partials (criado na Phase 8) — se nao existir, o include falha silenciosamente ou com erro claro

5. Em `tests/test_web_dashboard.py`, adicionar:
   - `test_custo_chart_uses_bar_type`: GET /local/casa/dashboard — response text contem "type: 'bar'" duas vezes (ambos datasets) e NAO contem "type: 'line'"
   - `test_ranking_has_poupanca_column`: GET /local/casa/dashboard — response text contem "Poupanca Potencial"
   - `test_dashboard_has_upload_pdf`: GET / — response text contem "upload_pdf" ou "Importar Fatura PDF"
   - `test_dashboard_no_custo_form`: GET / — response text NAO contem "custo_form" (formulario removido)
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -m pytest tests/test_web_dashboard.py tests/test_web_rankings.py -x -q</automated>
  </verify>
  <done>Grafico de custo usa dois bar datasets (sem line). Ranking mostra coluna poupanca. Banner mostra plano. Upload PDF integrado no layout. Formulario manual removido do layout. Testes passam.</done>
</task>

</tasks>

<verification>
```bash
# Verificacao completa do plano 02
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade
python3 -m pytest tests/test_web_dashboard.py tests/test_web_rankings.py -x -q

# Suite completa — garantir que nada quebrou
python3 -m pytest tests/ -x -q
```
</verification>

<success_criteria>
- Grafico de custo usa dois datasets bar lado a lado per UI-SPEC (nao line chart)
- Ranking de fornecedores tem coluna "Poupanca Potencial (EUR/ano)" com valores calculados
- Banner de recomendacao mostra "Mudando para [fornecedor] — plano [plano], poupa cerca de [X] EUR/ano"
- Upload PDF integrado no layout do dashboard (include upload_pdf.html)
- Formulario manual de custo (custo_form.html) removido do dashboard_content.html
- Todos os testes passam incluindo os novos
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-ui/09-02-SUMMARY.md`
</output>
