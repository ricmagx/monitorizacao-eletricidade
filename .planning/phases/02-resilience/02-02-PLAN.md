---
phase: 02-resilience
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/backend/eredes_to_monthly_csv.py
  - tests/test_xlsx_validation.py
autonomous: true
requirements: [RES-02]

must_haves:
  truths:
    - "Ao fornecer um XLSX com valores fora dos limites plausiveis (0 kWh ou > 5000 kWh num mes), o pipeline aborta com ValueError antes de escrever qualquer output CSV"
    - "XLSX com valores normais (30-5000 kWh/mes) continua a ser processado sem erros"
  artifacts:
    - path: "src/backend/eredes_to_monthly_csv.py"
      provides: "Bounds check com constantes MIN_MONTHLY_KWH e MAX_MONTHLY_KWH"
      contains: "MIN_MONTHLY_KWH"
    - path: "tests/test_xlsx_validation.py"
      provides: "Testes de bounds check RES-02"
      exports: ["test_bounds_check_rejects_zero_kwh", "test_bounds_check_rejects_above_max", "test_bounds_check_accepts_normal_values"]
  key_links:
    - from: "src/backend/eredes_to_monthly_csv.py"
      to: "output CSV"
      via: "bounds check antes de output_path.open()"
      pattern: "MIN_MONTHLY_KWH.*MAX_MONTHLY_KWH|raise ValueError"
---

<objective>
Adicionar verificacao de sanidade ao parser XLSX (RES-02): bounds check de consumo mensal antes de escrever o CSV de output.

Purpose: Ficheiros XLSX corrompidos ou com colunas trocadas podem produzir valores absurdos (0 kWh ou 50000 kWh num mes) que passam silenciosamente para o relatorio. O bounds check intercepta estes casos antes de escrever qualquer output.
Output: Parser XLSX com validacao de limites 30-5000 kWh/mes, com testes automatizados.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resilience/02-RESEARCH.md

@src/backend/eredes_to_monthly_csv.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/backend/eredes_to_monthly_csv.py:
```python
def convert_xlsx_to_monthly_csv(input_path: Path, output_path: Path, drop_partial_last_month: bool = False) -> Path
def detect_data_start_row(worksheet) -> int
def extract_date_time_and_kwh(row, date_idx, time_idx) -> tuple[str, str, float | None] | None

# Internal data structure (defaultdict):
# monthly[year_month] = {"total_kwh": float, "vazio_kwh": float, "fora_vazio_kwh": float}

# Current flow in convert_xlsx_to_monthly_csv():
# 1. Load workbook, detect header
# 2. Loop rows, aggregate into monthly dict
# 3. Optionally drop partial last month
# 4. output_path.parent.mkdir(...)          <-- line 131
# 5. with output_path.open("w", ...) as f:  <-- line 132
#    write CSV rows
#
# Bounds check must be inserted between step 3 and step 4.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar bounds check no parser XLSX (RES-02)</name>
  <files>
    src/backend/eredes_to_monthly_csv.py,
    tests/test_xlsx_validation.py
  </files>
  <read_first>
    src/backend/eredes_to_monthly_csv.py,
    tests/conftest.py,
    pytest.ini
  </read_first>
  <behavior>
    - Test 1: `test_bounds_check_rejects_zero_kwh` — XLSX com um mes de consumo total 0 kWh lanca ValueError com mensagem "fora dos limites plausiveis" e o CSV de output NAO e criado
    - Test 2: `test_bounds_check_rejects_above_max` — XLSX com um mes de consumo > 5000 kWh lanca ValueError com mensagem "fora dos limites plausiveis" e o CSV de output NAO e criado
    - Test 3: `test_bounds_check_rejects_below_min` — XLSX com um mes de consumo 10 kWh (abaixo de 30) lanca ValueError
    - Test 4: `test_bounds_check_accepts_normal_values` — XLSX com meses de consumo entre 30-5000 kWh e processado normalmente, CSV criado com sucesso
    - Test 5: `test_bounds_check_message_includes_year_month` — A mensagem de erro do ValueError inclui o year_month do mes problematico
  </behavior>
  <action>
**RED phase — criar testes primeiro:**

1. Criar `tests/test_xlsx_validation.py`:

```python
"""Testes de bounds check para o parser XLSX (RES-02)."""
import pytest
from pathlib import Path
from datetime import datetime
from openpyxl import Workbook


def _create_test_xlsx(tmp_path: Path, rows: list[list], filename: str = "test.xlsx") -> Path:
    """Cria um XLSX de teste com formato E-REDES simplificado.

    O formato minimo que convert_xlsx_to_monthly_csv aceita:
    - Sheet 'Consumos'
    - Header row: Data | Hora | (col2) | Potencia(col3) | ... | ... | ... | Registada(col7)
    - Data rows: YYYY/MM/DD | HH:MM | ... | ... | ... | ... | ... | power_kw
    """
    wb = Workbook()
    ws = wb.active
    ws.title = "Consumos"
    # Header row que detect_data_start_row() vai encontrar
    ws.append(["Data", "Hora", "Ativa", "Potencia", "", "", "", "Registada"])
    for row in rows:
        ws.append(row)
    xlsx_path = tmp_path / filename
    wb.save(xlsx_path)
    return xlsx_path


def _make_rows_for_month(year_month: str, total_kwh: float, n_intervals: int = 4) -> list[list]:
    """Gera n_intervals linhas para um mes, distribuindo total_kwh igualmente.

    Cada intervalo = 15 min (0.25h), power_kw = kwh / 0.25.
    """
    kwh_per_interval = total_kwh / n_intervals
    power_kw = kwh_per_interval / 0.25  # power_kw * 0.25 = kwh_per_interval
    year, month = year_month.split("-")
    rows = []
    for i in range(n_intervals):
        hour = str(i).zfill(2)
        rows.append([f"{year}/{month}/15", f"{hour}:00", None, None, None, None, None, power_kw])
    return rows


def test_bounds_check_rejects_zero_kwh(tmp_path):
    """RES-02: XLSX com mes de consumo 0 kWh deve lancar ValueError antes de escrever CSV."""
    from eredes_to_monthly_csv import convert_xlsx_to_monthly_csv
    rows = _make_rows_for_month("2025-01", 0.0)
    xlsx = _create_test_xlsx(tmp_path, rows)
    output_csv = tmp_path / "out.csv"
    with pytest.raises(ValueError, match="fora dos limites"):
        convert_xlsx_to_monthly_csv(xlsx, output_csv)
    assert not output_csv.exists(), "CSV nao deve ser criado quando bounds check falha"


def test_bounds_check_rejects_above_max(tmp_path):
    """RES-02: XLSX com mes de consumo > 5000 kWh deve lancar ValueError."""
    from eredes_to_monthly_csv import convert_xlsx_to_monthly_csv
    rows = _make_rows_for_month("2025-01", 6000.0)
    xlsx = _create_test_xlsx(tmp_path, rows)
    output_csv = tmp_path / "out.csv"
    with pytest.raises(ValueError, match="fora dos limites"):
        convert_xlsx_to_monthly_csv(xlsx, output_csv)
    assert not output_csv.exists(), "CSV nao deve ser criado quando bounds check falha"


def test_bounds_check_rejects_below_min(tmp_path):
    """RES-02: XLSX com mes de consumo < 30 kWh deve lancar ValueError."""
    from eredes_to_monthly_csv import convert_xlsx_to_monthly_csv
    rows = _make_rows_for_month("2025-01", 10.0)
    xlsx = _create_test_xlsx(tmp_path, rows)
    output_csv = tmp_path / "out.csv"
    with pytest.raises(ValueError, match="fora dos limites"):
        convert_xlsx_to_monthly_csv(xlsx, output_csv)
    assert not output_csv.exists()


def test_bounds_check_accepts_normal_values(tmp_path):
    """RES-02: XLSX com valores normais e processado sem erros."""
    from eredes_to_monthly_csv import convert_xlsx_to_monthly_csv
    rows = _make_rows_for_month("2025-01", 800.0) + _make_rows_for_month("2025-02", 1200.0)
    xlsx = _create_test_xlsx(tmp_path, rows)
    output_csv = tmp_path / "out.csv"
    result = convert_xlsx_to_monthly_csv(xlsx, output_csv)
    assert output_csv.exists(), "CSV deve ser criado para valores normais"
    content = output_csv.read_text(encoding="utf-8")
    assert "2025-01" in content
    assert "2025-02" in content


def test_bounds_check_message_includes_year_month(tmp_path):
    """RES-02: Mensagem de erro inclui o year_month do mes problematico."""
    from eredes_to_monthly_csv import convert_xlsx_to_monthly_csv
    rows = _make_rows_for_month("2025-06", 0.0)
    xlsx = _create_test_xlsx(tmp_path, rows)
    output_csv = tmp_path / "out.csv"
    with pytest.raises(ValueError, match="2025-06"):
        convert_xlsx_to_monthly_csv(xlsx, output_csv)
```

**GREEN phase — implementar bounds check:**

2. Modificar `src/backend/eredes_to_monthly_csv.py`:

   a) Adicionar constantes nomeadas no topo do ficheiro, apos a linha `LISBON = ZoneInfo("Europe/Lisbon")` (linha 13):
   ```python
   # Limites plausiveis de consumo mensal (kWh)
   # 30 kWh = minimo conservador (casa desocupada)
   # 5000 kWh = maximo conservador (consumo industrial nao esperado em residencial)
   MIN_MONTHLY_KWH = 30
   MAX_MONTHLY_KWH = 5000
   ```

   b) Na funcao `convert_xlsx_to_monthly_csv()`, inserir o bounds check ANTES da linha `output_path.parent.mkdir(parents=True, exist_ok=True)` (linha 131) e APOS o bloco de `drop_partial_last_month` (linhas 126-129):

   ```python
   # Bounds check: verificar limites plausiveis antes de escrever output (RES-02)
   for ym, totals in monthly.items():
       if not (MIN_MONTHLY_KWH <= totals["total_kwh"] <= MAX_MONTHLY_KWH):
           raise ValueError(
               f"Consumo fora dos limites plausiveis para {ym}: "
               f"{totals['total_kwh']:.1f} kWh (esperado {MIN_MONTHLY_KWH}–{MAX_MONTHLY_KWH} kWh/mes)"
           )
   ```

   O ponto de insercao exacto: apos a linha 129 (`del monthly[last_year_month]`) e antes da linha 131 (`output_path.parent.mkdir(...)`). Isto garante que o bounds check ocorre ANTES de qualquer escrita em disco.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/test_xlsx_validation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/eredes_to_monthly_csv.py contains `MIN_MONTHLY_KWH = 30` at module level
    - src/backend/eredes_to_monthly_csv.py contains `MAX_MONTHLY_KWH = 5000` at module level
    - src/backend/eredes_to_monthly_csv.py contains `raise ValueError` with "fora dos limites" in the message
    - The raise ValueError line appears BEFORE `output_path.parent.mkdir` (bounds check before any disk write)
    - tests/test_xlsx_validation.py contains all 5 test functions
    - `python -m pytest tests/test_xlsx_validation.py -x` exits 0 (all 5 tests pass)
    - `python -m pytest tests/ -x -q` exits 0 (full suite including plan 01 tests still passes)
  </acceptance_criteria>
  <done>Parser XLSX rejeita valores fora de 30-5000 kWh/mes com ValueError explicito antes de escrever qualquer ficheiro CSV (RES-02). 5 testes green. Suite completa green.</done>
</task>

</tasks>

<verification>
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/ -v
</verification>

<success_criteria>
1. `python -m pytest tests/test_xlsx_validation.py -x` — 5 testes green
2. `python -m pytest tests/ -x` — suite completa green (incluindo testes de plan 01)
3. XLSX com 0 kWh lanca ValueError antes de criar CSV
4. XLSX com > 5000 kWh lanca ValueError antes de criar CSV
5. XLSX com valores 30-5000 kWh e processado normalmente
6. Constantes MIN_MONTHLY_KWH e MAX_MONTHLY_KWH definidas no topo do modulo
</success_criteria>

<output>
After completion, create `.planning/phases/02-resilience/02-02-SUMMARY.md`
</output>
