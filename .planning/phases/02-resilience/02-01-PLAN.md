---
phase: 02-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_tiagofelicia_fallback.py
  - tests/test_supplier_missing.py
  - pytest.ini
  - config/tarifarios.json
  - src/backend/tiagofelicia_compare.py
  - src/backend/monthly_workflow.py
autonomous: true
requirements: [RES-01, RES-03]

must_haves:
  truths:
    - "Com tiagofelicia.pt inacessivel, o pipeline termina com sucesso e o relatorio indica explicitamente que usou o catalogo local como fallback"
    - "Se o nome do fornecedor actual nao corresponder a nenhuma linha da tabela do simulador, o pipeline termina com sucesso e o relatorio assinala a ausencia de correspondencia"
  artifacts:
    - path: "src/backend/monthly_workflow.py"
      provides: "Orquestracao do fallback tiagofelicia -> catalogo local"
      contains: "local_catalog"
    - path: "src/backend/tiagofelicia_compare.py"
      provides: "Campo supplier_not_found quando fornecedor sem correspondencia"
      contains: "supplier_not_found"
    - path: "config/tarifarios.json"
      provides: "Catalogo local de tarifarios para fallback"
      contains: "tariffs"
    - path: "tests/test_tiagofelicia_fallback.py"
      provides: "Testes de fallback RES-01"
      exports: ["test_fallback_activated_on_network_error", "test_report_indicates_local_catalog"]
    - path: "tests/test_supplier_missing.py"
      provides: "Testes de fornecedor sem match RES-03"
      exports: ["test_report_warns_when_supplier_not_found"]
    - path: "pytest.ini"
      provides: "Configuracao pytest"
      contains: "testpaths"
  key_links:
    - from: "src/backend/monthly_workflow.py"
      to: "src/backend/energy_compare.py"
      via: "fallback call to analyse()"
      pattern: "energy_compare\\.analyse"
    - from: "src/backend/monthly_workflow.py"
      to: "config/tarifarios.json"
      via: "resolve_path para catalogo local"
      pattern: "local_tariffs_path"
    - from: "src/backend/monthly_workflow.py"
      to: "render_report()"
      via: "verificacao de source e supplier_not_found"
      pattern: "source.*local_catalog|supplier_not_found"
---

<objective>
Implementar fallback do simulador tiagofelicia.pt para catalogo local (RES-01) e tratamento gracioso de fornecedor sem correspondencia (RES-03), com infraestrutura de testes pytest.

Purpose: O pipeline actualmente crasha quando tiagofelicia.pt esta inacessivel e produz output enganoso quando o fornecedor nao tem match no simulador. Esta plan torna ambos os cenarios robustos.
Output: Pipeline resiliente com fallback funcional, testes pytest a cobrir ambos os cenarios, e catalogo local de tarifarios operacional.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resilience/02-RESEARCH.md

@src/backend/monthly_workflow.py
@src/backend/tiagofelicia_compare.py
@src/backend/energy_compare.py
@config/system.json
@config/tarifarios.exemplo.json
@config/fornecedor_atual.exemplo.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/backend/energy_compare.py:
```python
@dataclass(frozen=True)
class MonthlyConsumption:
    year_month: str
    total_kwh: float
    vazio_kwh: float
    fora_vazio_kwh: float

def load_monthly_consumption(path: Path) -> list[MonthlyConsumption]
def load_tariffs(path: Path) -> list[Tariff]
def load_current_contract(path: Path) -> dict[str, Any]
def seasonal_summary(monthly_rows: list[MonthlyConsumption]) -> dict[str, Any]
def analyse(consumption_path: Path, tariffs_path: Path, contract_path: Path, alerts_path: Path | None = None) -> dict[str, Any]
```

From src/backend/tiagofelicia_compare.py:
```python
def analyse_with_tiago(consumption_path: Path, power_label: str, current_supplier: str, current_plan_contains: str | None = None, months_limit: int | None = None) -> dict[str, Any]
def pick_current_result(combined: list[dict], current_supplier: str, current_plan_contains: str | None) -> dict | None
def compare_month(page, month_row, power_label, current_supplier, current_plan_contains) -> dict[str, Any]
def summarise_history(history: list[dict]) -> dict[str, Any]
```

From src/backend/monthly_workflow.py:
```python
def load_config(path: Path) -> dict[str, Any]
def project_root_from_config(config_path: Path) -> Path
def resolve_path(project_root: Path, relative_path: str) -> Path
def render_report(config: dict, analysis: dict, xlsx_path: Path, csv_path: Path) -> str
def run_workflow(config_path: Path, input_xlsx: Path | None = None) -> dict[str, Any]
```

From config/system.json:
```json
{
  "current_contract": {
    "supplier": "Meo Energia",
    "current_plan_contains": "Tarifa Variavel",
    "power_label": "10.35 kVA"
  },
  "pipeline": {
    "processed_csv_path": "data/processed/consumo_mensal_atual.csv",
    "analysis_json_path": "data/processed/analise_tiagofelicia_atual.json",
    "report_dir": "data/reports",
    "status_path": "state/monthly_status.json",
    ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar infraestrutura de testes + catalogo local de tarifarios</name>
  <files>
    pytest.ini,
    tests/__init__.py,
    tests/conftest.py,
    config/tarifarios.json
  </files>
  <read_first>
    config/tarifarios.exemplo.json,
    config/fornecedor_atual.exemplo.json,
    config/system.json,
    src/backend/energy_compare.py
  </read_first>
  <action>
1. Criar `pytest.ini` na raiz do projecto com:
```ini
[pytest]
testpaths = tests
pythonpath = src/backend
```

2. Criar `tests/__init__.py` vazio.

3. Criar `config/tarifarios.json` como copia de `config/tarifarios.exemplo.json` — o conteudo e identico (tarifas publicas, nao contém dados pessoais). Este ficheiro e o catalogo local de fallback usado pelo RES-01.

4. Adicionar `"local_tariffs_path": "config/tarifarios.json"` e `"local_contract_path": "config/fornecedor_atual.exemplo.json"` ao objecto `"pipeline"` em `config/system.json`. Estes paths sao usados pelo fallback em monthly_workflow.py.

5. Criar `tests/conftest.py` com as seguintes fixtures partilhadas:

```python
import csv
import json
import pytest
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent

@pytest.fixture
def project_root():
    return PROJECT_ROOT

@pytest.fixture
def sample_csv(tmp_path):
    """CSV de consumo mensal com 3 meses de dados reais plausíveis."""
    csv_path = tmp_path / "consumo_mensal.csv"
    rows = [
        {"year_month": "2025-01", "total_kwh": "1429.000", "vazio_kwh": "571.600", "fora_vazio_kwh": "857.400"},
        {"year_month": "2025-02", "total_kwh": "1556.000", "vazio_kwh": "622.400", "fora_vazio_kwh": "933.600"},
        {"year_month": "2025-03", "total_kwh": "1100.000", "vazio_kwh": "440.000", "fora_vazio_kwh": "660.000"},
    ]
    with csv_path.open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=["year_month", "total_kwh", "vazio_kwh", "fora_vazio_kwh"])
        writer.writeheader()
        writer.writerows(rows)
    return csv_path

@pytest.fixture
def sample_tariffs(tmp_path):
    """Catalogo local de tarifarios minimo para testes."""
    tariffs_path = tmp_path / "tarifarios.json"
    payload = {
        "metadata": {"currency": "EUR", "country": "PT", "updated_at": "2026-03-26"},
        "tariffs": [
            {
                "id": "luzboa-mono-base",
                "supplier": "Luzboa",
                "plan": "Mono Base",
                "type": "simples",
                "valid_from": "2026-03-01",
                "valid_to": None,
                "energy": {"simples": 0.1619},
                "fixed_daily": {"power_contract": 0.305},
                "source_url": "https://example.com/luzboa-mono",
            },
            {
                "id": "luzboa-bi-base",
                "supplier": "Luzboa",
                "plan": "Bi Base",
                "type": "bihorario",
                "valid_from": "2026-03-01",
                "valid_to": None,
                "energy": {"vazio": 0.108, "fora_vazio": 0.1905},
                "fixed_daily": {"power_contract": 0.305},
                "source_url": "https://example.com/luzboa-bi",
            },
        ],
    }
    tariffs_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    return tariffs_path

@pytest.fixture
def sample_contract(tmp_path):
    """Contrato actual para testes (aponta para luzboa-mono-base)."""
    contract_path = tmp_path / "fornecedor_atual.json"
    payload = {
        "supplier": "Luzboa",
        "plan": "Mono Base",
        "current_tariff_id": "luzboa-mono-base",
    }
    contract_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    return contract_path

@pytest.fixture
def test_config(tmp_path, sample_csv, sample_tariffs, sample_contract):
    """Config system.json minima para testes de workflow."""
    config_path = tmp_path / "config" / "system.json"
    config_path.parent.mkdir(parents=True, exist_ok=True)

    # Criar dirs esperados pelo workflow
    (tmp_path / "data" / "processed").mkdir(parents=True, exist_ok=True)
    (tmp_path / "data" / "reports").mkdir(parents=True, exist_ok=True)
    (tmp_path / "state").mkdir(parents=True, exist_ok=True)
    (tmp_path / "data" / "raw" / "eredes").mkdir(parents=True, exist_ok=True)

    payload = {
        "current_contract": {
            "supplier": "Meo Energia",
            "current_plan_contains": "Tarifa Variavel",
            "power_label": "10.35 kVA",
        },
        "eredes": {
            "download_dir": "data/raw/eredes",
        },
        "pipeline": {
            "processed_csv_path": "data/processed/consumo_mensal_atual.csv",
            "analysis_json_path": "data/processed/analise_tiagofelicia_atual.json",
            "report_dir": "data/reports",
            "status_path": "state/monthly_status.json",
            "drop_partial_last_month": True,
            "notify_on_completion": False,
            "months_limit": None,
            "local_tariffs_path": str(sample_tariffs),
            "local_contract_path": str(sample_contract),
        },
    }
    config_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    return config_path
```
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/ --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - pytest.ini exists and contains `testpaths = tests` and `pythonpath = src/backend`
    - tests/__init__.py exists (empty file)
    - tests/conftest.py exists and contains `def sample_csv`, `def sample_tariffs`, `def sample_contract`, `def test_config`
    - config/tarifarios.json exists and contains `"tariffs"` array with at least 6 entries
    - config/system.json contains `"local_tariffs_path": "config/tarifarios.json"` and `"local_contract_path": "config/fornecedor_atual.exemplo.json"`
    - `python -m pytest tests/ --collect-only` runs without import errors
  </acceptance_criteria>
  <done>Infraestrutura pytest funcional, catalogo local de tarifarios criado, config/system.json actualizado com paths de fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar fallback tiagofelicia (RES-01) + fornecedor sem match (RES-03)</name>
  <files>
    tests/test_tiagofelicia_fallback.py,
    tests/test_supplier_missing.py,
    src/backend/monthly_workflow.py,
    src/backend/tiagofelicia_compare.py
  </files>
  <read_first>
    src/backend/monthly_workflow.py,
    src/backend/tiagofelicia_compare.py,
    src/backend/energy_compare.py,
    config/system.json,
    tests/conftest.py
  </read_first>
  <behavior>
    RES-01 (fallback):
    - Test 1: `test_fallback_activated_on_network_error` — Quando `analyse_with_tiago` lanca Exception, o workflow usa `energy_compare.analyse()` com catalogo local e o resultado contem `"source": "local_catalog"`
    - Test 2: `test_report_indicates_local_catalog` — O relatorio .md gerado em modo fallback contem a string "catalogo local"
    - Test 3: `test_fallback_reason_recorded` — O resultado contem `"fallback_reason"` com a mensagem da excepcao original

    RES-03 (supplier not found):
    - Test 4: `test_compare_month_marks_supplier_not_found` — Quando `pick_current_result` retorna None, `compare_month` inclui `"supplier_not_found": True` no resultado
    - Test 5: `test_report_warns_when_supplier_not_found` — O relatorio .md contem "nao foi encontrado" quando supplier_not_found e True
    - Test 6: `test_pipeline_does_not_crash_on_missing_supplier` — O workflow completo termina sem excepcao quando o fornecedor nao tem match
  </behavior>
  <action>
**RED phase — criar testes primeiro:**

1. Criar `tests/test_tiagofelicia_fallback.py`:
```python
from unittest.mock import patch, MagicMock
from pathlib import Path
import json
import pytest

def test_fallback_activated_on_network_error(test_config, sample_csv, tmp_path):
    """RES-01: Pipeline usa catalogo local quando tiagofelicia.pt inacessivel."""
    from monthly_workflow import run_workflow
    # Copiar sample_csv para o path que o workflow espera
    import shutil
    # Mock analyse_with_tiago para lancar Exception
    with patch("monthly_workflow.analyse_with_tiago") as mock_at:
        mock_at.side_effect = Exception("net::ERR_NAME_NOT_RESOLVED")
        result = run_workflow(config_path=test_config, input_xlsx=sample_csv)
    assert result["source"] == "local_catalog"

def test_report_indicates_local_catalog(test_config, sample_csv, tmp_path):
    """RES-01: Relatorio indica explicitamente que usou catalogo local."""
    from monthly_workflow import run_workflow
    with patch("monthly_workflow.analyse_with_tiago") as mock_at:
        mock_at.side_effect = Exception("net::ERR_NAME_NOT_RESOLVED")
        result = run_workflow(config_path=test_config, input_xlsx=sample_csv)
    report_text = Path(result["report_path"]).read_text(encoding="utf-8")
    assert "catalogo local" in report_text.lower() or "catálogo local" in report_text.lower()

def test_fallback_reason_recorded(test_config, sample_csv, tmp_path):
    """RES-01: Resultado contem razao do fallback."""
    from monthly_workflow import run_workflow
    with patch("monthly_workflow.analyse_with_tiago") as mock_at:
        mock_at.side_effect = Exception("net::ERR_NAME_NOT_RESOLVED")
        result = run_workflow(config_path=test_config, input_xlsx=sample_csv)
    assert "fallback_reason" in result
    assert "ERR_NAME_NOT_RESOLVED" in result["fallback_reason"]
```

2. Criar `tests/test_supplier_missing.py`:
```python
import pytest

def test_compare_month_marks_supplier_not_found():
    """RES-03: compare_month inclui supplier_not_found quando fornecedor sem match."""
    from tiagofelicia_compare import compare_month, pick_current_result
    from energy_compare import MonthlyConsumption
    from unittest.mock import MagicMock

    # Criar mock page com resultados que NAO incluem "Fornecedor Inexistente"
    mock_page = MagicMock()
    # Mock run_simple_simulation e run_bi_simulation para retornar resultados sem o fornecedor
    import tiagofelicia_compare
    from unittest.mock import patch

    fake_results = [
        {"supplier": "Luzboa", "plan": "Mono", "product_type": "", "cycle": "Simples",
         "year_month": "2025-01", "total_eur": 150.0, "energy_rate": "0.16", "power_rate": "0.30"},
    ]
    with patch.object(tiagofelicia_compare, "run_simple_simulation", return_value=fake_results), \
         patch.object(tiagofelicia_compare, "run_bi_simulation", return_value=fake_results):
        result = compare_month(
            page=mock_page,
            month_row=MonthlyConsumption("2025-01", 1429.0, 571.6, 857.4),
            power_label="10.35 kVA",
            current_supplier="Fornecedor Inexistente XYZ",
            current_plan_contains=None,
        )
    assert result["supplier_not_found"] is True
    assert result["current_supplier_result"] is None

def test_report_warns_when_supplier_not_found(test_config, sample_csv):
    """RES-03: Relatorio contem aviso quando fornecedor sem match no simulador."""
    from monthly_workflow import run_workflow
    from unittest.mock import patch

    # Simular analyse_with_tiago que retorna resultado com supplier_not_found
    fake_analysis = {
        "generated_at": "2026-03-29",
        "source": "tiagofelicia.pt",
        "power_label": "10.35 kVA",
        "current_supplier": "Fornecedor Inexistente",
        "current_plan_contains": None,
        "seasonality": {
            "avg_monthly_total_kwh": 1362.0,
            "avg_vazio_ratio": 0.4,
            "winter_avg_kwh": 1492.0,
            "summer_avg_kwh": None,
            "monthly_profile": [],
        },
        "history_summary": {
            "months_analysed": 1,
            "simple_wins": 1,
            "bihorario_wins": 0,
            "latest_month": "2025-01",
            "latest_recommendation": "simples",
            "latest_top_3": [
                {"supplier": "Luzboa", "plan": "Mono", "cycle": "Simples",
                 "year_month": "2025-01", "total_eur": 150.0, "energy_rate": "0.16", "power_rate": "0.30"},
            ],
            "latest_current_supplier_result": None,
            "latest_change_needed": False,
            "latest_saving_vs_current_eur": None,
            "supplier_not_found": True,
        },
        "history": [
            {
                "year_month": "2025-01",
                "top_3": [
                    {"supplier": "Luzboa", "plan": "Mono", "cycle": "Simples",
                     "year_month": "2025-01", "total_eur": 150.0, "energy_rate": "0.16", "power_rate": "0.30"},
                ],
                "current_supplier_result": None,
                "supplier_not_found": True,
                "best_simple": {"supplier": "Luzboa", "total_eur": 150.0},
                "best_bihorario": {"supplier": "Luzboa", "total_eur": 150.0},
                "recommended_option": "simples",
                "difference_simple_vs_bi_eur": 0.0,
                "needs_change": False,
                "saving_vs_current_eur": None,
            }
        ],
    }
    with patch("monthly_workflow.analyse_with_tiago", return_value=fake_analysis):
        result = run_workflow(config_path=test_config, input_xlsx=sample_csv)
    report_text = open(result["report_path"], encoding="utf-8").read()
    assert "nao foi encontrado" in report_text.lower() or "não foi encontrado" in report_text.lower()

def test_pipeline_does_not_crash_on_missing_supplier(test_config, sample_csv):
    """RES-03: Pipeline termina sem excepcao quando fornecedor sem match."""
    from monthly_workflow import run_workflow
    from unittest.mock import patch

    fake_analysis = {
        "generated_at": "2026-03-29",
        "source": "tiagofelicia.pt",
        "power_label": "10.35 kVA",
        "current_supplier": "Fornecedor Inexistente",
        "current_plan_contains": None,
        "seasonality": {
            "avg_monthly_total_kwh": 1362.0,
            "avg_vazio_ratio": 0.4,
            "winter_avg_kwh": 1492.0,
            "summer_avg_kwh": None,
            "monthly_profile": [],
        },
        "history_summary": {
            "months_analysed": 1,
            "simple_wins": 1,
            "bihorario_wins": 0,
            "latest_month": "2025-01",
            "latest_recommendation": "simples",
            "latest_top_3": [
                {"supplier": "Luzboa", "plan": "Mono", "cycle": "Simples",
                 "year_month": "2025-01", "total_eur": 150.0, "energy_rate": "0.16", "power_rate": "0.30"},
            ],
            "latest_current_supplier_result": None,
            "latest_change_needed": False,
            "latest_saving_vs_current_eur": None,
            "supplier_not_found": True,
        },
        "history": [
            {
                "year_month": "2025-01",
                "top_3": [
                    {"supplier": "Luzboa", "plan": "Mono", "cycle": "Simples",
                     "year_month": "2025-01", "total_eur": 150.0, "energy_rate": "0.16", "power_rate": "0.30"},
                ],
                "current_supplier_result": None,
                "supplier_not_found": True,
                "best_simple": {"supplier": "Luzboa", "total_eur": 150.0},
                "best_bihorario": {"supplier": "Luzboa", "total_eur": 150.0},
                "recommended_option": "simples",
                "difference_simple_vs_bi_eur": 0.0,
                "needs_change": False,
                "saving_vs_current_eur": None,
            }
        ],
    }
    with patch("monthly_workflow.analyse_with_tiago", return_value=fake_analysis):
        result = run_workflow(config_path=test_config, input_xlsx=sample_csv)
    assert result["status"] == "ok"
```

**GREEN phase — implementar codigo de producao:**

3. Modificar `src/backend/tiagofelicia_compare.py` — funcao `compare_month()`:
   - Apos a linha `current = pick_current_result(combined, current_supplier, current_plan_contains)` (linha 122), adicionar:
   ```python
   supplier_not_found = current is None
   ```
   - No dict retornado por `compare_month`, adicionar campo `"supplier_not_found": supplier_not_found`

4. Modificar `src/backend/tiagofelicia_compare.py` — funcao `summarise_history()`:
   - Apos `latest = history[-1]` (linha 147), adicionar verificacao:
   ```python
   any_supplier_not_found = any(item.get("supplier_not_found", False) for item in history)
   ```
   - Adicionar ao dict retornado: `"supplier_not_found": any_supplier_not_found`

5. Modificar `src/backend/monthly_workflow.py` — funcao `run_workflow()`:
   - Substituir o bloco que chama `analyse_with_tiago` (linhas 137-143) pelo seguinte:
   ```python
   import energy_compare

   try:
       analysis = analyse_with_tiago(
           consumption_path=processed_csv_path,
           power_label=config["current_contract"]["power_label"],
           current_supplier=config["current_contract"]["supplier"],
           current_plan_contains=config["current_contract"].get("current_plan_contains"),
           months_limit=config["pipeline"].get("months_limit"),
       )
       if "source" not in analysis:
           analysis["source"] = "tiagofelicia.pt"
   except Exception as exc:
       local_tariffs_path = resolve_path(project_root, pipeline["local_tariffs_path"])
       local_contract_path = resolve_path(project_root, pipeline["local_contract_path"])
       analysis = energy_compare.analyse(
           consumption_path=processed_csv_path,
           tariffs_path=local_tariffs_path,
           contract_path=local_contract_path,
       )
       analysis["source"] = "local_catalog"
       analysis["fallback_reason"] = str(exc)
   ```
   - Adicionar `import energy_compare` ao topo de `monthly_workflow.py` (junto aos outros imports, apos a linha 8).

6. Modificar `src/backend/monthly_workflow.py` — funcao `render_report()`:
   - Apos a linha `lines = [` (linha 37), antes do bloco de resultado, adicionar verificacao de source e supplier_not_found:

   Apos a linha `"## Resultado",` e antes de `f"- Fornecedor atual:`, inserir:
   ```python
   # Indicador de fonte (RES-01)
   if analysis.get("source") == "local_catalog":
       lines.extend([
           "",
           "> **Aviso:** Fonte: catalogo local (tiagofelicia.pt indisponivel — dados podem estar desactualizados)",
           f"> Razao: {analysis.get('fallback_reason', 'desconhecida')}",
           "",
       ])
   ```

   Apos a linha `f"- Poupanca estimada no ultimo mes:`, inserir:
   ```python
   # Aviso de fornecedor sem correspondencia (RES-03)
   if analysis.get("history_summary", {}).get("supplier_not_found", False):
       lines.extend([
           "",
           f"> **Aviso:** Fornecedor actual \"{config['current_contract']['supplier']}\" nao foi encontrado na tabela do simulador.",
           "> O ranking e apresentado sem comparacao com o contrato actual.",
           "",
       ])
   ```

   Tambem corrigir a linha que mostra `latest['latest_saving_vs_current_eur']` — quando e `None`, mostrar "N/A" em vez de `None`:
   ```python
   saving_display = latest['latest_saving_vs_current_eur']
   if saving_display is None:
       saving_display = "N/A (fornecedor nao encontrado)"
   ```
   Usar `saving_display` na f-string em vez de `latest['latest_saving_vs_current_eur']`.

7. Tambem em `render_report()`, o campo `latest['latest_change_needed']` pode ser `False` por ausencia de current — proteger:
   ```python
   change_display = "sim" if latest.get("latest_change_needed", False) else "nao"
   ```

8. Adicionar ao dict `status` retornado por `run_workflow()` (apos a linha 156):
   ```python
   "source": analysis.get("source", "tiagofelicia.pt"),
   "fallback_reason": analysis.get("fallback_reason"),
   ```
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/test_tiagofelicia_fallback.py tests/test_supplier_missing.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tiagofelicia_fallback.py contains `def test_fallback_activated_on_network_error`, `def test_report_indicates_local_catalog`, `def test_fallback_reason_recorded`
    - tests/test_supplier_missing.py contains `def test_compare_month_marks_supplier_not_found`, `def test_report_warns_when_supplier_not_found`, `def test_pipeline_does_not_crash_on_missing_supplier`
    - src/backend/monthly_workflow.py contains `import energy_compare`
    - src/backend/monthly_workflow.py contains `analysis["source"] = "local_catalog"`
    - src/backend/monthly_workflow.py contains `analysis["fallback_reason"] = str(exc)`
    - src/backend/monthly_workflow.py contains `catalogo local` (in render_report)
    - src/backend/monthly_workflow.py contains `nao foi encontrado` (in render_report)
    - src/backend/tiagofelicia_compare.py contains `"supplier_not_found": supplier_not_found` in compare_month return dict
    - All 6 tests pass: `python -m pytest tests/test_tiagofelicia_fallback.py tests/test_supplier_missing.py -x` exits 0
  </acceptance_criteria>
  <done>Fallback para catalogo local activo quando tiagofelicia.pt inacessivel (RES-01). Fornecedor sem correspondencia assinalado no relatorio com aviso explicito em vez de crash (RES-03). 6 testes green.</done>
</task>

</tasks>

<verification>
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/ -v
</verification>

<success_criteria>
1. `python -m pytest tests/test_tiagofelicia_fallback.py -x` — 3 testes green
2. `python -m pytest tests/test_supplier_missing.py -x` — 3 testes green
3. `config/tarifarios.json` existe com tarifarios publicos
4. `config/system.json` contem `local_tariffs_path` e `local_contract_path`
5. `render_report()` inclui aviso "catalogo local" quando `source == "local_catalog"`
6. `render_report()` inclui aviso "nao foi encontrado" quando `supplier_not_found == True`
</success_criteria>

<output>
After completion, create `.planning/phases/02-resilience/02-01-SUMMARY.md`
</output>
