---
phase: 07-upload-xlsx-ingest-o-de-dados
plan: 03
type: execute
wave: 1
depends_on: [07-01, 07-02]
files_modified:
  - src/db/migrations/env.py
  - src/db/migrations/versions/002_add_locais.py
autonomous: true
requirements: [COMP-02]
gap_closure: true

must_haves:
  truths:
    - "alembic upgrade head completa sem erros numa BD SQLite fresca"
    - "A migration 002 adiciona UniqueConstraint em comparacoes via batch mode"
    - "env.py configura render_as_batch=True para ambos os modos (online e offline)"
  artifacts:
    - path: "src/db/migrations/env.py"
      provides: "Alembic env com render_as_batch=True para SQLite"
      contains: "render_as_batch=True"
    - path: "src/db/migrations/versions/002_add_locais.py"
      provides: "Migration com batch_alter_table para SQLite compatibility"
      contains: "batch_alter_table"
  key_links:
    - from: "src/db/migrations/env.py"
      to: "src/db/migrations/versions/002_add_locais.py"
      via: "render_as_batch=True enables batch_alter_table operations"
      pattern: "render_as_batch=True"
---

<objective>
Corrigir o bug bloqueador na migration Alembic 002 que impede `alembic upgrade head` de funcionar numa BD SQLite fresca. O SQLite nao suporta `ALTER TABLE ADD CONSTRAINT` directamente — requer batch mode (copy-and-move strategy) do Alembic.

Purpose: Sem este fix, Docker fresh install, CI/CD e novos ambientes falham ao aplicar migrations. O caminho de upgrade via Alembic fica quebrado para deploys no Unraid.
Output: Dois ficheiros corrigidos — env.py com render_as_batch e migration 002 com batch_alter_table.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-VERIFICATION.md

<interfaces>
<!-- Current broken files that need surgical fixes -->

From src/db/migrations/env.py (lines 28, 40 — missing render_as_batch=True):
```python
def run_migrations_offline():
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    # ^ MISSING: render_as_batch=True

def run_migrations_online():
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        # ^ MISSING: render_as_batch=True
```

From src/db/migrations/versions/002_add_locais.py (lines 27-29 — uses bare op.create_unique_constraint):
```python
def upgrade():
    op.create_table('locais', ...)  # This is fine — new table
    op.create_unique_constraint(
        'uq_comparacao_loc_month', 'comparacoes', ['location_id', 'year_month']
    )
    # ^ BROKEN: SQLite cannot ALTER TABLE ADD CONSTRAINT without batch mode

def downgrade():
    op.drop_constraint('uq_comparacao_loc_month', 'comparacoes')
    # ^ BROKEN: same issue — needs batch mode
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar render_as_batch=True ao env.py</name>
  <files>src/db/migrations/env.py</files>
  <read_first>
    - src/db/migrations/env.py
  </read_first>
  <action>
Editar `src/db/migrations/env.py` para adicionar `render_as_batch=True` em AMBAS as chamadas a `context.configure()`:

1. Na funcao `run_migrations_offline()` (linha 28), alterar:
```python
# DE:
context.configure(url=url, target_metadata=target_metadata, literal_binds=True)

# PARA:
context.configure(
    url=url,
    target_metadata=target_metadata,
    literal_binds=True,
    render_as_batch=True,
)
```

2. Na funcao `run_migrations_online()` (linha 40), alterar:
```python
# DE:
context.configure(connection=connection, target_metadata=target_metadata)

# PARA:
context.configure(
    connection=connection,
    target_metadata=target_metadata,
    render_as_batch=True,
)
```

Nao alterar mais nada no ficheiro. O resto do env.py (imports, sys.path, db_path, etc.) fica intacto.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && grep -c "render_as_batch=True" src/db/migrations/env.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "render_as_batch=True" src/db/migrations/env.py retorna exactamente 2 ocorrencias
    - A funcao run_migrations_offline contem render_as_batch=True
    - A funcao run_migrations_online contem render_as_batch=True
    - O ficheiro continua a ter exactamente 2 funcoes: run_migrations_offline e run_migrations_online
  </acceptance_criteria>
  <done>env.py tem render_as_batch=True em ambas as funcoes de configuracao (offline e online)</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar migration 002 para usar batch_alter_table</name>
  <files>src/db/migrations/versions/002_add_locais.py</files>
  <read_first>
    - src/db/migrations/versions/002_add_locais.py
  </read_first>
  <action>
Editar `src/db/migrations/versions/002_add_locais.py` para substituir as operacoes de constraint directas por batch mode.

O ficheiro completo deve ficar:

```python
"""Adiciona tabela locais e UniqueConstraint em comparacoes.

Revision ID: 002
Revises: 001
Create Date: 2026-03-30
"""
from alembic import op
import sqlalchemy as sa

revision = '002'
down_revision = '001'
branch_labels = None
depends_on = None


def upgrade():
    op.create_table(
        'locais',
        sa.Column('id', sa.String(64), primary_key=True),
        sa.Column('name', sa.String(128), nullable=False),
        sa.Column('cpe', sa.String(64), nullable=False, unique=True),
        sa.Column('current_supplier', sa.String(128)),
        sa.Column('current_plan_contains', sa.String(128)),
        sa.Column('power_label', sa.String(32)),
        sa.Column('created_at', sa.DateTime),
    )
    with op.batch_alter_table('comparacoes') as batch_op:
        batch_op.create_unique_constraint(
            'uq_comparacao_loc_month', ['location_id', 'year_month']
        )


def downgrade():
    with op.batch_alter_table('comparacoes') as batch_op:
        batch_op.drop_constraint('uq_comparacao_loc_month', type_='unique')
    op.drop_table('locais')
```

Mudancas especificas:
- `upgrade()`: Substituir `op.create_unique_constraint('uq_comparacao_loc_month', 'comparacoes', ['location_id', 'year_month'])` por `with op.batch_alter_table('comparacoes') as batch_op: batch_op.create_unique_constraint('uq_comparacao_loc_month', ['location_id', 'year_month'])` — nota: dentro de batch_op, o nome da tabela NAO e passado (ja esta no contexto do batch).
- `downgrade()`: Substituir `op.drop_constraint('uq_comparacao_loc_month', 'comparacoes')` por `with op.batch_alter_table('comparacoes') as batch_op: batch_op.drop_constraint('uq_comparacao_loc_month', type_='unique')` — nota: `type_='unique'` e necessario no batch mode para identificar o tipo de constraint.
- `op.create_table('locais', ...)` NAO precisa de batch — e uma tabela nova, nao ALTER de existente.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && grep -c "batch_alter_table" src/db/migrations/versions/002_add_locais.py && python -c "
import tempfile, os
os.environ['DB_PATH'] = tempfile.mktemp(suffix='.db')
from alembic.config import Config
from alembic import command
cfg = Config('src/db/alembic.ini')
cfg.set_main_option('sqlalchemy.url', f'sqlite:///{os.environ[\"DB_PATH\"]}')
command.upgrade(cfg, 'head')
print('alembic upgrade head: OK')
os.unlink(os.environ['DB_PATH'])
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "batch_alter_table" src/db/migrations/versions/002_add_locais.py retorna exactamente 2 ocorrencias (upgrade + downgrade)
    - grep "op.create_unique_constraint" src/db/migrations/versions/002_add_locais.py retorna 0 ocorrencias (removido)
    - grep "op.drop_constraint" src/db/migrations/versions/002_add_locais.py retorna 0 ocorrencias (removido)
    - alembic upgrade head numa BD SQLite fresca (tempfile) completa sem erros
    - op.create_table('locais', ...) permanece inalterado (nao precisa de batch)
    - revision = '002' e down_revision = '001' permanecem inalterados
  </acceptance_criteria>
  <done>Migration 002 usa batch_alter_table para UniqueConstraint em comparacoes; alembic upgrade head funciona numa BD SQLite fresca</done>
</task>

</tasks>

<verification>
- `grep -c "render_as_batch=True" src/db/migrations/env.py` retorna `2`
- `grep -c "batch_alter_table" src/db/migrations/versions/002_add_locais.py` retorna `2`
- `grep -c "op.create_unique_constraint" src/db/migrations/versions/002_add_locais.py` retorna `0`
- `alembic upgrade head` numa BD SQLite fresca completa sem erros (NotImplementedError resolvido)
- `pytest tests/ -x -q` — suite completa continua a passar (sem regressoes)
</verification>

<success_criteria>
1. env.py tem render_as_batch=True em AMBAS as funcoes de configuracao
2. Migration 002 usa batch_alter_table para operacoes de constraint em tabela existente (comparacoes)
3. op.create_table('locais') permanece inalterado (tabela nova nao precisa de batch)
4. alembic upgrade head funciona numa BD SQLite fresca sem erros
5. Suite de testes existente continua a passar
</success_criteria>

<output>
After completion, create `.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-03-SUMMARY.md`
</output>
