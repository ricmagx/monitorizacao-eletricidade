---
phase: 07-upload-xlsx-ingest-o-de-dados
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/web/routes/upload.py
  - src/web/routes/locais.py
  - src/web/app.py
  - src/web/services/data_loader.py
  - src/web/templates/partials/upload_xlsx.html
  - src/web/templates/partials/upload_confirmacao.html
  - src/web/templates/partials/locais_form.html
  - src/web/templates/dashboard.html
  - Dockerfile
  - requirements-docker.txt
autonomous: true
requirements: [UPLD-01, CONF-01, CONF-02, COMP-01]

must_haves:
  truths:
    - "POST /upload/xlsx aceita ficheiro multipart e retorna HTML partial com confirmacao ou erro"
    - "GET /locais retorna formulario de criacao e lista de locais existentes"
    - "POST /locais cria novo local e actualiza o selector de locais"
    - "POST /locais/{id}/fornecedor actualiza o fornecedor actual"
    - "Apos upload bem-sucedido, BackgroundTask e registada para consultar tiagofelicia.pt"
    - "Background task insere em comparacoes com ON CONFLICT DO NOTHING (idempotente)"
    - "Dashboard inclui seccao de upload XLSX e link para gerir locais"
    - "Dashboard location selector mostra locais criados via UI (lidos de SQLite)"
  artifacts:
    - path: "src/web/routes/upload.py"
      provides: "POST /upload/xlsx endpoint"
      exports: ["router"]
    - path: "src/web/routes/locais.py"
      provides: "GET/POST /locais + POST /locais/{id}/fornecedor"
      exports: ["router"]
    - path: "src/web/services/data_loader.py"
      provides: "load_locations() com fallback para SQLite"
      exports: ["load_locations"]
    - path: "src/web/templates/partials/upload_xlsx.html"
      provides: "Formulario de upload XLSX com HTMX"
      contains: "hx-encoding"
    - path: "src/web/templates/partials/upload_confirmacao.html"
      provides: "Partial de confirmacao/erro apos upload"
    - path: "src/web/templates/partials/locais_form.html"
      provides: "Formulario criar local + lista de locais"
    - path: "Dockerfile"
      provides: "Playwright + Chromium instalados no container"
      contains: "playwright install chromium"
  key_links:
    - from: "src/web/routes/upload.py"
      to: "src/web/services/ingestao_xlsx.py"
      via: "import ingerir_xlsx"
      pattern: "from src.web.services.ingestao_xlsx import ingerir_xlsx"
    - from: "src/web/routes/locais.py"
      to: "src/web/services/locais_service.py"
      via: "import CRUD functions"
      pattern: "from src.web.services.locais_service import"
    - from: "src/web/app.py"
      to: "src/web/routes/upload.py"
      via: "app.include_router"
      pattern: "app.include_router(upload_router)"
    - from: "src/web/app.py"
      to: "src/web/routes/locais.py"
      via: "app.include_router"
      pattern: "app.include_router(locais_router)"
    - from: "src/web/routes/upload.py"
      to: "src/db/schema.py"
      via: "_consultar_tiagofelicia_bg uses on_conflict_do_nothing on comparacoes"
      pattern: "on_conflict_do_nothing"
    - from: "src/web/services/data_loader.py"
      to: "src/web/services/locais_service.py"
      via: "load_locations falls back to get_all_locais when SQLite has data"
      pattern: "get_all_locais"
---

<objective>
Criar os endpoints HTTP, templates HTMX e wiring da app para upload XLSX, gestao de locais, e background task tiagofelicia.pt. Adicionalmente, ligar o selector de locais do dashboard ao SQLite para que locais criados via UI aparecam no selector (CONF-01 success criterion 4).

Purpose: Ligar os servicos criados em Plan 01 a endpoints acessiveis pelo utilizador via browser. Adicionar playwright ao Docker para COMP-01. Garantir que o dashboard ve locais novos.
Output: Routes upload.py + locais.py registados na app, templates HTMX, Dockerfile com playwright, data_loader com fallback SQLite
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-RESEARCH.md
@.planning/phases/06-ui-design-ui-phase/06-UI-SPEC.md
@.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/web/services/ingestao_xlsx.py (created in Plan 01):
```python
def ingerir_xlsx(tmp_path: Path, filename: str, engine: Engine) -> dict:
    """Returns dict with keys: erro (str|None), location_id, location_name, cpe,
    meses_inseridos (int), meses_total (int), periodo_inicio, periodo_fim."""
```

From src/web/services/locais_service.py (created in Plan 01):
```python
def get_all_locais(engine: Engine) -> list[dict]:
def get_local_by_id(local_id: str, engine: Engine) -> dict | None:
def create_local(local_id: str, name: str, cpe: str, engine: Engine) -> dict:
    # Raises ValueError if CPE already exists
def update_fornecedor(local_id: str, supplier: str, plan_contains: str | None, engine: Engine) -> dict | None:
```

From src/db/schema.py (updated in Plan 01):
```python
comparacoes = Table(
    "comparacoes", metadata,
    ...
    UniqueConstraint("location_id", "year_month", name="uq_comparacao_loc_month"),
)
```

From src/web/app.py:
```python
app.state.db_engine = engine  # set in lifespan
app.state.templates = templates  # Jinja2Templates
app.state.project_root = PROJECT_ROOT
app.state.config_path = PROJECT_ROOT / "config" / "system.json"
```

From src/web/routes/dashboard.py (existing pattern):
```python
router = APIRouter()
@router.get("/", response_class=HTMLResponse)
async def homepage(request: Request): ...
```

From src/web/routes/custos_reais.py (existing pattern for HTMX POST):
```python
router = APIRouter()
@router.post("/local/{local_id}/custo-real", response_class=HTMLResponse)
async def save_custo_real(request: Request, local_id: str, ...): ...
```

From src/web/services/data_loader.py (current load_locations — reads config.json only):
```python
def load_locations(config_path: Path) -> list:
    """Le config/system.json e retorna locations[]."""
    try:
        with open(config_path, encoding="utf-8") as f:
            config = json.load(f)
        return config.get("locations", [])
    except (FileNotFoundError, json.JSONDecodeError):
        return []
```

From src/web/routes/dashboard.py (how load_locations is consumed):
```python
locations = load_locations(config_path)
selected_location = locations[0] if locations else {}
# Uses: location["id"], location["name"], location["pipeline"], location["current_contract"]["supplier"]
```

UI-SPEC copywriting:
- Confirmacao XLSX: "Importados [N] meses para [local] (CPE: [CPE]). Periodo: [mes inicio] a [mes fim]."
- Upload idempotente: "Dados ja importados para [mes/ano]. Nenhuma alteracao efectuada."
- Erro CPE: "CPE nao detectado no ficheiro. Associe o ficheiro ao local manualmente."
- Erro XLSX invalido: "Ficheiro nao reconhecido como exportacao E-REDES. Verifique o formato e tente novamente."
- CTA upload: "Importar XLSX"
- CTA criar local: "Criar Local"
- Empty state locais: "Nenhum local configurado. Crie um local com o CPE da E-REDES."

UI-SPEC colors:
- Accent button: #0d6efd (submit buttons)
- Success message: #198754
- Danger message: #dc3545
- Border: #dee2e6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload route + templates HTMX + background task tiagofelicia (idempotente)</name>
  <files>src/web/routes/upload.py, src/web/templates/partials/upload_xlsx.html, src/web/templates/partials/upload_confirmacao.html</files>
  <read_first>
    - src/web/routes/custos_reais.py
    - src/web/templates/partials/custo_form.html
    - src/web/templates/base.html
    - src/web/services/ingestao_xlsx.py
    - src/backend/tiagofelicia_compare.py
    - src/db/schema.py
  </read_first>
  <action>
1. Criar `src/web/routes/upload.py`:

```python
"""Routes de upload de ficheiros XLSX E-REDES."""
import os
import tempfile
from pathlib import Path

from fastapi import APIRouter, BackgroundTasks, File, Request, UploadFile
from fastapi.responses import HTMLResponse

from src.web.services.ingestao_xlsx import ingerir_xlsx

router = APIRouter()


def _consultar_tiagofelicia_bg(location_id: str, engine):
    """Background task: consulta tiagofelicia.pt e guarda resultado em comparacoes.

    Funcao sincrona (nao async def) — FastAPI corre automaticamente em thread pool.
    Se playwright nao estiver disponivel, loga aviso e retorna sem erro.
    Insere com ON CONFLICT DO NOTHING para idempotencia (UniqueConstraint uq_comparacao_loc_month).
    """
    import json
    import logging
    from datetime import datetime, timezone
    from sqlalchemy.dialects.sqlite import insert as sqlite_insert
    from src.db.schema import comparacoes, consumo_mensal
    from sqlalchemy import select

    logger = logging.getLogger("upload")

    try:
        from src.backend.tiagofelicia_compare import analyse_with_tiago
    except ImportError:
        logger.warning("playwright nao disponivel — consulta tiagofelicia.pt ignorada")
        return

    # Ler ultimo mes de consumo para este local
    with engine.connect() as conn:
        row = conn.execute(
            select(consumo_mensal)
            .where(consumo_mensal.c.location_id == location_id)
            .order_by(consumo_mensal.c.year_month.desc())
            .limit(1)
        ).fetchone()

    if not row:
        logger.warning(f"Sem dados de consumo para {location_id} — tiagofelicia ignorado")
        return

    try:
        result = analyse_with_tiago(
            total_kwh=row.total_kwh,
            vazio_kwh=row.vazio_kwh,
            fora_vazio_kwh=row.fora_vazio_kwh,
        )
        with engine.begin() as conn:
            stmt = sqlite_insert(comparacoes).values(
                location_id=location_id,
                year_month=row.year_month,
                top_3_json=json.dumps(result.get("top_3", []), ensure_ascii=False),
                current_supplier_result_json=json.dumps(
                    result.get("current_supplier_result"), ensure_ascii=False
                ),
                generated_at=result.get("generated_at", ""),
                cached_at=datetime.now(timezone.utc),
            ).on_conflict_do_nothing(
                index_elements=["location_id", "year_month"]
            )
            conn.execute(stmt)
        logger.info(f"tiagofelicia.pt consultado com sucesso para {location_id}")
    except Exception:
        logger.exception(f"Erro ao consultar tiagofelicia.pt para {location_id}")


@router.post("/upload/xlsx", response_class=HTMLResponse)
async def upload_xlsx(
    request: Request,
    background_tasks: BackgroundTasks,
    ficheiro: UploadFile = File(...),
):
    """Recebe XLSX E-REDES, ingere em SQLite, lanca tiagofelicia em background."""
    templates = request.app.state.templates
    engine = request.app.state.db_engine

    # Guardar temporariamente (openpyxl requer ficheiro no disco)
    with tempfile.NamedTemporaryFile(suffix=".xlsx", delete=False) as tmp:
        content = await ficheiro.read()
        tmp.write(content)
        tmp_path = Path(tmp.name)

    try:
        resultado = ingerir_xlsx(tmp_path, ficheiro.filename, engine)
    finally:
        os.unlink(tmp_path)

    if resultado["erro"]:
        return templates.TemplateResponse(
            request=request,
            name="partials/upload_confirmacao.html",
            context={"erro": resultado["erro"], "resultado": None},
        )

    # Lancar consulta tiagofelicia em background (nao bloqueia resposta)
    background_tasks.add_task(_consultar_tiagofelicia_bg, resultado["location_id"], engine)

    return templates.TemplateResponse(
        request=request,
        name="partials/upload_confirmacao.html",
        context={"erro": None, "resultado": resultado},
    )
```

2. Criar `src/web/templates/partials/upload_xlsx.html` seguindo UI-SPEC:

```html
<!-- Upload XLSX E-REDES — conforme UI-SPEC Phase 6 -->
<div class="card" style="margin-bottom: 2rem;">
  <h2>Importar XLSX E-REDES</h2>
  <form hx-post="/upload/xlsx"
        hx-target="#upload-xlsx-resultado"
        hx-swap="innerHTML"
        hx-encoding="multipart/form-data"
        hx-indicator="#upload-xlsx-spinner">
    <div style="display: flex; align-items: center; gap: 0.75rem; flex-wrap: wrap;">
      <input type="file" name="ficheiro" accept=".xlsx" required id="xlsx-input">
      <button type="submit" class="btn-primary">Importar XLSX</button>
      <span id="upload-xlsx-spinner" class="htmx-indicator" style="color: var(--muted, #6c757d);">A processar...</span>
    </div>
  </form>
  <div id="upload-xlsx-resultado" style="margin-top: 1rem;"></div>
</div>
```

3. Criar `src/web/templates/partials/upload_confirmacao.html`:

```html
{% if erro %}
<div style="padding: 0.75rem 1rem; background: #f8d7da; border: 1px solid #f5c6cb; border-radius: 4px; color: #721c24;">
  {{ erro }}
</div>
{% elif resultado %}
  {% if resultado.meses_inseridos > 0 %}
  <div style="padding: 0.75rem 1rem; background: #d4edda; border: 1px solid #c3e6cb; border-radius: 4px; color: #155724;">
    Importados {{ resultado.meses_inseridos }} meses para {{ resultado.location_name }} (CPE: {{ resultado.cpe }}). Periodo: {{ resultado.periodo_inicio }} a {{ resultado.periodo_fim }}.
  </div>
  {% else %}
  <div style="padding: 0.75rem 1rem; background: #e2e3e5; border: 1px solid #d6d8db; border-radius: 4px; color: #383d41;">
    Dados ja importados para este periodo. Nenhuma alteracao efectuada.
  </div>
  {% endif %}
{% endif %}
```
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "from src.web.routes.upload import router; print('upload router ok')" && test -f src/web/templates/partials/upload_xlsx.html && test -f src/web/templates/partials/upload_confirmacao.html && echo "templates ok"</automated>
  </verify>
  <done>Endpoint POST /upload/xlsx funcional com feedback HTMX, background task tiagofelicia com insercao idempotente em comparacoes, graceful degradation quando playwright indisponivel</done>
</task>

<task type="auto">
  <name>Task 2: Locais routes + app.py wiring dos novos routers</name>
  <files>src/web/routes/locais.py, src/web/templates/partials/locais_form.html, src/web/app.py</files>
  <read_first>
    - src/web/routes/dashboard.py
    - src/web/routes/custos_reais.py
    - src/web/templates/base.html
    - src/web/app.py
    - src/web/services/locais_service.py
  </read_first>
  <action>
1. Criar `src/web/routes/locais.py`:

```python
"""Routes de gestao de locais."""
import re
from fastapi import APIRouter, Form, Request
from fastapi.responses import HTMLResponse

from src.web.services.locais_service import (
    create_local,
    get_all_locais,
    get_local_by_id,
    update_fornecedor,
)

router = APIRouter()


def _slugify(name: str) -> str:
    """Gera slug a partir do nome (ex: 'Escritorio Porto' -> 'escritorio-porto')."""
    slug = name.lower().strip()
    slug = re.sub(r"[^a-z0-9]+", "-", slug)
    return slug.strip("-")


@router.get("/locais", response_class=HTMLResponse)
async def locais_page(request: Request):
    """Pagina de gestao de locais: formulario + lista."""
    templates = request.app.state.templates
    engine = request.app.state.db_engine
    all_locais = get_all_locais(engine)
    return templates.TemplateResponse(
        request=request,
        name="partials/locais_form.html",
        context={"locais": all_locais, "erro": None, "sucesso": None},
    )


@router.post("/locais", response_class=HTMLResponse)
async def criar_local(
    request: Request,
    name: str = Form(...),
    cpe: str = Form(...),
):
    """Cria novo local via formulario HTMX."""
    templates = request.app.state.templates
    engine = request.app.state.db_engine

    local_id = _slugify(name)
    if not local_id:
        all_locais = get_all_locais(engine)
        return templates.TemplateResponse(
            request=request,
            name="partials/locais_form.html",
            context={"locais": all_locais, "erro": "Nome invalido.", "sucesso": None},
        )

    try:
        novo = create_local(local_id, name, cpe.strip(), engine)
    except ValueError as e:
        all_locais = get_all_locais(engine)
        return templates.TemplateResponse(
            request=request,
            name="partials/locais_form.html",
            context={"locais": all_locais, "erro": str(e), "sucesso": None},
        )

    all_locais = get_all_locais(engine)
    return templates.TemplateResponse(
        request=request,
        name="partials/locais_form.html",
        context={
            "locais": all_locais,
            "erro": None,
            "sucesso": f"Local '{novo['name']}' criado com sucesso.",
        },
    )


@router.post("/locais/{local_id}/fornecedor", response_class=HTMLResponse)
async def editar_fornecedor(
    request: Request,
    local_id: str,
    supplier: str = Form(...),
    plan_contains: str = Form(""),
):
    """Actualiza fornecedor actual de um local."""
    templates = request.app.state.templates
    engine = request.app.state.db_engine

    result = update_fornecedor(local_id, supplier.strip(), plan_contains.strip() or None, engine)
    all_locais = get_all_locais(engine)

    if result is None:
        return templates.TemplateResponse(
            request=request,
            name="partials/locais_form.html",
            context={"locais": all_locais, "erro": f"Local '{local_id}' nao encontrado.", "sucesso": None},
        )

    return templates.TemplateResponse(
        request=request,
        name="partials/locais_form.html",
        context={
            "locais": all_locais,
            "erro": None,
            "sucesso": f"Fornecedor de '{result['name']}' actualizado para '{supplier}'.",
        },
    )
```

2. Criar `src/web/templates/partials/locais_form.html`:

```html
<div class="card" style="margin-bottom: 2rem;">
  <h2>Gerir Locais</h2>

  {% if erro %}
  <div style="padding: 0.75rem 1rem; background: #f8d7da; border: 1px solid #f5c6cb; border-radius: 4px; color: #721c24; margin-bottom: 1rem;">
    {{ erro }}
  </div>
  {% endif %}
  {% if sucesso %}
  <div style="padding: 0.75rem 1rem; background: #d4edda; border: 1px solid #c3e6cb; border-radius: 4px; color: #155724; margin-bottom: 1rem;">
    {{ sucesso }}
  </div>
  {% endif %}

  <h3 style="font-size: 1rem; margin-top: 1.5rem;">Criar novo local</h3>
  <form hx-post="/locais" hx-target="#locais-container" hx-swap="innerHTML">
    <div style="display: flex; gap: 0.75rem; flex-wrap: wrap; align-items: end; margin-bottom: 1rem;">
      <div>
        <label for="loc-name" style="font-size: 0.85rem; color: var(--muted, #6c757d);">Nome</label><br>
        <input type="text" id="loc-name" name="name" required placeholder="Ex: Escritorio"
               style="padding: 0.5rem 0.75rem; border: 1px solid #dee2e6; border-radius: 4px;">
      </div>
      <div>
        <label for="loc-cpe" style="font-size: 0.85rem; color: var(--muted, #6c757d);">CPE</label><br>
        <input type="text" id="loc-cpe" name="cpe" required placeholder="PT000..."
               style="padding: 0.5rem 0.75rem; border: 1px solid #dee2e6; border-radius: 4px;">
      </div>
      <button type="submit" class="btn-primary">Criar Local</button>
    </div>
  </form>

  {% if locais %}
  <h3 style="font-size: 1rem; margin-top: 1.5rem;">Locais configurados</h3>
  <table style="width: 100%; border-collapse: collapse;">
    <thead>
      <tr>
        <th style="text-align: left; padding: 0.5rem 0.75rem; border-bottom: 2px solid #dee2e6; font-size: 0.85rem; text-transform: uppercase; color: var(--muted, #6c757d);">Nome</th>
        <th style="text-align: left; padding: 0.5rem 0.75rem; border-bottom: 2px solid #dee2e6; font-size: 0.85rem; text-transform: uppercase; color: var(--muted, #6c757d);">CPE</th>
        <th style="text-align: left; padding: 0.5rem 0.75rem; border-bottom: 2px solid #dee2e6; font-size: 0.85rem; text-transform: uppercase; color: var(--muted, #6c757d);">Fornecedor Actual</th>
        <th style="text-align: left; padding: 0.5rem 0.75rem; border-bottom: 2px solid #dee2e6; font-size: 0.85rem; text-transform: uppercase; color: var(--muted, #6c757d);">Accoes</th>
      </tr>
    </thead>
    <tbody>
      {% for loc in locais %}
      <tr>
        <td style="padding: 0.5rem 0.75rem; border-bottom: 1px solid #dee2e6;">{{ loc.name }}</td>
        <td style="padding: 0.5rem 0.75rem; border-bottom: 1px solid #dee2e6; font-size: 0.85rem;">{{ loc.cpe }}</td>
        <td style="padding: 0.5rem 0.75rem; border-bottom: 1px solid #dee2e6;">{{ loc.current_supplier or '—' }}</td>
        <td style="padding: 0.5rem 0.75rem; border-bottom: 1px solid #dee2e6;">
          <form hx-post="/locais/{{ loc.id }}/fornecedor" hx-target="#locais-container" hx-swap="innerHTML"
                style="display: flex; gap: 0.5rem; align-items: center;">
            <input type="text" name="supplier" placeholder="Fornecedor" value="{{ loc.current_supplier or '' }}"
                   style="padding: 0.25rem 0.5rem; border: 1px solid #dee2e6; border-radius: 4px; width: 140px; font-size: 0.85rem;">
            <input type="text" name="plan_contains" placeholder="Plano (opcional)" value="{{ loc.current_plan_contains or '' }}"
                   style="padding: 0.25rem 0.5rem; border: 1px solid #dee2e6; border-radius: 4px; width: 140px; font-size: 0.85rem;">
            <button type="submit" style="padding: 0.25rem 0.75rem; font-size: 0.85rem; background: var(--accent, #0d6efd); color: white; border: none; border-radius: 4px; cursor: pointer;">Guardar</button>
          </form>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  {% else %}
  <p style="color: var(--muted, #6c757d); margin-top: 1rem;">Nenhum local configurado. Crie um local com o CPE da E-REDES.</p>
  {% endif %}
</div>
```

3. Em `src/web/app.py`, registar os novos routers. Adicionar junto aos imports existentes de routers:

```python
from src.web.routes.upload import router as upload_router
from src.web.routes.locais import router as locais_router

app.include_router(upload_router)
app.include_router(locais_router)
```

4. Garantir que `src/web/routes/__init__.py` existe (deve ja existir).
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "from src.web.app import app; routes = [r.path for r in app.routes]; assert '/upload/xlsx' in routes, f'missing /upload/xlsx in {routes}'; assert '/locais' in routes, f'missing /locais in {routes}'; print('all routes registered')" && pytest tests/test_web_upload.py tests/test_web_locais.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Endpoints de locais registados, templates HTMX funcionais, routers wired na app, pytest stubs executam (skipped)</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard wiring — upload section + link gerir locais</name>
  <files>src/web/templates/dashboard.html</files>
  <read_first>
    - src/web/templates/dashboard.html
  </read_first>
  <action>
1. Em `src/web/templates/dashboard.html`, adicionar a seccao de upload ENTRE o grid de graficos e a tabela de ranking. Procurar o bloco que inclui os graficos (`consumo_chart.html` e `custo_chart.html`) e DEPOIS dele, antes da tabela de ranking, inserir:

```html
<!-- Seccao de Upload — Phase 7 -->
{% include "partials/upload_xlsx.html" %}
```

2. Tambem adicionar link "Gerir locais" ao lado do selector de local existente. Procurar o `<select` do selector de locais e adicionar depois dele:

```html
<a href="/locais" style="font-size: 0.85rem; color: var(--muted, #6c757d); margin-left: 0.75rem;">Gerir locais</a>
```

NAO alterar mais nada no dashboard.html — apenas estas duas insercoes cirurgicas para minimizar risco de regressao no dashboard Phase 4.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "
content = open('src/web/templates/dashboard.html').read()
assert 'upload_xlsx.html' in content, 'missing upload include'
assert 'Gerir locais' in content, 'missing link gerir locais'
print('dashboard wiring ok')
"</automated>
  </verify>
  <done>Dashboard inclui seccao upload XLSX e link gerir locais, sem alteracoes ao layout existente</done>
</task>

<task type="auto">
  <name>Task 4: Dockerfile + requirements-docker.txt — adicionar playwright para COMP-01</name>
  <files>Dockerfile, requirements-docker.txt</files>
  <read_first>
    - Dockerfile
    - requirements-docker.txt
  </read_first>
  <action>
1. Em `requirements-docker.txt`, adicionar `playwright==1.49.1` ao final (ou versao mais recente disponivel — verificar com `pip index versions playwright 2>/dev/null || echo "usar 1.49.1"`).

2. No `Dockerfile`, DEPOIS do `RUN pip install -r requirements-docker.txt` e ANTES do `COPY . .` (ou bloco equivalente), adicionar:

```dockerfile
# Instalar Chromium para playwright (necessario para tiagofelicia.pt — COMP-01)
RUN playwright install chromium --with-deps
```

NOTA: `playwright install chromium --with-deps` instala o Chromium binary E as dependencias de sistema (libs X11, fontes, etc.) necessarias para correr headless em Linux. Isto adiciona ~500MB a imagem mas e necessario para COMP-01.

Se o Dockerfile usa multi-stage build, garantir que o comando `playwright install` corre no stage final (onde a app executa), nao num build stage descartavel.

3. Verificar que o Dockerfile nao tem `ENV PLAYWRIGHT_BROWSERS_PATH` — o default (`~/.cache/ms-playwright`) e correcto para o container.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && grep -q "playwright" requirements-docker.txt && echo "playwright in requirements" && grep -q "playwright install chromium" Dockerfile && echo "playwright install in Dockerfile"</automated>
  </verify>
  <done>Container Docker inclui playwright + Chromium para consulta tiagofelicia.pt em background</done>
</task>

<task type="auto">
  <name>Task 5: Ligar load_locations() ao SQLite — locais criados via UI aparecem no dashboard selector</name>
  <files>src/web/services/data_loader.py</files>
  <read_first>
    - src/web/services/data_loader.py
    - src/web/services/locais_service.py
    - src/web/routes/dashboard.py
    - config/system.json
  </read_first>
  <action>
Addresses checker blocker: "Dashboard location selector reads from config/system.json but locais created via Phase 7 UI are stored in SQLite. Locals created in Phase 7 will not appear in the dashboard selector."

Abordagem: Modificar `load_locations()` em `data_loader.py` para fazer merge de locais do config.json COM locais do SQLite. Locais do config.json mantem o seu formato original (com `pipeline` e `current_contract` — necessarios para o dashboard existente). Locais que existem apenas em SQLite (criados via UI) sao adicionados com um formato compativel (sem `pipeline`, com `current_contract` mapeado dos campos flat do SQLite).

O dashboard existente usa `location["pipeline"]` para carregar dados CSV — locais criados via UI ainda nao terao dados CSV (so tem dados em SQLite via upload). Isto e aceitavel para Phase 7: o local aparece no selector, mas os graficos so serao populados quando Phase 9 migrar a leitura de dados de CSV para SQLite.

1. Em `src/web/services/data_loader.py`, modificar `load_locations()`:

```python
def load_locations(config_path: Path, engine=None) -> list:
    """Le locais de config/system.json e complementa com locais do SQLite.

    Locais de config.json mantem formato original (com pipeline, current_contract).
    Locais que existem apenas em SQLite (criados via UI Phase 7) sao adicionados
    com formato compativel — aparecem no selector mas podem nao ter pipeline data.

    Args:
        config_path: Path para o ficheiro config/system.json.
        engine: SQLAlchemy engine (opcional). Se fornecido, merge com SQLite.

    Returns:
        Lista de dicts com os locais configurados. Retorna [] se nenhuma fonte tem dados.
    """
    # 1. Ler config.json (fonte original)
    config_locations = []
    try:
        with open(config_path, encoding="utf-8") as f:
            config = json.load(f)
        config_locations = config.get("locations", [])
    except (FileNotFoundError, json.JSONDecodeError):
        pass

    # 2. Se engine fornecido, merge com SQLite
    if engine is not None:
        try:
            from src.web.services.locais_service import get_all_locais
            sqlite_locais = get_all_locais(engine)
        except Exception:
            return config_locations

        # IDs ja presentes no config.json
        config_ids = {loc["id"] for loc in config_locations}

        # Adicionar locais do SQLite que nao existem no config.json
        for loc in sqlite_locais:
            if loc["id"] not in config_ids:
                config_locations.append({
                    "id": loc["id"],
                    "name": loc["name"],
                    "cpe": loc.get("cpe", ""),
                    "current_contract": {
                        "supplier": loc.get("current_supplier", ""),
                        "current_plan_contains": loc.get("current_plan_contains", ""),
                        "power_label": loc.get("power_label", ""),
                    },
                    # pipeline ausente — dashboard deve tratar gracefully
                })

    return config_locations
```

2. Em `src/web/routes/dashboard.py`, actualizar as chamadas a `load_locations()` para passar o engine. Nas funcoes `homepage` e `local_dashboard`, mudar:

```python
# ANTES:
locations = load_locations(config_path)

# DEPOIS:
locations = load_locations(config_path, engine=request.app.state.db_engine)
```

3. Em `src/web/routes/dashboard.py`, no metodo `_load_location_data()`, adicionar guard para locais sem `pipeline`:

```python
# No inicio de _load_location_data, ANTES de aceder a location["pipeline"]:
if "pipeline" not in location:
    # Local criado via UI (Phase 7) — sem dados de pipeline CSV
    # Retornar dados vazios — Phase 9 migrara para leitura directa de SQLite
    return {
        "consumo_data": [],
        "analysis": None,
        "freshness": get_freshness_info(None),
        "custos_reais": {},
        "consumo_chart": {"labels": [], "vazio_data": [], "fora_vazio_data": []},
        "custo_chart": {"labels": [], "estimativa_data": [], "custo_real_data": []},
        "ranking": [],
        "recommendation": {"show": False},
    }
```

4. Em `src/web/routes/custos_reais.py`, tambem actualizar a chamada a `load_locations()` se existir — verificar e passar `engine=request.app.state.db_engine` se aplicavel.

NOTA: Nao alterar a assinatura das outras funcoes de data_loader (load_consumo_csv, etc.) — essas continuam a ler de CSV por agora.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "
from src.web.services.data_loader import load_locations
import inspect
sig = inspect.signature(load_locations)
assert 'engine' in sig.parameters, f'engine param missing from load_locations: {sig}'
print('load_locations has engine param')
" && python -c "
content = open('src/web/routes/dashboard.py').read()
assert 'engine=' in content, 'dashboard not passing engine to load_locations'
assert 'pipeline' in content and 'not in' in content, 'missing pipeline guard'
print('dashboard wiring ok')
"</automated>
  </verify>
  <done>load_locations() le de config.json + SQLite, locais criados via UI aparecem no dashboard selector, locais sem pipeline tratados gracefully com dados vazios</done>
</task>

</tasks>

<verification>
- `python -c "from src.web.app import app; routes = [r.path for r in app.routes]; print(routes)"` — confirma /upload/xlsx, /locais, /locais/{local_id}/fornecedor registados
- `test -f src/web/templates/partials/upload_xlsx.html` — template upload existe
- `test -f src/web/templates/partials/locais_form.html` — template locais existe
- `grep "on_conflict_do_nothing" src/web/routes/upload.py` — insercao idempotente em comparacoes
- `grep "playwright" requirements-docker.txt` — playwright incluido
- `grep "playwright install chromium" Dockerfile` — Chromium instalado no container
- `python -c "from src.web.services.data_loader import load_locations; import inspect; assert 'engine' in inspect.signature(load_locations).parameters"` — load_locations aceita engine
- `pytest tests/test_web_upload.py tests/test_web_locais.py -x -q` — test stubs executam (skipped)
- `pytest tests/ -x -q 2>&1 | tail -5` — testes existentes nao quebrados
</verification>

<success_criteria>
1. POST /upload/xlsx aceita ficheiro XLSX, retorna HTML partial com confirmacao (meses importados, local, CPE) ou erro
2. Background task tiagofelicia registada (sem bloquear resposta), insere com ON CONFLICT DO NOTHING
3. GET /locais mostra formulario + lista de locais
4. POST /locais cria local novo
5. POST /locais/{id}/fornecedor actualiza fornecedor
6. Dashboard inclui seccao upload XLSX e link gerir locais (alteracao isolada)
7. Dockerfile tem playwright para COMP-01
8. Locais criados via UI aparecem no dashboard location selector (CONF-01 criterion 4)
</success_criteria>

<output>
After completion, create `.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-02-SUMMARY.md`
</output>
