---
phase: 07-upload-xlsx-ingest-o-de-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_web_upload.py
  - tests/test_ingestao_xlsx.py
  - tests/test_web_locais.py
  - tests/test_comparacao.py
  - tests/conftest.py
  - src/db/schema.py
  - src/db/migrations/versions/002_add_locais.py
  - src/web/app.py
  - src/backend/eredes_to_monthly_csv.py
  - src/web/services/ingestao_xlsx.py
  - src/web/services/locais_service.py
autonomous: true
requirements: [CONF-01, CONF-02, UPLD-02, UPLD-05, COMP-02]

must_haves:
  truths:
    - "Tabela locais existe em SQLite com colunas id, name, cpe, current_supplier, current_plan_contains, power_label, created_at"
    - "Locais de config/system.json sao automaticamente copiados para SQLite se tabela estiver vazia no arranque"
    - "parse_xlsx_to_dict() retorna dict[str, dict] com year_month como chave e total_kwh/vazio_kwh/fora_vazio_kwh como valores"
    - "ingerir_xlsx() escreve dados em consumo_mensal com idempotencia (INSERT OR IGNORE)"
    - "locais_service permite criar, listar e actualizar fornecedor de um local"
    - "Test stubs existem para todos os requisitos Phase 7 antes de qualquer implementacao"
  artifacts:
    - path: "tests/test_web_upload.py"
      provides: "Test stubs para endpoint POST /upload/xlsx"
      contains: "def test_"
    - path: "tests/test_ingestao_xlsx.py"
      provides: "Test stubs para ingestao XLSX e idempotencia"
      contains: "def test_"
    - path: "tests/test_web_locais.py"
      provides: "Test stubs para endpoints de gestao de locais"
      contains: "def test_"
    - path: "tests/test_comparacao.py"
      provides: "Test stubs para comparacoes tiagofelicia"
      contains: "def test_"
    - path: "src/db/schema.py"
      provides: "Tabela locais definida em SQLAlchemy Core"
      contains: "locais = Table"
    - path: "src/db/migrations/versions/002_add_locais.py"
      provides: "Alembic migration para tabela locais"
      contains: "op.create_table"
    - path: "src/web/services/ingestao_xlsx.py"
      provides: "Servico de ingestao XLSX para SQLite"
      exports: ["ingerir_xlsx"]
    - path: "src/web/services/locais_service.py"
      provides: "CRUD de locais em SQLite"
      exports: ["get_all_locais", "get_local_by_cpe", "create_local", "update_fornecedor"]
    - path: "src/backend/eredes_to_monthly_csv.py"
      provides: "parse_xlsx_to_dict() reutilizavel"
      contains: "def parse_xlsx_to_dict"
  key_links:
    - from: "src/web/services/ingestao_xlsx.py"
      to: "src/backend/eredes_to_monthly_csv.py"
      via: "import parse_xlsx_to_dict"
      pattern: "from src.backend.eredes_to_monthly_csv import parse_xlsx_to_dict"
    - from: "src/web/services/ingestao_xlsx.py"
      to: "src/db/schema.py"
      via: "import consumo_mensal table"
      pattern: "from src.db.schema import consumo_mensal"
    - from: "src/web/services/locais_service.py"
      to: "src/db/schema.py"
      via: "import locais table"
      pattern: "from src.db.schema import locais"
    - from: "src/web/app.py"
      to: "src/web/services/locais_service.py"
      via: "seed locais from config on lifespan"
      pattern: "_seed_locais_from_config"
---

<objective>
Criar test stubs Wave 0 para todos os requisitos Phase 7, seguidos da camada de dados e servicos: tabela `locais` em SQLite, refactoring do parser XLSX para uso em memoria, e servicos de ingestao e gestao de locais.

Purpose: Estabelecer cobertura de testes desde o inicio (Nyquist compliance) e criar os contratos e servicos que os endpoints HTTP (Plan 02) vao consumir. Sem esta camada, upload e gestao de locais nao tem onde escrever.
Output: Test stubs + Tabela locais + migracao Alembic + parse_xlsx_to_dict() + ingestao_xlsx service + locais_service
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/db/schema.py:
```python
from sqlalchemy import MetaData, Table, Column, Integer, String, Float, DateTime, Text, UniqueConstraint
metadata = MetaData()

consumo_mensal = Table(
    "consumo_mensal", metadata,
    Column("id", Integer, primary_key=True),
    Column("location_id", String(64), nullable=False),
    Column("year_month", String(7), nullable=False),
    Column("total_kwh", Float, nullable=False),
    Column("vazio_kwh", Float, nullable=False),
    Column("fora_vazio_kwh", Float, nullable=False),
    UniqueConstraint("location_id", "year_month", name="uq_consumo_loc_month"),
)

comparacoes = Table(
    "comparacoes", metadata,
    Column("id", Integer, primary_key=True),
    Column("location_id", String(64), nullable=False),
    Column("year_month", String(7), nullable=False),
    Column("top_3_json", Text),
    Column("current_supplier_result_json", Text),
    Column("generated_at", String(32)),
    Column("cached_at", DateTime, default=lambda: datetime.now(timezone.utc)),
)
```

From src/db/engine.py:
```python
def get_engine(db_path: str | None = None):
    """Cria engine SQLAlchemy para SQLite com WAL mode."""
engine = get_engine()  # singleton
```

From src/backend/eredes_to_monthly_csv.py:
```python
def convert_xlsx_to_monthly_csv(input_path: Path, output_path: Path, drop_partial_last_month: bool = False) -> Path:
    # Writes CSV to disk. Internal logic: load_workbook -> pick_sheet -> detect_data_start_row -> iterate rows -> monthly dict -> write CSV
def pick_sheet(workbook) -> Any:  # Bug: Any not imported
def detect_data_start_row(worksheet) -> int:
def extract_date_time_and_kwh(row, date_idx, time_idx) -> tuple | None:
def is_daily_cycle_vazio(local_dt: datetime) -> bool:
MIN_MONTHLY_KWH = 30
MAX_MONTHLY_KWH = 5000
```

From src/backend/cpe_routing.py:
```python
def extract_cpe_from_filename(filename: str) -> str | None:
def find_location_by_cpe(locations: list[dict], cpe: str) -> dict | None:
```

From src/web/app.py (lifespan):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    metadata.create_all(engine)
    app.state.db_engine = engine
    yield
```

From config/system.json (locations structure):
```json
{
  "locations": [
    {
      "id": "casa",
      "name": "Casa",
      "cpe": "PT0002000084968079SX",
      "current_contract": {
        "supplier": "Meo Energia",
        "current_plan_contains": "Tarifa Variavel",
        "power_label": "10.35 kVA"
      }
    }
  ]
}
```

From tests/conftest.py (existing fixtures):
```python
@pytest.fixture
def web_client(sample_config_json):
    """FastAPI TestClient com config mock via override de app.state.config_path."""
    from fastapi.testclient import TestClient
    from src.web.app import app
    app.state.config_path = sample_config_json
    client = TestClient(app)
    return client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — Criar test stubs para todos os requisitos Phase 7</name>
  <files>tests/test_web_upload.py, tests/test_ingestao_xlsx.py, tests/test_web_locais.py, tests/test_comparacao.py, tests/conftest.py</files>
  <action>
1. Criar `tests/test_web_upload.py` com stubs pytest para UPLD-01 e COMP-01:

```python
"""Tests para endpoint POST /upload/xlsx (UPLD-01, COMP-01)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_upload_xlsx_ok(web_client):
    """UPLD-01: Upload de ficheiro XLSX via POST /upload/xlsx retorna HTTP 200."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_upload_xlsx_cpe_nao_detectado(web_client):
    """UPLD-05 fallback: Upload com ficheiro sem CPE no nome retorna erro."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_background_task_registada(web_client):
    """COMP-01: Apos upload, BackgroundTask e registada (nao bloqueia response)."""
    pass
```

2. Criar `tests/test_ingestao_xlsx.py` com stubs para UPLD-02, UPLD-05, idempotencia:

```python
"""Tests para servico de ingestao XLSX (UPLD-02, UPLD-05, idempotencia)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 2+3")
def test_ingestao_escreve_sqlite():
    """UPLD-02: Apos ingestao, linha inserida em consumo_mensal SQLite."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 2+3")
def test_cpe_routing():
    """UPLD-05: CPE detectado do nome de ficheiro resolve location_id correcto."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 2+3")
def test_idempotencia():
    """Segundo upload do mesmo XLSX nao duplica dados (INSERT OR IGNORE)."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 2+3")
def test_cpe_nao_detectado():
    """Upload com ficheiro sem CPE no nome retorna erro util."""
    pass
```

3. Criar `tests/test_web_locais.py` com stubs para CONF-01, CONF-02:

```python
"""Tests para endpoints de gestao de locais (CONF-01, CONF-02)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_criar_local(web_client):
    """CONF-01: POST /locais cria local e fica visivel."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_editar_fornecedor(web_client):
    """CONF-02: POST /locais/{id}/fornecedor actualiza current_supplier."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar em Plan 02")
def test_listar_locais(web_client):
    """CONF-01: GET /locais retorna lista de locais."""
    pass
```

4. Criar `tests/test_comparacao.py` com stubs para COMP-02:

```python
"""Tests para armazenamento de comparacoes tiagofelicia (COMP-02)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 1 Plan 02")
def test_comparacao_guardada():
    """COMP-02: Resultado tiagofelicia gravado em comparacoes com cached_at."""
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementar apos Task 1 Plan 02")
def test_comparacao_idempotente():
    """Segundo upload para mesmo (location_id, year_month) nao duplica comparacao."""
    pass
```

5. Em `tests/conftest.py`, adicionar fixture `db_engine_test` para SQLite in-memory, DEPOIS das fixtures existentes:

```python
@pytest.fixture
def db_engine_test():
    """SQLAlchemy engine para SQLite in-memory — tabelas Phase 7."""
    from sqlalchemy import create_engine
    from src.db.schema import metadata
    engine = create_engine("sqlite:///:memory:")
    metadata.create_all(engine)
    yield engine
    engine.dispose()
```

NAO modificar fixtures existentes. Apenas acrescentar ao final do ficheiro.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && pytest tests/test_web_upload.py tests/test_ingestao_xlsx.py tests/test_web_locais.py tests/test_comparacao.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>4 ficheiros de test stubs criados, todos os testes passam como skipped, fixture db_engine_test disponivel em conftest.py</done>
</task>

<task type="auto">
  <name>Task 1: Tabela locais em schema.py + Alembic migration 002 + seed no lifespan</name>
  <files>src/db/schema.py, src/db/migrations/versions/002_add_locais.py, src/web/app.py</files>
  <read_first>
    - src/db/schema.py
    - src/db/migrations/versions/001_initial_schema.py
    - src/web/app.py
    - config/system.json
  </read_first>
  <action>
1. Em `src/db/schema.py`, adicionar import `from datetime import datetime, timezone` no topo (se nao existir) e adicionar a tabela `locais` DEPOIS da tabela `custos_reais`:

```python
locais = Table(
    "locais", metadata,
    Column("id", String(64), primary_key=True),
    Column("name", String(128), nullable=False),
    Column("cpe", String(64), nullable=False, unique=True),
    Column("current_supplier", String(128)),
    Column("current_plan_contains", String(128)),
    Column("power_label", String(32)),
    Column("created_at", DateTime, default=lambda: datetime.now(timezone.utc)),
)
```

Tambem adicionar `UniqueConstraint` a tabela `comparacoes` se nao existir — necessario para idempotencia de insercoes tiagofelicia (evitar duplicados para mesmo location_id + year_month):

```python
# Na definicao de comparacoes, adicionar:
UniqueConstraint("location_id", "year_month", name="uq_comparacao_loc_month"),
```

2. Criar `src/db/migrations/versions/002_add_locais.py` com:
- `revision = '002'`
- `down_revision = '001'`
- `upgrade()`: `op.create_table('locais', ...)` com todas as colunas acima. Tambem adicionar UniqueConstraint em comparacoes: `op.create_unique_constraint('uq_comparacao_loc_month', 'comparacoes', ['location_id', 'year_month'])`
- `downgrade()`: `op.drop_table('locais')` e `op.drop_constraint('uq_comparacao_loc_month', 'comparacoes')`

3. Em `src/web/app.py`, adicionar funcao `_seed_locais_from_config(engine, config_path: Path)` que:
- Le `config/system.json` e extrai a lista `locations`
- Faz `SELECT count(*) FROM locais` — se > 0, retorna sem fazer nada
- Para cada location em `locations`, insere na tabela `locais` mapeando:
  - `id` -> `loc["id"]`
  - `name` -> `loc["name"]`
  - `cpe` -> `loc.get("cpe", "")`
  - `current_supplier` -> `loc.get("current_contract", {}).get("supplier")`
  - `current_plan_contains` -> `loc.get("current_contract", {}).get("current_plan_contains")`
  - `power_label` -> `loc.get("current_contract", {}).get("power_label")`
- Chamar `_seed_locais_from_config(engine, PROJECT_ROOT / "config" / "system.json")` no lifespan, DEPOIS de `metadata.create_all(engine)`
- Imports necessarios no topo de app.py: `import json`, `from sqlalchemy import select, insert, func`, `from src.db.schema import locais`
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "from src.db.schema import locais; print(locais.c.keys())" && python -c "from src.db.migrations.versions import __init__; print('migration module ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.py contains `locais = Table(`
    - src/db/schema.py contains `Column("cpe", String(64), nullable=False, unique=True)`
    - src/db/schema.py contains `UniqueConstraint("location_id", "year_month", name="uq_comparacao_loc_month")`
    - src/db/migrations/versions/002_add_locais.py contains `revision = '002'`
    - src/db/migrations/versions/002_add_locais.py contains `down_revision = '001'`
    - src/db/migrations/versions/002_add_locais.py contains `op.create_table('locais'`
    - src/db/migrations/versions/002_add_locais.py contains `uq_comparacao_loc_month`
    - src/web/app.py contains `def _seed_locais_from_config(`
    - src/web/app.py contains `_seed_locais_from_config(engine`
    - src/web/app.py contains `import json`
  </acceptance_criteria>
  <done>Tabela locais definida em schema, UniqueConstraint em comparacoes, migracao Alembic 002 pronta, seed de config.json para SQLite no lifespan da app</done>
</task>

<task type="auto">
  <name>Task 2: Refactoring eredes_to_monthly_csv — extrair parse_xlsx_to_dict()</name>
  <files>src/backend/eredes_to_monthly_csv.py</files>
  <read_first>
    - src/backend/eredes_to_monthly_csv.py
  </read_first>
  <action>
1. Adicionar `from typing import Any` ao bloco de imports (corrigir bug Pitfall 6 — `Any` usado em `pick_sheet` sem estar importado).

2. Extrair a logica de parsing de `convert_xlsx_to_monthly_csv()` para uma nova funcao `parse_xlsx_to_dict()`:

```python
def parse_xlsx_to_dict(
    input_path: Path, drop_partial_last_month: bool = False
) -> dict[str, dict[str, float]]:
    """Parse XLSX E-REDES e retorna dados mensais em memoria.

    Returns:
        Dict com year_month como chave (ex: '2025-01') e dict com
        keys 'total_kwh', 'vazio_kwh', 'fora_vazio_kwh' como valor.
    Raises:
        ValueError: Se consumo fora dos limites plausiveis.
    """
    wb = load_workbook(input_path, data_only=True, read_only=True)
    ws = pick_sheet(wb)
    data_start_row = detect_data_start_row(ws)

    header_row = next(ws.iter_rows(min_row=data_start_row - 1, max_row=data_start_row - 1, values_only=True))
    header_values = [str(cell).strip() if cell is not None else "" for cell in header_row]
    if len(header_values) >= 3 and header_values[0] == "Data" and header_values[1] == "Hora":
        date_idx, time_idx = 0, 1
    else:
        date_idx, time_idx = 1, 2

    monthly: dict[str, dict[str, float]] = defaultdict(
        lambda: {"total_kwh": 0.0, "vazio_kwh": 0.0, "fora_vazio_kwh": 0.0}
    )
    latest_date_by_month: dict[str, datetime.date] = {}

    for row in ws.iter_rows(min_row=data_start_row, values_only=True):
        extracted = extract_date_time_and_kwh(row, date_idx=date_idx, time_idx=time_idx)
        if extracted is None:
            continue
        row_date, row_time, interval_kwh = extracted
        local_dt = datetime.strptime(f"{row_date} {row_time}", "%Y/%m/%d %H:%M").replace(tzinfo=LISBON)
        year_month = local_dt.strftime("%Y-%m")
        current_date = local_dt.date()
        monthly[year_month]["total_kwh"] += interval_kwh
        if is_daily_cycle_vazio(local_dt):
            monthly[year_month]["vazio_kwh"] += interval_kwh
        else:
            monthly[year_month]["fora_vazio_kwh"] += interval_kwh
        if year_month not in latest_date_by_month or current_date > latest_date_by_month[year_month]:
            latest_date_by_month[year_month] = current_date

    if drop_partial_last_month and monthly:
        last_year_month = max(monthly)
        if not is_complete_month(last_year_month, latest_date_by_month):
            del monthly[last_year_month]

    for ym, totals in monthly.items():
        if not (MIN_MONTHLY_KWH <= totals["total_kwh"] <= MAX_MONTHLY_KWH):
            raise ValueError(
                f"Consumo fora dos limites plausiveis para {ym}: "
                f"{totals['total_kwh']:.1f} kWh (esperado {MIN_MONTHLY_KWH}-{MAX_MONTHLY_KWH} kWh/mes)"
            )

    return dict(monthly)
```

3. Refactorizar `convert_xlsx_to_monthly_csv()` para chamar `parse_xlsx_to_dict()` internamente:

```python
def convert_xlsx_to_monthly_csv(
    input_path: Path, output_path: Path, drop_partial_last_month: bool = False
) -> Path:
    monthly = parse_xlsx_to_dict(input_path, drop_partial_last_month=drop_partial_last_month)

    output_path.parent.mkdir(parents=True, exist_ok=True)
    with output_path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.writer(handle)
        writer.writerow(["year_month", "total_kwh", "vazio_kwh", "fora_vazio_kwh"])
        for year_month in sorted(monthly):
            totals = monthly[year_month]
            writer.writerow([
                year_month,
                f"{totals['total_kwh']:.3f}",
                f"{totals['vazio_kwh']:.3f}",
                f"{totals['fora_vazio_kwh']:.3f}",
            ])
    return output_path
```

A funcao publica existente mantem a mesma interface — backward compatible.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "from src.backend.eredes_to_monthly_csv import parse_xlsx_to_dict, convert_xlsx_to_monthly_csv; print('imports ok')" && pytest tests/test_xlsx_validation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/eredes_to_monthly_csv.py contains `from typing import Any`
    - src/backend/eredes_to_monthly_csv.py contains `def parse_xlsx_to_dict(`
    - src/backend/eredes_to_monthly_csv.py contains `return dict(monthly)`
    - convert_xlsx_to_monthly_csv still exists and calls parse_xlsx_to_dict internally
    - Existing tests in tests/test_xlsx_validation.py still pass (backward compat)
  </acceptance_criteria>
  <done>parse_xlsx_to_dict() exportada e reutilizavel, convert_xlsx_to_monthly_csv() refactorizada como wrapper, bug Any corrigido, testes existentes passam</done>
</task>

<task type="auto">
  <name>Task 3: Servicos ingestao_xlsx.py e locais_service.py</name>
  <files>src/web/services/ingestao_xlsx.py, src/web/services/locais_service.py</files>
  <read_first>
    - src/db/schema.py
    - src/db/engine.py
    - src/backend/eredes_to_monthly_csv.py
    - src/backend/cpe_routing.py
    - src/web/services/data_loader.py
  </read_first>
  <action>
1. Criar `src/web/services/ingestao_xlsx.py`:

```python
"""Servico de ingestao de XLSX E-REDES para SQLite."""
from pathlib import Path
from sqlalchemy import Engine
from sqlalchemy.dialects.sqlite import insert as sqlite_insert

from src.backend.cpe_routing import extract_cpe_from_filename
from src.backend.eredes_to_monthly_csv import parse_xlsx_to_dict
from src.db.schema import consumo_mensal
from src.web.services.locais_service import get_local_by_cpe


def ingerir_xlsx(tmp_path: Path, filename: str, engine: Engine) -> dict:
    """Ingere XLSX E-REDES: detecta CPE, resolve local, escreve em SQLite.

    Args:
        tmp_path: Path para o ficheiro XLSX temporario no disco.
        filename: Nome original do ficheiro (para extrair CPE).
        engine: SQLAlchemy engine.

    Returns:
        Dict com keys: erro (str|None), location_id, location_name, cpe,
        meses_inseridos (int), meses_total (int), periodo_inicio, periodo_fim.
    """
    # 1. Extrair CPE do nome de ficheiro
    cpe = extract_cpe_from_filename(filename)
    if not cpe:
        return {"erro": "CPE nao detectado no ficheiro. Associe o ficheiro ao local manualmente."}

    # 2. Resolver location via SQLite (tabela locais)
    local = get_local_by_cpe(cpe, engine)
    if not local:
        return {"erro": f"CPE {cpe} nao corresponde a nenhum local configurado."}

    # 3. Parse XLSX em memoria
    try:
        monthly_data = parse_xlsx_to_dict(tmp_path)
    except ValueError as e:
        return {"erro": str(e)}

    if not monthly_data:
        return {"erro": "Ficheiro XLSX nao contem dados de consumo validos."}

    # 4. Escrever em SQLite com idempotencia
    meses_inseridos = 0
    with engine.begin() as conn:
        for ym, totals in monthly_data.items():
            stmt = sqlite_insert(consumo_mensal).values(
                location_id=local["id"],
                year_month=ym,
                total_kwh=totals["total_kwh"],
                vazio_kwh=totals["vazio_kwh"],
                fora_vazio_kwh=totals["fora_vazio_kwh"],
            ).on_conflict_do_nothing(
                index_elements=["location_id", "year_month"]
            )
            result = conn.execute(stmt)
            meses_inseridos += result.rowcount

    return {
        "erro": None,
        "location_id": local["id"],
        "location_name": local["name"],
        "cpe": cpe,
        "meses_inseridos": meses_inseridos,
        "meses_total": len(monthly_data),
        "periodo_inicio": min(monthly_data.keys()),
        "periodo_fim": max(monthly_data.keys()),
    }
```

2. Criar `src/web/services/locais_service.py`:

```python
"""Servico CRUD de locais em SQLite."""
from datetime import datetime, timezone
from sqlalchemy import Engine, select, insert, update

from src.db.schema import locais


def get_all_locais(engine: Engine) -> list[dict]:
    """Retorna todos os locais ordenados por nome."""
    with engine.connect() as conn:
        rows = conn.execute(
            select(locais).order_by(locais.c.name)
        ).fetchall()
    return [dict(row._mapping) for row in rows]


def get_local_by_id(local_id: str, engine: Engine) -> dict | None:
    """Retorna um local pelo id ou None."""
    with engine.connect() as conn:
        row = conn.execute(
            select(locais).where(locais.c.id == local_id)
        ).fetchone()
    return dict(row._mapping) if row else None


def get_local_by_cpe(cpe: str, engine: Engine) -> dict | None:
    """Retorna um local pelo CPE ou None."""
    with engine.connect() as conn:
        row = conn.execute(
            select(locais).where(locais.c.cpe == cpe)
        ).fetchone()
    return dict(row._mapping) if row else None


def create_local(local_id: str, name: str, cpe: str, engine: Engine) -> dict:
    """Cria um novo local. Retorna dict do local criado.

    Args:
        local_id: Slug unico (ex: 'escritorio'). Gerado a partir do nome se nao fornecido.
        name: Nome livre (ex: 'Escritorio Porto').
        cpe: CPE E-REDES (ex: 'PT0002000084968079SX').
        engine: SQLAlchemy engine.

    Returns:
        Dict com dados do local criado.

    Raises:
        ValueError: Se CPE ja existe na tabela.
    """
    existing = get_local_by_cpe(cpe, engine)
    if existing:
        raise ValueError(f"CPE {cpe} ja esta associado ao local '{existing['name']}'.")

    with engine.begin() as conn:
        conn.execute(insert(locais).values(
            id=local_id,
            name=name,
            cpe=cpe,
            created_at=datetime.now(timezone.utc),
        ))
    return get_local_by_id(local_id, engine)


def update_fornecedor(local_id: str, supplier: str, plan_contains: str | None, engine: Engine) -> dict | None:
    """Actualiza o fornecedor actual de um local.

    Returns:
        Dict actualizado do local, ou None se local nao existe.
    """
    with engine.begin() as conn:
        result = conn.execute(
            update(locais)
            .where(locais.c.id == local_id)
            .values(
                current_supplier=supplier,
                current_plan_contains=plan_contains,
            )
        )
    if result.rowcount == 0:
        return None
    return get_local_by_id(local_id, engine)
```

3. Garantir que `src/web/services/` tem `__init__.py` (criar se nao existir — pode estar vazio).
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -c "from src.web.services.ingestao_xlsx import ingerir_xlsx; print('ingestao ok')" && python -c "from src.web.services.locais_service import get_all_locais, get_local_by_cpe, create_local, update_fornecedor; print('locais ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/web/services/ingestao_xlsx.py contains `def ingerir_xlsx(`
    - src/web/services/ingestao_xlsx.py contains `from src.backend.eredes_to_monthly_csv import parse_xlsx_to_dict`
    - src/web/services/ingestao_xlsx.py contains `.on_conflict_do_nothing(`
    - src/web/services/locais_service.py contains `def get_all_locais(`
    - src/web/services/locais_service.py contains `def get_local_by_cpe(`
    - src/web/services/locais_service.py contains `def create_local(`
    - src/web/services/locais_service.py contains `def update_fornecedor(`
    - Both files import successfully without errors
  </acceptance_criteria>
  <done>Servicos de ingestao XLSX e gestao de locais prontos para consumo pelos endpoints HTTP de Plan 02</done>
</task>

</tasks>

<verification>
- `pytest tests/test_web_upload.py tests/test_ingestao_xlsx.py tests/test_web_locais.py tests/test_comparacao.py -x -q` — test stubs passam (skipped)
- `python -c "from src.db.schema import locais, consumo_mensal, comparacoes, custos_reais; print('all tables ok')"` — todas as tabelas importam
- `python -c "from src.backend.eredes_to_monthly_csv import parse_xlsx_to_dict; print('refactor ok')"` — nova funcao acessivel
- `python -c "from src.web.services.ingestao_xlsx import ingerir_xlsx; from src.web.services.locais_service import get_all_locais; print('services ok')"` — servicos importam
- `pytest tests/test_xlsx_validation.py -x -q` — testes existentes nao quebrados pelo refactoring
</verification>

<success_criteria>
1. Test stubs existem para todos os requisitos Phase 7 (UPLD-01, UPLD-02, UPLD-05, CONF-01, CONF-02, COMP-01, COMP-02)
2. Tabela `locais` definida em schema.py e migracao Alembic 002 existe
3. UniqueConstraint em comparacoes para idempotencia de insercoes tiagofelicia
4. Seed automatico de config.json para SQLite funciona no lifespan
5. parse_xlsx_to_dict() retorna dict em memoria sem escrever para disco
6. convert_xlsx_to_monthly_csv() continua a funcionar (backward compat)
7. ingerir_xlsx() escreve em consumo_mensal com idempotencia
8. locais_service tem CRUD completo (list, get_by_cpe, create, update_fornecedor)
</success_criteria>

<output>
After completion, create `.planning/phases/07-upload-xlsx-ingest-o-de-dados/07-01-SUMMARY.md`
</output>
