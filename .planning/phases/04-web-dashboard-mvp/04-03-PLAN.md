---
phase: 04-web-dashboard-mvp
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/web/services/rankings.py
  - src/web/templates/partials/ranking_table.html
  - src/web/templates/partials/recomendacao_banner.html
  - src/web/templates/partials/dashboard_content.html
  - src/web/routes/dashboard.py
  - launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist
  - tests/test_web_rankings.py
autonomous: true
requirements:
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Tabela de ranking mostra top-5 fornecedores por custo anual estimado + fornecedor actual destacado"
    - "Fornecedor actual aparece na tabela mesmo que nao esteja no top-5"
    - "Banner de recomendacao aparece quando poupanca > 50 EUR/ano e mostra nome do fornecedor mais barato"
    - "Banner de recomendacao nao aparece quando poupanca < 50 EUR/ano"
    - "LaunchAgent plist arranca uvicorn no login sem erros"
  artifacts:
    - path: "src/web/services/rankings.py"
      provides: "Calculo de ranking anual a partir de history[].top_3 + current_supplier_result"
      exports: ["calculate_annual_ranking", "build_recommendation"]
    - path: "src/web/templates/partials/ranking_table.html"
      provides: "Tabela HTML com top-5 + fornecedor actual"
      contains: "highlight"
    - path: "src/web/templates/partials/recomendacao_banner.html"
      provides: "Banner colorido com poupanca estimada"
      contains: "banner-success"
    - path: "launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist"
      provides: "LaunchAgent para uvicorn auto-start"
      contains: "RunAtLoad"
  key_links:
    - from: "src/web/services/rankings.py"
      to: "data/{local}/processed/analise_tiagofelicia_atual.json"
      via: "Recebe analysis dict ja carregado pelo data_loader"
      pattern: "history.*top_3"
    - from: "src/web/routes/dashboard.py"
      to: "src/web/services/rankings.py"
      via: "import calculate_annual_ranking, build_recommendation"
      pattern: "from src\\.web\\.services\\.rankings import"
    - from: "launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist"
      to: "src/web/app.py"
      via: "ProgramArguments: python3 -m uvicorn src.web.app:app"
      pattern: "uvicorn"
---

<objective>
Implementar a tabela de ranking de fornecedores, o banner de recomendacao de mudanca, e o LaunchAgent para auto-start da dashboard.

Purpose: O ranking (DASH-05) e a recomendacao (DASH-06) sao o core da proposta de valor da dashboard — responder a pergunta "qual e o fornecedor mais barato e quanto poupo se mudar?". O LaunchAgent (D-04) garante que a dashboard esta sempre disponivel sem accao manual.

Output: Ranking funcional + banner de poupanca + dashboard disponivel em background apos login.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-dashboard-mvp/04-01-SUMMARY.md

<interfaces>
<!-- Analysis JSON history entry schema (from RESEARCH.md) -->
{
  "year_month": "2025-01",
  "top_3": [
    {"supplier": "Ibelectra", "plan": "Indexado Bi-Horario", "total_eur": 188.32},
    {"supplier": "Goldenergy", "plan": "Bi-Horario Gold", "total_eur": 192.45},
    {"supplier": "Luzboa", "plan": "Bi-Horario Verde", "total_eur": 195.10}
  ],
  "current_supplier_result": {"supplier": "Meo Energia", "plan": "Tarifa Variavel", "total_eur": 263.12},
  "saving_vs_current_eur": 74.8,
  "recommended_option": "bihorario",
  "needs_change": true
}

<!-- history_summary schema -->
{
  "months_analysed": 11,
  "latest_top_3": [...],
  "latest_current_supplier_result": {"supplier": "Meo Energia", "total_eur": 263.12},
  "latest_saving_vs_current_eur": 46.54,
  "latest_change_needed": true,
  "latest_recommendation": "bihorario"
}

<!-- From Plan 01: data_loader functions used -->
def load_analysis_json(json_path: Path) -> dict | None
def load_locations(config_path: Path) -> list[dict]

<!-- Existing launchd plist pattern (from launchd/com.ricmag.monitorizacao-eletricidade.plist) -->
ProgramArguments: /usr/local/opt/python@3.11/libexec/bin/python3
WorkingDirectory: /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade
StandardOutPath: .../state/launchd.{label}.stdout.log
StandardErrorPath: .../state/launchd.{label}.stderr.log
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Servico de ranking + templates de ranking e recomendacao</name>
  <files>
    src/web/services/rankings.py
    src/web/templates/partials/ranking_table.html
    src/web/templates/partials/recomendacao_banner.html
    src/web/templates/partials/dashboard_content.html
    src/web/routes/dashboard.py
    tests/test_web_rankings.py
  </files>
  <read_first>
    src/web/services/data_loader.py
    src/web/routes/dashboard.py
    src/web/templates/partials/dashboard_content.html
    tests/conftest.py
  </read_first>
  <behavior>
    - test_calculate_annual_ranking_basic: Com 3 meses de history, cada com 3 fornecedores em top_3 + current_supplier, retorna lista ordenada por custo_anual_estimado (menor primeiro); cada entry tem keys "supplier", "plan", "custo_anual_estimado", "is_current"
    - test_calculate_annual_ranking_top5: Com history que tem 8 fornecedores distintos, retorna apenas 5 + fornecedor actual (6 entries max se actual nao esta no top-5)
    - test_calculate_annual_ranking_current_in_top5: Se fornecedor actual esta no top-5, retorna exactamente 5 entries (nao duplica)
    - test_calculate_annual_ranking_empty: Com analysis=None ou history vazio, retorna lista vazia
    - test_build_recommendation_significant: Com poupanca > 50 EUR/ano, retorna {"show": True, "supplier": "Ibelectra", "saving_eur": 74.8, "message": "Podes poupar ~75 EUR/ano mudando para Ibelectra"}
    - test_build_recommendation_insignificant: Com poupanca < 50 EUR/ano, retorna {"show": False}
    - test_build_recommendation_no_data: Com analysis=None, retorna {"show": False}
  </behavior>
  <action>
    **1. Criar `src/web/services/rankings.py`:**

    ```python
    from collections import defaultdict

    SAVING_THRESHOLD_EUR = 50  # Per D-09: limiar para mostrar banner de recomendacao

    def calculate_annual_ranking(
        analysis: dict | None,
        current_supplier_name: str
    ) -> list[dict]:
        """Calcula ranking de fornecedores por custo anual estimado.

        Algoritmo (da RESEARCH.md):
        1. Iterar history[]
        2. Para cada mes, recolher todos os fornecedores de top_3 + current_supplier_result
        3. Somar total_eur por fornecedor ao longo dos meses
        4. Extrapolar para 12 meses: custo_anual = (soma_total / n_meses) * 12
        5. Ordenar por custo_anual (menor primeiro)
        6. Devolver top-5 + fornecedor actual (per D-08)

        Retorna lista de dicts:
        [{"supplier": str, "plan": str, "custo_anual_estimado": float, "is_current": bool}, ...]
        """
        if not analysis or "history" not in analysis:
            return []

        history = analysis["history"]
        if not history:
            return []

        n_months = len(history)

        # Acumular custo total e plano por fornecedor
        supplier_totals = defaultdict(lambda: {"total_eur": 0.0, "plan": "", "months_seen": 0})

        for entry in history:
            # Processar top_3
            for s in entry.get("top_3", []):
                name = s.get("supplier", "")
                supplier_totals[name]["total_eur"] += s.get("total_eur", 0)
                supplier_totals[name]["plan"] = s.get("plan", "")
                supplier_totals[name]["months_seen"] += 1

            # Processar current_supplier_result
            csr = entry.get("current_supplier_result", {})
            csr_name = csr.get("supplier", "")
            if csr_name:
                supplier_totals[csr_name]["total_eur"] += csr.get("total_eur", 0)
                supplier_totals[csr_name]["plan"] = csr.get("plan", "")
                supplier_totals[csr_name]["months_seen"] += 1

        # Calcular custo anual estimado (extrapolado para 12 meses)
        ranked = []
        for supplier, data in supplier_totals.items():
            if data["months_seen"] > 0:
                custo_anual = (data["total_eur"] / data["months_seen"]) * 12
            else:
                custo_anual = 0
            ranked.append({
                "supplier": supplier,
                "plan": data["plan"],
                "custo_anual_estimado": round(custo_anual, 2),
                "is_current": supplier == current_supplier_name
            })

        ranked.sort(key=lambda x: x["custo_anual_estimado"])

        # Top-5 + fornecedor actual (per D-08)
        top5 = ranked[:5]
        current_in_top5 = any(r["is_current"] for r in top5)
        if not current_in_top5:
            current_entry = next((r for r in ranked if r["is_current"]), None)
            if current_entry:
                top5.append(current_entry)

        return top5

    def build_recommendation(analysis: dict | None) -> dict:
        """Constroi dados para o banner de recomendacao.

        Per D-09: Banner "Podes poupar ~X EUR/ano mudando para [Fornecedor]"
        So aparece se poupanca > SAVING_THRESHOLD_EUR (50 EUR/ano).

        Retorna:
        - {"show": True, "supplier": str, "saving_eur": float, "message": str} se significativa
        - {"show": False} se nao significativa ou sem dados
        """
        if not analysis or "history_summary" not in analysis:
            return {"show": False}

        hs = analysis["history_summary"]
        saving = hs.get("latest_saving_vs_current_eur", 0)
        # Extrapolacao simples: poupanca mensal * 12
        saving_annual = saving * 12

        if saving_annual <= SAVING_THRESHOLD_EUR:
            return {"show": False}

        # Melhor fornecedor: primeiro do latest_top_3
        top3 = hs.get("latest_top_3", [])
        best_supplier = top3[0]["supplier"] if top3 else "desconhecido"

        return {
            "show": True,
            "supplier": best_supplier,
            "saving_eur": round(saving_annual, 0),
            "message": f"Podes poupar ~{int(round(saving_annual, 0))} EUR/ano mudando para {best_supplier}"
        }
    ```

    **2. Criar `src/web/templates/partials/ranking_table.html`:**
    ```html
    <div class="card">
      <h2>Ranking de Fornecedores (Custo Anual Estimado)</h2>
      {% if ranking %}
      <table>
        <thead>
          <tr>
            <th>#</th>
            <th>Fornecedor</th>
            <th>Plano</th>
            <th>Custo Anual Est. (EUR)</th>
          </tr>
        </thead>
        <tbody>
          {% for r in ranking %}
          <tr{% if r.is_current %} class="highlight"{% endif %}>
            <td>{{ loop.index }}</td>
            <td>{{ r.supplier }}{% if r.is_current %} (actual){% endif %}</td>
            <td>{{ r.plan }}</td>
            <td>{{ "%.2f" | format(r.custo_anual_estimado) }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
      {% else %}
      <p style="color: var(--muted);">Sem dados de ranking disponiveis.</p>
      {% endif %}
    </div>
    ```
    Per D-08: top-5 + fornecedor actual. Fornecedor actual com class="highlight" (fundo amarelo claro).

    **3. Criar `src/web/templates/partials/recomendacao_banner.html`:**
    ```html
    {% if recommendation.show %}
    <div class="banner banner-success">
      {{ recommendation.message }}
    </div>
    {% endif %}
    ```
    Per D-09: Banner simples e directo. Nao aparece se poupanca < 50 EUR/ano.

    **4. Actualizar `src/web/templates/partials/dashboard_content.html`:**
    Substituir placeholders de ranking e recomendacao por includes:
    ```html
    {% include "partials/recomendacao_banner.html" %}
    <div class="grid-2">
      {% include "partials/consumo_chart.html" %}
      <div id="custo-section">
        {% include "partials/custo_section.html" %}
      </div>
    </div>
    {% include "partials/custo_form.html" %}
    <div id="ranking-section">
      {% include "partials/ranking_table.html" %}
    </div>
    ```

    **5. Actualizar `src/web/routes/dashboard.py`:**
    Adicionar imports de `calculate_annual_ranking` e `build_recommendation` de `src.web.services.rankings`.
    No context passado ao template, adicionar:
    - `ranking`: resultado de `calculate_annual_ranking(analysis, location["current_contract"]["supplier"])`
    - `recommendation`: resultado de `build_recommendation(analysis)`

    **6. Criar `tests/test_web_rankings.py`:**
    Implementar todos os testes do behavior block. Usar fixtures do conftest para analysis JSON mock.

    Fixture helper para analysis mock (adicionar ao conftest.py se necessario):
    ```python
    def make_analysis(months=3, saving=74.8, current_total=263.12):
        history = []
        for i in range(months):
            ym = f"2025-{i+1:02d}"
            history.append({
                "year_month": ym,
                "top_3": [
                    {"supplier": "Ibelectra", "plan": "Indexado Bi-Horario", "total_eur": 188.32},
                    {"supplier": "Goldenergy", "plan": "Bi-Horario Gold", "total_eur": 192.45},
                    {"supplier": "Luzboa", "plan": "Bi-Horario Verde", "total_eur": 195.10},
                ],
                "current_supplier_result": {"supplier": "Meo Energia", "plan": "Tarifa Variavel", "total_eur": current_total},
                "saving_vs_current_eur": saving,
            })
        return {
            "current_supplier": "Meo Energia",
            "history_summary": {
                "months_analysed": months,
                "latest_top_3": history[-1]["top_3"],
                "latest_current_supplier_result": history[-1]["current_supplier_result"],
                "latest_saving_vs_current_eur": saving,
                "latest_change_needed": saving > 0,
            },
            "history": history,
        }
    ```
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -m pytest tests/test_web_rankings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/web/services/rankings.py contem `def calculate_annual_ranking(`
    - src/web/services/rankings.py contem `def build_recommendation(`
    - src/web/services/rankings.py contem `SAVING_THRESHOLD_EUR = 50`
    - src/web/services/rankings.py contem `top5 = ranked[:5]` (top-5 logic)
    - src/web/services/rankings.py contem `is_current` (flag para fornecedor actual)
    - src/web/templates/partials/ranking_table.html contem `highlight` (class para fornecedor actual)
    - src/web/templates/partials/ranking_table.html contem `(actual)` (label para fornecedor actual)
    - src/web/templates/partials/recomendacao_banner.html contem `banner-success`
    - src/web/templates/partials/recomendacao_banner.html contem `recommendation.show`
    - src/web/routes/dashboard.py contem `calculate_annual_ranking`
    - src/web/routes/dashboard.py contem `build_recommendation`
    - tests/test_web_rankings.py contem `test_calculate_annual_ranking_basic` e `test_build_recommendation_significant` e `test_build_recommendation_insignificant`
    - pytest tests/test_web_rankings.py -x -q sai com exit code 0
  </acceptance_criteria>
  <done>
    Tabela de ranking mostra top-5 fornecedores + actual (destacado). Banner de recomendacao aparece quando poupanca > 50 EUR/ano com mensagem "Podes poupar ~X EUR/ano mudando para [Fornecedor]". Testes de ranking e recomendacao passam.
  </done>
</task>

<task type="auto">
  <name>Task 2: LaunchAgent plist para uvicorn auto-start</name>
  <files>
    launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist
  </files>
  <read_first>
    launchd/com.ricmag.monitorizacao-eletricidade.plist
  </read_first>
  <action>
    **1. Ler o plist existente** (`launchd/com.ricmag.monitorizacao-eletricidade.plist`) para copiar o padrao exacto (Python path, WorkingDirectory, formato XML).

    **2. Criar `launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist`:**

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>Label</key>
        <string>com.ricmag.monitorizacao-eletricidade.dashboard</string>

        <key>ProgramArguments</key>
        <array>
            <string>/usr/local/opt/python@3.11/libexec/bin/python3</string>
            <string>-m</string>
            <string>uvicorn</string>
            <string>src.web.app:app</string>
            <string>--host</string>
            <string>127.0.0.1</string>
            <string>--port</string>
            <string>8000</string>
        </array>

        <key>WorkingDirectory</key>
        <string>/Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade</string>

        <key>RunAtLoad</key>
        <true/>

        <key>KeepAlive</key>
        <true/>

        <key>StandardOutPath</key>
        <string>/Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade/state/launchd.dashboard.stdout.log</string>

        <key>StandardErrorPath</key>
        <string>/Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade/state/launchd.dashboard.stderr.log</string>
    </dict>
    </plist>
    ```

    Per D-04: LaunchAgent automatico que arranca uvicorn no login.
    Per D-05: NAO abrir browser automaticamente.
    `KeepAlive: true` — se uvicorn crashar, launchd reinicia.
    SEM `--reload` (anti-pattern para producao — RESEARCH.md).
    Porta 8000 conforme success criteria do ROADMAP.

    **3. NaO instalar o plist automaticamente** — deixar para o utilizador:
    ```bash
    # Instrucoes para instalar (NAO executar automaticamente):
    # cp launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist ~/Library/LaunchAgents/
    # launchctl load ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.dashboard.plist
    ```
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -c "import plistlib; p=plistlib.load(open('launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist','rb')); assert p['Label']=='com.ricmag.monitorizacao-eletricidade.dashboard'; assert 'uvicorn' in p['ProgramArguments']; assert 'src.web.app:app' in p['ProgramArguments']; assert p['RunAtLoad']==True; assert p['KeepAlive']==True; assert '--reload' not in p['ProgramArguments']; assert '8000' in p['ProgramArguments']; assert '127.0.0.1' in p['ProgramArguments']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - launchd/com.ricmag.monitorizacao-eletricidade.dashboard.plist existe
    - Plist contem Label = "com.ricmag.monitorizacao-eletricidade.dashboard"
    - Plist contem ProgramArguments com "uvicorn" e "src.web.app:app" e "--port" e "8000" e "--host" e "127.0.0.1"
    - Plist contem RunAtLoad = true
    - Plist contem KeepAlive = true
    - Plist contem WorkingDirectory com path do projecto
    - Plist NAO contem "--reload"
    - Plist contem StandardOutPath e StandardErrorPath apontando para state/launchd.dashboard.*.log
    - Python path e /usr/local/opt/python@3.11/libexec/bin/python3 (mesmo dos plists existentes)
    - plistlib.load nao lanca erro (XML valido)
  </acceptance_criteria>
  <done>
    Plist de LaunchAgent criado, valido, e pronto para instalar. Usa o mesmo Python path e padrao dos plists existentes. Arranca uvicorn em 127.0.0.1:8000 no login com KeepAlive.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_web_rankings.py -x -q` — todos green
2. `pytest tests/ -x -q` — suite completa green (todos os planos)
3. `plistlib.load` do plist sem erros e contem campos correctos
4. GET /local/casa/dashboard retorna HTML com tabela de ranking e banner de recomendacao (se dados existem)
5. Plist nao contem --reload e tem KeepAlive true
</verification>

<success_criteria>
- Ranking mostra top-5 fornecedores + fornecedor actual destacado
- Banner de recomendacao aparece quando poupanca > 50 EUR/ano
- LaunchAgent plist criado e validado
- Todos os testes web passam na suite completa
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-dashboard-mvp/04-03-SUMMARY.md`
</output>
