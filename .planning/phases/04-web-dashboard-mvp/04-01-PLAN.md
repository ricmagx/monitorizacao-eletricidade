---
phase: 04-web-dashboard-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__init__.py
  - src/web/__init__.py
  - src/web/app.py
  - src/web/routes/__init__.py
  - src/web/routes/dashboard.py
  - src/web/services/__init__.py
  - src/web/services/data_loader.py
  - src/web/templates/base.html
  - src/web/templates/dashboard.html
  - src/web/templates/partials/dashboard_content.html
  - src/web/templates/partials/frescura_badge.html
  - src/web/static/vendor/htmx.min.js
  - src/web/static/vendor/chart.umd.min.js
  - src/web/static/style.css
  - tests/conftest.py
  - tests/test_web_dashboard.py
  - tests/test_web_data_loader.py
  - requirements.txt
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "uvicorn src.web.app:app arranca sem erros e GET / retorna 200 com selector de local"
    - "O selector de local lista os locais de config/system.json (casa, apartamento)"
    - "HTMX e Chart.js sao servidos de /static/vendor/ — nenhum pedido a CDN externo"
    - "GET /local/{id}/dashboard retorna fragmento HTML parcial (HTMX swap)"
    - "Indicador de frescura mostra data do ultimo relatorio e estado de aviso se > 40 dias"
  artifacts:
    - path: "src/web/app.py"
      provides: "FastAPI app com StaticFiles e Jinja2Templates"
      exports: ["app"]
    - path: "src/web/services/data_loader.py"
      provides: "Funcoes de leitura de CSV, JSON analise, monthly_status, custos_reais"
      exports: ["load_locations", "load_consumo_csv", "load_analysis_json", "load_monthly_status", "get_freshness_info"]
    - path: "src/web/routes/dashboard.py"
      provides: "GET / e GET /local/{id}/dashboard"
      exports: ["router"]
    - path: "src/web/static/vendor/htmx.min.js"
      provides: "HTMX 2.0.8 ficheiro estatico local"
    - path: "src/web/static/vendor/chart.umd.min.js"
      provides: "Chart.js 4.5.1 ficheiro estatico local"
  key_links:
    - from: "src/web/routes/dashboard.py"
      to: "src/web/services/data_loader.py"
      via: "import load_locations, load_consumo_csv, load_analysis_json, get_freshness_info"
      pattern: "from src\\.web\\.services\\.data_loader import"
    - from: "src/web/app.py"
      to: "src/web/routes/dashboard.py"
      via: "app.include_router(dashboard.router)"
      pattern: "include_router"
    - from: "src/web/services/data_loader.py"
      to: "config/system.json"
      via: "json.load para obter locations"
      pattern: "config.*system\\.json"
---

<objective>
Criar a fundacao da dashboard web: FastAPI app, ficheiros estaticos (HTMX + Chart.js), templates base com selector de local, servico de leitura de dados do pipeline, e infraestrutura de testes.

Purpose: Estabelecer a estrutura completa de src/web/ com um app funcional que serve a pagina principal, permite trocar de local via HTMX, e mostra o indicador de frescura. Os planos seguintes (charts, ranking, custos) constroem sobre esta base.

Output: App FastAPI funcional em http://localhost:8000 com selector de local e frescura badge.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config/system.json
@tests/conftest.py

<interfaces>
<!-- Config schema from config/system.json -->
locations[].id: string ("casa", "apartamento")
locations[].name: string ("Casa", "Apartamento")
locations[].current_contract.supplier: string
locations[].pipeline.processed_csv_path: string (relative to project root)
locations[].pipeline.analysis_json_path: string (relative to project root)
locations[].pipeline.status_path: string (relative to project root)
locations[].pipeline.report_dir: string (relative to project root)

<!-- CSV schema: data/{local}/processed/consumo_mensal_atual.csv -->
year_month,total_kwh,vazio_kwh,fora_vazio_kwh
2025-01,1429.626,832.157,597.469

<!-- monthly_status.json schema -->
{
  "status": "ok",
  "generated_at": "2026-03-29T22:40:59.508976",
  "report_path": "data/casa/reports/relatorio_eletricidade_2026-03-29.md",
  "latest_recommendation": "bihorario",
  "latest_saving_vs_current_eur": 46.54
}

<!-- analysis JSON top-level keys -->
{
  "generated_at": "2026-03-29",
  "current_supplier": "Meo Energia",
  "history_summary": {
    "months_analysed": 11,
    "latest_recommendation": "bihorario",
    "latest_top_3": [...],
    "latest_current_supplier_result": {...},
    "latest_saving_vs_current_eur": 46.54
  },
  "history": [...]
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/web/ com FastAPI app, data_loader service e testes</name>
  <files>
    src/__init__.py
    src/web/__init__.py
    src/web/app.py
    src/web/routes/__init__.py
    src/web/routes/dashboard.py
    src/web/services/__init__.py
    src/web/services/data_loader.py
    tests/conftest.py
    tests/test_web_dashboard.py
    tests/test_web_data_loader.py
    requirements.txt
  </files>
  <read_first>
    config/system.json
    tests/conftest.py
    requirements.txt
  </read_first>
  <behavior>
    - test_load_locations: load_locations(config_path) retorna lista com 2 dicts, cada um com keys "id", "name", "current_contract", "pipeline"
    - test_load_consumo_csv: load_consumo_csv(csv_path) retorna lista de dicts com keys "year_month", "total_kwh", "vazio_kwh", "fora_vazio_kwh" (valores float)
    - test_load_consumo_csv_missing: load_consumo_csv(path_inexistente) retorna lista vazia (sem crash)
    - test_load_analysis_json: load_analysis_json(json_path) retorna dict com keys "history_summary", "history", "current_supplier"
    - test_load_analysis_json_missing: load_analysis_json(path_inexistente) retorna None (sem crash)
    - test_get_freshness_info_recent: get_freshness_info(status_json) com generated_at de ontem retorna {"days_ago": 1, "is_stale": False, "generated_at": "..."}
    - test_get_freshness_info_stale: get_freshness_info(status_json) com generated_at de 50 dias atras retorna {"is_stale": True, ...}
    - test_get_freshness_info_missing: get_freshness_info(None) retorna {"days_ago": None, "is_stale": True, "generated_at": None}
    - test_homepage_ok: GET / retorna 200 e HTML contendo "<select" e "htmx.min.js"
    - test_local_dashboard_swap: GET /local/casa/dashboard retorna 200 e HTML contendo "id=\"consumo-chart\""
    - test_local_dashboard_invalid: GET /local/inexistente/dashboard retorna 404
  </behavior>
  <action>
    **1. Criar pacotes Python:**
    - `src/__init__.py` — ficheiro vazio (necessario para uvicorn resolver `src.web.app`)
    - `src/web/__init__.py` — ficheiro vazio
    - `src/web/routes/__init__.py` — ficheiro vazio
    - `src/web/services/__init__.py` — ficheiro vazio

    **2. Criar `src/web/services/data_loader.py`:**

    Funcoes (todas aceitam `project_root: Path` como primeiro argumento para resolver paths relativos):

    ```python
    def load_locations(config_path: Path) -> list[dict]:
        """Le config/system.json e retorna locations[]."""
        # json.load, retorna config["locations"]
        # Se ficheiro nao existe, retorna []

    def load_consumo_csv(csv_path: Path) -> list[dict]:
        """Le CSV de consumo mensal. Retorna lista de dicts com valores float."""
        # csv.DictReader; converter total_kwh/vazio_kwh/fora_vazio_kwh para float
        # Se ficheiro nao existe, retorna []

    def load_analysis_json(json_path: Path) -> dict | None:
        """Le JSON de analise tiagofelicia. Retorna dict ou None se nao existe."""

    def load_monthly_status(status_path: Path) -> dict | None:
        """Le state/{local}/monthly_status.json. Retorna dict ou None se nao existe."""

    def get_freshness_info(status: dict | None) -> dict:
        """Calcula frescura a partir do status.
        Retorna: {"days_ago": int|None, "is_stale": bool, "generated_at": str|None}
        Limiar de stale: > 40 dias.
        Se status is None ou sem generated_at: days_ago=None, is_stale=True.
        """
    ```

    **3. Criar `src/web/app.py`:**

    ```python
    from fastapi import FastAPI
    from fastapi.staticfiles import StaticFiles
    from fastapi.templating import Jinja2Templates
    from pathlib import Path

    BASE_DIR = Path(__file__).resolve().parent
    PROJECT_ROOT = BASE_DIR.parent.parent  # monitorizacao-eletricidade/

    app = FastAPI(title="Monitorizacao Eletricidade")
    app.mount("/static", StaticFiles(directory=BASE_DIR / "static"), name="static")
    templates = Jinja2Templates(directory=str(BASE_DIR / "templates"))

    # Disponibilizar project_root e config_path como app.state
    app.state.project_root = PROJECT_ROOT
    app.state.config_path = PROJECT_ROOT / "config" / "system.json"

    from src.web.routes.dashboard import router as dashboard_router
    app.include_router(dashboard_router)
    ```

    **4. Criar `src/web/routes/dashboard.py`:**

    ```python
    from fastapi import APIRouter, Request, Depends
    from fastapi.responses import HTMLResponse

    router = APIRouter()

    @router.get("/", response_class=HTMLResponse)
    async def homepage(request: Request):
        # load_locations de config_path
        # Usar primeiro local como default
        # load_consumo_csv, load_analysis_json, load_monthly_status para o local default
        # get_freshness_info
        # Render dashboard.html com context: locations, selected_location, consumo_data, analysis, freshness

    @router.get("/local/{local_id}/dashboard", response_class=HTMLResponse)
    async def local_dashboard(request: Request, local_id: str):
        # Validar local_id existe em locations (404 se nao)
        # Mesmo loading que homepage mas para o local_id especificado
        # Render partials/dashboard_content.html (fragmento HTMX)
    ```

    **5. Actualizar `tests/conftest.py`** — adicionar fixtures:
    - `sample_analysis_json(tmp_path)` — JSON mock com structure: generated_at, current_supplier, history_summary (com latest_top_3, latest_saving_vs_current_eur), history (com 2 meses mock)
    - `sample_status_json(tmp_path)` — monthly_status mock com status "ok" e generated_at recente
    - `sample_config_json(tmp_path)` — system.json mock com 2 locations (casa, apartamento), paths relativos ao tmp_path
    - `web_client` — FastAPI TestClient que usa a app com config mock via monkeypatch de app.state.config_path

    **6. Criar `tests/test_web_data_loader.py`** com testes do behavior block.

    **7. Criar `tests/test_web_dashboard.py`** com testes de routes (test_homepage_ok, test_local_dashboard_swap, test_local_dashboard_invalid).

    **8. Actualizar `requirements.txt`** — adicionar se ausentes: `fastapi`, `uvicorn[standard]`, `jinja2`, `python-multipart`, `httpx` (para TestClient).

    Per D-01: HTMX e Chart.js sao ficheiros estaticos locais, NAO CDN.
    Per D-02: entry point e `uvicorn src.web.app:app`.
    Per D-03: sem autenticacao.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -m pytest tests/test_web_data_loader.py tests/test_web_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/__init__.py existe (ficheiro vazio ou com docstring)
    - src/web/__init__.py existe
    - src/web/app.py contem `app = FastAPI`
    - src/web/app.py contem `app.mount("/static"`
    - src/web/app.py contem `Jinja2Templates`
    - src/web/app.py contem `include_router`
    - src/web/services/data_loader.py contem `def load_locations(`
    - src/web/services/data_loader.py contem `def load_consumo_csv(`
    - src/web/services/data_loader.py contem `def load_analysis_json(`
    - src/web/services/data_loader.py contem `def get_freshness_info(`
    - src/web/services/data_loader.py contem `40` (limiar de stale)
    - src/web/routes/dashboard.py contem `@router.get("/")`
    - src/web/routes/dashboard.py contem `@router.get("/local/{local_id}/dashboard")`
    - src/web/routes/dashboard.py contem `404` (para local inexistente)
    - tests/test_web_data_loader.py contem `test_load_locations` e `test_get_freshness_info_stale`
    - tests/test_web_dashboard.py contem `test_homepage_ok` e `test_local_dashboard_invalid`
    - requirements.txt contem `fastapi` e `uvicorn`
    - pytest tests/test_web_data_loader.py tests/test_web_dashboard.py -x -q sai com exit code 0
  </acceptance_criteria>
  <done>
    Data loader service testa e funciona para CSV, JSON e frescura. Routes GET / e GET /local/{id}/dashboard testados. App FastAPI importavel como src.web.app:app.
  </done>
</task>

<task type="auto">
  <name>Task 2: Descarregar ficheiros estaticos e criar templates HTML</name>
  <files>
    src/web/static/vendor/htmx.min.js
    src/web/static/vendor/chart.umd.min.js
    src/web/static/style.css
    src/web/templates/base.html
    src/web/templates/dashboard.html
    src/web/templates/partials/dashboard_content.html
    src/web/templates/partials/frescura_badge.html
  </files>
  <read_first>
    src/web/app.py
    src/web/routes/dashboard.py
  </read_first>
  <action>
    **1. Descarregar HTMX e Chart.js:**
    ```bash
    mkdir -p src/web/static/vendor
    curl -sL -o src/web/static/vendor/htmx.min.js https://unpkg.com/htmx.org@2.0.8/dist/htmx.min.js
    curl -sL -o src/web/static/vendor/chart.umd.min.js https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js
    ```
    Verificar que ambos os ficheiros tem mais de 10KB (htmx ~50KB, chart.js ~200KB).

    **2. Criar `src/web/static/style.css`:**
    CSS minimalista, sem framework. Conteudo:
    ```css
    :root {
      --bg: #f8f9fa;
      --card-bg: #ffffff;
      --text: #212529;
      --muted: #6c757d;
      --accent: #0d6efd;
      --success: #198754;
      --warning: #ffc107;
      --danger: #dc3545;
      --border: #dee2e6;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, -apple-system, sans-serif; background: var(--bg); color: var(--text); line-height: 1.5; padding: 1rem 2rem; max-width: 1200px; margin: 0 auto; }
    h1 { font-size: 1.5rem; margin-bottom: 1rem; }
    h2 { font-size: 1.2rem; margin-bottom: 0.75rem; color: var(--muted); }
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; border-bottom: 1px solid var(--border); padding-bottom: 1rem; }
    .selector { padding: 0.5rem; font-size: 1rem; border: 1px solid var(--border); border-radius: 4px; }
    .card { background: var(--card-bg); border: 1px solid var(--border); border-radius: 8px; padding: 1.25rem; margin-bottom: 1.25rem; }
    .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
    @media (max-width: 768px) { .grid-2 { grid-template-columns: 1fr; } }
    .badge { display: inline-block; padding: 0.25rem 0.75rem; border-radius: 12px; font-size: 0.85rem; font-weight: 500; }
    .badge-ok { background: #d1e7dd; color: var(--success); }
    .badge-stale { background: #fff3cd; color: #856404; }
    .banner { padding: 1rem 1.25rem; border-radius: 8px; margin-bottom: 1.25rem; font-weight: 500; }
    .banner-success { background: #d1e7dd; color: var(--success); border: 1px solid #badbcc; }
    .banner-muted { background: #e9ecef; color: var(--muted); border: 1px solid var(--border); }
    table { width: 100%; border-collapse: collapse; }
    th, td { text-align: left; padding: 0.5rem 0.75rem; border-bottom: 1px solid var(--border); }
    th { font-weight: 600; color: var(--muted); font-size: 0.85rem; text-transform: uppercase; }
    tr.highlight { background: #fff3cd; }
    .form-inline { display: flex; gap: 0.5rem; align-items: center; }
    .form-inline input { padding: 0.35rem 0.5rem; border: 1px solid var(--border); border-radius: 4px; width: 100px; }
    .form-inline button { padding: 0.35rem 0.75rem; background: var(--accent); color: white; border: none; border-radius: 4px; cursor: pointer; }
    .htmx-indicator { display: none; }
    .htmx-request .htmx-indicator { display: inline; }
    ```

    **3. Criar `src/web/templates/base.html`:**
    Template Jinja2 base com:
    - `<!DOCTYPE html>`, charset UTF-8, viewport meta
    - `<title>Monitorizacao Eletricidade</title>`
    - `<link rel="stylesheet" href="{{ url_for('static', path='style.css') }}">`
    - `<script src="{{ url_for('static', path='vendor/htmx.min.js') }}"></script>`
    - `<script src="{{ url_for('static', path='vendor/chart.umd.min.js') }}"></script>`
    - `{% block content %}{% endblock %}`
    - NENHUMA referencia a URLs externas (CDN). Per D-01.

    **4. Criar `src/web/templates/dashboard.html`:**
    Extende base.html. Conteudo:
    - Header com titulo "Monitorizacao Eletricidade" e frescura badge (include partial)
    - Selector de local: `<select class="selector" name="local_id" hx-get="/local/{{ selected_location.id }}/dashboard" hx-target="#dashboard-content" hx-swap="innerHTML" hx-trigger="change" hx-vals="js:{}" hx-include="this">` — mas na verdade o hx-get deve ser dinamico. Usar JS minimo:
      ```html
      <select class="selector" id="local-selector"
              hx-get="/local/placeholder/dashboard"
              hx-target="#dashboard-content"
              hx-swap="innerHTML"
              hx-trigger="change">
        {% for loc in locations %}
        <option value="{{ loc.id }}" {% if loc.id == selected_location.id %}selected{% endif %}>{{ loc.name }}</option>
        {% endfor %}
      </select>
      <script>
      document.getElementById('local-selector').addEventListener('htmx:configRequest', function(evt) {
        evt.detail.path = '/local/' + this.value + '/dashboard';
      });
      </script>
      ```
    - `<div id="dashboard-content">{% include "partials/dashboard_content.html" %}</div>`

    **5. Criar `src/web/templates/partials/dashboard_content.html`:**
    Fragmento retornado pelo HTMX swap. Conteudo placeholder:
    - `<div class="grid-2">`
    - Card com `<canvas id="consumo-chart"></canvas>` e texto "Consumo mensal (kWh)" — dados injectados pelo Plan 02
    - Card com `<canvas id="custo-chart"></canvas>` e texto "Custo mensal (EUR)" — dados injectados pelo Plan 02
    - `</div>`
    - `<div class="card" id="ranking-section">` — placeholder "Ranking de fornecedores" — preenchido pelo Plan 03
    - `<div id="recomendacao-section">` — placeholder "Recomendacao" — preenchido pelo Plan 03

    **6. Criar `src/web/templates/partials/frescura_badge.html`:**
    - Se `freshness.generated_at` existe: mostrar "Ultimo relatorio: {{ freshness.generated_at }}"
    - Classe CSS: `badge-ok` se `not freshness.is_stale`, `badge-stale` se `freshness.is_stale`
    - Se `freshness.days_ago` existe: " (ha {{ freshness.days_ago }} dias)"
    - Se `freshness.is_stale`: adicionar texto " — Dados desactualizados"
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && test -s src/web/static/vendor/htmx.min.js && test -s src/web/static/vendor/chart.umd.min.js && python3 -c "from fastapi.testclient import TestClient; from src.web.app import app; c=TestClient(app); r=c.get('/'); assert r.status_code==200; assert 'htmx.min.js' in r.text; assert 'chart.umd.min.js' in r.text; assert 'cdn' not in r.text.lower() and 'unpkg' not in r.text.lower() and 'jsdelivr' not in r.text.lower(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/web/static/vendor/htmx.min.js existe e tem mais de 10000 bytes
    - src/web/static/vendor/chart.umd.min.js existe e tem mais de 10000 bytes
    - src/web/static/style.css existe e contem `--bg:`
    - src/web/templates/base.html contem `vendor/htmx.min.js` (path local, nao CDN)
    - src/web/templates/base.html contem `vendor/chart.umd.min.js` (path local, nao CDN)
    - src/web/templates/base.html NAO contem `unpkg.com` nem `jsdelivr.net` nem `cdnjs`
    - src/web/templates/dashboard.html contem `hx-get` e `hx-target="#dashboard-content"`
    - src/web/templates/dashboard.html contem `local-selector`
    - src/web/templates/partials/dashboard_content.html contem `consumo-chart`
    - src/web/templates/partials/frescura_badge.html contem `badge-ok` e `badge-stale`
    - GET / retorna 200 e HTML com referencia a htmx.min.js local
    - Nenhum URL externo (unpkg, jsdelivr, cdnjs) presente no HTML de GET /
  </acceptance_criteria>
  <done>
    Ficheiros estaticos HTMX 2.0.8 e Chart.js 4.5.1 servidos localmente. Templates HTML renderizam com selector de local funcional via HTMX. Pagina principal abre em localhost:8000 sem erros.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_web_data_loader.py tests/test_web_dashboard.py -x -q` — todos os testes passam
2. `uvicorn src.web.app:app --host 127.0.0.1 --port 8000` arranca sem erros (testar com curl localhost:8000)
3. GET / retorna HTML com `<select` e `htmx.min.js` referenciado de /static/vendor/
4. Nenhum pedido externo no HTML (grep por https:// no output de GET /)
5. GET /local/casa/dashboard retorna fragmento HTML parcial (sem `<!DOCTYPE`)
6. GET /local/inexistente/dashboard retorna 404
</verification>

<success_criteria>
- App FastAPI funcional com selector de local e frescura badge
- Data loader service testado para CSV, JSON, status e frescura
- HTMX e Chart.js servidos como ficheiros estaticos locais
- Templates renderizam correctamente para ambos os locais
- Infraestrutura de testes web operacional (pytest + TestClient)
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-dashboard-mvp/04-01-SUMMARY.md`
</output>
