---
phase: 10-cache-tiagofelicia-pt-integra-o-compara-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/routes/upload.py
  - src/web/services/data_loader.py
  - tests/test_web_data_loader.py
autonomous: true
requirements:
  - COMP-03

must_haves:
  truths:
    - "cached_at actualiza sempre que a consulta tiagofelicia.pt tem sucesso, mesmo que os dados sejam identicos"
    - "get_freshness_from_sqlite retorna source='fresh' quando cached_at < 48h"
    - "get_freshness_from_sqlite retorna source='cache' quando cached_at >= 48h"
    - "get_freshness_from_sqlite retorna source='none' quando nao ha comparacoes"
  artifacts:
    - path: "src/web/routes/upload.py"
      provides: "Upsert com on_conflict_do_update em vez de on_conflict_do_nothing"
      contains: "on_conflict_do_update"
    - path: "src/web/services/data_loader.py"
      provides: "Campo source no dict freshness"
      contains: "FRESH_THRESHOLD_HOURS"
    - path: "tests/test_web_data_loader.py"
      provides: "Testes para source fresh/cache/none"
      contains: "test_freshness_source"
  key_links:
    - from: "src/web/routes/upload.py"
      to: "comparacoes table"
      via: "on_conflict_do_update with cached_at refresh"
      pattern: "on_conflict_do_update"
    - from: "src/web/services/data_loader.py"
      to: "comparacoes table"
      via: "MAX(cached_at) comparison with FRESH_THRESHOLD_HOURS"
      pattern: "FRESH_THRESHOLD_HOURS"
---

<objective>
Actualizar o backend para que (1) o upsert de comparacoes actualize `cached_at` em cada consulta bem-sucedida a tiagofelicia.pt, e (2) o dict `freshness` retornado por `get_freshness_from_sqlite` inclua um campo `source` que distingue "fresh" de "cache" de "none".

Purpose: Sem esta alteracao, `on_conflict_do_nothing` impede que `cached_at` se actualize apos o primeiro insert — o badge de frescura nunca mostraria dados como "frescos". O campo `source` e a base para o badge ternario da Phase 10 Plan 02.
Output: upload.py com upsert actualizado, data_loader.py com campo source, testes verdes.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cache-tiagofelicia-pt-integra-o-compara-o/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/db/schema.py (tabela comparacoes):
```python
comparacoes = Table(
    "comparacoes", metadata,
    Column("id", Integer, primary_key=True),
    Column("location_id", Text, nullable=False),
    Column("year_month", Text, nullable=False),
    Column("top_3_json", Text),
    Column("current_supplier_result_json", Text),
    Column("generated_at", Text),
    Column("cached_at", DateTime),
    UniqueConstraint("location_id", "year_month", name="uq_comparacao_loc_month"),
)
```

From src/web/services/data_loader.py (freshness dict actual):
```python
def get_freshness_from_sqlite(location_id: str, engine: Engine) -> dict:
    # Retorna: {"days_ago": int|None, "is_stale": bool, "generated_at": str|None}
```

From src/web/routes/upload.py (upsert actual):
```python
stmt = sqlite_insert(comparacoes).values(...).on_conflict_do_nothing(
    index_elements=["location_id", "year_month"]
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upsert on_conflict_do_update + freshness source field</name>
  <files>src/web/routes/upload.py, src/web/services/data_loader.py, tests/test_web_data_loader.py</files>
  <read_first>
    - src/web/routes/upload.py
    - src/web/services/data_loader.py
    - tests/test_web_data_loader.py
    - tests/conftest.py
    - src/db/schema.py
  </read_first>
  <behavior>
    - Test freshness_source_fresh: Inserir comparacao com cached_at de 1h atras -> get_freshness_from_sqlite retorna source="fresh"
    - Test freshness_source_cache: Inserir comparacao com cached_at de 72h atras -> get_freshness_from_sqlite retorna source="cache"
    - Test freshness_source_none: Sem comparacoes -> get_freshness_from_sqlite retorna source="none"
    - Test freshness_source_stale_but_cache: Inserir comparacao com cached_at de 50 dias atras -> source="cache" e is_stale=True
    - Test existing freshness fields preserved: days_ago, is_stale, generated_at continuam a funcionar
  </behavior>
  <action>
    **1. Escrever testes PRIMEIRO em tests/test_web_data_loader.py:**

    Adicionar ao final do ficheiro existente (nao apagar os testes existentes):

    ```python
    def test_freshness_source_fresh(db_engine_test):
        """cached_at recente (<48h) -> source='fresh'."""
        import json as _json
        from datetime import datetime, timedelta, timezone
        from sqlalchemy import insert
        from src.db.schema import comparacoes
        from src.web.services.data_loader import get_freshness_from_sqlite

        recent_ts = datetime.now(timezone.utc) - timedelta(hours=1)
        with db_engine_test.begin() as conn:
            conn.execute(insert(comparacoes).values(
                location_id="teste-fresh",
                year_month="2025-03",
                top_3_json=_json.dumps([]),
                current_supplier_result_json=_json.dumps({}),
                generated_at="2026-03-01T10:00:00",
                cached_at=recent_ts,
            ))

        result = get_freshness_from_sqlite("teste-fresh", db_engine_test)
        assert result["source"] == "fresh"
        assert result["is_stale"] is False

    def test_freshness_source_cache(db_engine_test):
        """cached_at antigo (>48h) -> source='cache'."""
        import json as _json
        from datetime import datetime, timedelta, timezone
        from sqlalchemy import insert
        from src.db.schema import comparacoes
        from src.web.services.data_loader import get_freshness_from_sqlite

        old_ts = datetime.now(timezone.utc) - timedelta(hours=72)
        with db_engine_test.begin() as conn:
            conn.execute(insert(comparacoes).values(
                location_id="teste-cache",
                year_month="2025-03",
                top_3_json=_json.dumps([]),
                current_supplier_result_json=_json.dumps({}),
                generated_at="2026-03-01T10:00:00",
                cached_at=old_ts,
            ))

        result = get_freshness_from_sqlite("teste-cache", db_engine_test)
        assert result["source"] == "cache"
        assert result["is_stale"] is False

    def test_freshness_source_none(db_engine_test):
        """Sem comparacoes -> source='none'."""
        from src.web.services.data_loader import get_freshness_from_sqlite
        result = get_freshness_from_sqlite("local-inexistente", db_engine_test)
        assert result["source"] == "none"

    def test_freshness_source_stale_is_cache(db_engine_test):
        """cached_at de 50 dias -> source='cache' e is_stale=True."""
        import json as _json
        from datetime import datetime, timedelta, timezone
        from sqlalchemy import insert
        from src.db.schema import comparacoes
        from src.web.services.data_loader import get_freshness_from_sqlite

        stale_ts = datetime.now(timezone.utc) - timedelta(days=50)
        with db_engine_test.begin() as conn:
            conn.execute(insert(comparacoes).values(
                location_id="teste-stale",
                year_month="2025-03",
                top_3_json=_json.dumps([]),
                current_supplier_result_json=_json.dumps({}),
                generated_at="2026-03-01T10:00:00",
                cached_at=stale_ts,
            ))

        result = get_freshness_from_sqlite("teste-stale", db_engine_test)
        assert result["source"] == "cache"
        assert result["is_stale"] is True
    ```

    Correr testes — devem FALHAR (RED) porque `source` nao existe.

    **2. Implementar get_freshness_from_sqlite com campo source em data_loader.py:**

    Adicionar constante `FRESH_THRESHOLD_HOURS = 48` ao topo da funcao (ou ao nivel do modulo, junto de onde ja existem constantes).

    Na funcao `get_freshness_from_sqlite`, alterar os 3 return statements:

    a) Return de fallback sem dados (max_cached_at is None ou exception):
    ```python
    return {"days_ago": None, "is_stale": True, "generated_at": None, "source": "none"}
    ```

    b) Return principal (com dados):
    ```python
    hours_ago = (now - cached_at).total_seconds() / 3600
    source = "fresh" if hours_ago <= FRESH_THRESHOLD_HOURS else "cache"
    return {
        "days_ago": days_ago,
        "is_stale": days_ago > STALE_THRESHOLD_DAYS,
        "generated_at": cached_at.isoformat(),
        "source": source,
    }
    ```

    c) Return de exception (ValueError/TypeError/AttributeError):
    ```python
    return {"days_ago": None, "is_stale": True, "generated_at": None, "source": "none"}
    ```

    Correr testes — devem PASSAR (GREEN).

    **3. Alterar _consultar_tiagofelicia_bg em upload.py:**

    Mudar `on_conflict_do_nothing` para `on_conflict_do_update`. Substituir:
    ```python
    ).on_conflict_do_nothing(
        index_elements=["location_id", "year_month"]
    )
    ```
    Por:
    ```python
    ).on_conflict_do_update(
        index_elements=["location_id", "year_month"],
        set_={
            "top_3_json": sqlite_insert(comparacoes).excluded.top_3_json,
            "current_supplier_result_json": sqlite_insert(comparacoes).excluded.current_supplier_result_json,
            "generated_at": sqlite_insert(comparacoes).excluded.generated_at,
            "cached_at": datetime.now(timezone.utc),
        }
    )
    ```

    NOTA: `sqlite_insert(comparacoes).excluded` acede aos valores do INSERT que conflituou — sintaxe standard do SQLAlchemy SQLite dialect.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/test_web_data_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_web_data_loader.py contem `def test_freshness_source_fresh`
    - tests/test_web_data_loader.py contem `def test_freshness_source_cache`
    - tests/test_web_data_loader.py contem `def test_freshness_source_none`
    - src/web/services/data_loader.py contem `FRESH_THRESHOLD_HOURS = 48`
    - src/web/services/data_loader.py contem `"source": source`
    - src/web/services/data_loader.py contem `"source": "none"`
    - src/web/routes/upload.py contem `on_conflict_do_update`
    - src/web/routes/upload.py NAO contem `on_conflict_do_nothing`
    - `python -m pytest tests/test_web_data_loader.py -x -q` retorna exit code 0
    - `python -m pytest tests/ -x -q` retorna exit code 0 (suite completa verde)
  </acceptance_criteria>
  <done>
    get_freshness_from_sqlite retorna campo `source` com valores "fresh" (cached_at < 48h), "cache" (cached_at >= 48h), ou "none" (sem dados). Upsert de comparacoes actualiza cached_at em cada consulta bem-sucedida. Todos os testes existentes + 4 novos testes passam.
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_web_data_loader.py tests/test_web_dashboard.py -x -q
</verification>

<success_criteria>
- get_freshness_from_sqlite retorna dict com campo "source" (valores: "fresh", "cache", "none")
- FRESH_THRESHOLD_HOURS = 48 definido em data_loader.py
- upload.py usa on_conflict_do_update em vez de on_conflict_do_nothing
- Suite completa de testes passa sem regressoes
</success_criteria>

<output>
After completion, create `.planning/phases/10-cache-tiagofelicia-pt-integra-o-compara-o/10-01-SUMMARY.md`
</output>
