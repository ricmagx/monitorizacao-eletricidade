---
phase: 10-cache-tiagofelicia-pt-integra-o-compara-o
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/web/templates/partials/frescura_badge.html
  - src/web/templates/partials/dashboard_content.html
  - src/web/templates/dashboard.html
  - src/web/static/style.css
  - tests/test_web_dashboard.py
autonomous: true
requirements:
  - COMP-04

must_haves:
  truths:
    - "Badge mostra 'Tarifarios actualizados em [data]' quando source=fresh"
    - "Badge mostra 'Tarifarios do cache — tiagofelicia.pt indisponivel (ultima consulta: [data])' quando source=cache"
    - "Badge mostra 'Sem dados de comparacao' quando source=none"
    - "Badge actualiza ao mudar de local no selector HTMX (esta dentro do #dashboard-content)"
  artifacts:
    - path: "src/web/templates/partials/frescura_badge.html"
      provides: "Badge ternario com 3 estados visuais"
      contains: "freshness.source"
    - path: "src/web/templates/partials/dashboard_content.html"
      provides: "Badge incluido dentro do swap zone HTMX"
      contains: "frescura_badge.html"
    - path: "src/web/templates/dashboard.html"
      provides: "Badge removido do header (fora do HTMX swap zone)"
    - path: "src/web/static/style.css"
      provides: "Classe badge-warn para estado cache"
      contains: "badge-warn"
    - path: "tests/test_web_dashboard.py"
      provides: "Testes para os 3 estados do badge"
      contains: "badge_fresh"
  key_links:
    - from: "src/web/templates/partials/frescura_badge.html"
      to: "freshness dict com campo source"
      via: "Jinja2 freshness.source"
      pattern: "freshness.source"
    - from: "src/web/templates/partials/dashboard_content.html"
      to: "frescura_badge.html"
      via: "Jinja2 include"
      pattern: "include.*frescura_badge"
---

<objective>
Implementar o badge ternario de frescura de comparacao (frescos/cache/sem dados) no template HTML, mover o badge para dentro do swap zone HTMX para que actualize ao trocar de local, e adicionar testes de integracao.

Purpose: O utilizador precisa de saber se os dados de ranking sao frescos ou do cache (COMP-04). O badge actual so distingue "ok" de "stale" e nao actualiza ao mudar de local (Pitfall 3 do Research).
Output: Badge ternario funcional, CSS para estado warn, testes de integracao verdes.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cache-tiagofelicia-pt-integra-o-compara-o/10-RESEARCH.md
@.planning/phases/10-cache-tiagofelicia-pt-integra-o-compara-o/10-01-SUMMARY.md
@.planning/phases/06-ui-design-ui-phase/06-UI-SPEC.md

<interfaces>
<!-- From Plan 01 output — freshness dict agora inclui source -->

From src/web/services/data_loader.py (apos Plan 01):
```python
FRESH_THRESHOLD_HOURS = 48

def get_freshness_from_sqlite(location_id: str, engine: Engine) -> dict:
    """Retorna:
    {
        "days_ago": int | None,
        "is_stale": bool,
        "generated_at": str | None,
        "source": "fresh" | "cache" | "none",
    }
    """
```

From 06-UI-SPEC.md (copywriting contract):
```
| Comparacao — dados frescos | "Tarifarios actualizados em [data]" |
| Comparacao — dados de cache | "Tarifarios do cache — tiagofelicia.pt indisponivel (ultima consulta: [data])" |
```

From 06-UI-SPEC.md (badge states):
```
| Fresh | badge-ok | fundo verde claro (#d1e7dd) |
| Stale | badge-stale | fundo amarelo (#fff3cd) |
```

From style.css (classes existentes):
```css
.badge-ok { background: #d1e7dd; color: var(--success); }
.badge-stale { background: #fff3cd; color: #856404; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge ternario + relocacao para dashboard_content + CSS badge-warn</name>
  <files>src/web/templates/partials/frescura_badge.html, src/web/templates/partials/dashboard_content.html, src/web/templates/dashboard.html, src/web/static/style.css, tests/test_web_dashboard.py</files>
  <read_first>
    - src/web/templates/partials/frescura_badge.html
    - src/web/templates/partials/dashboard_content.html
    - src/web/templates/dashboard.html
    - src/web/static/style.css
    - src/web/routes/dashboard.py
    - tests/test_web_dashboard.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - Test badge_fresh: Dashboard com comparacao cached_at recente (<48h) mostra texto "Tarifarios actualizados em" e classe "badge-ok" no HTML
    - Test badge_cache: Dashboard com comparacao cached_at antigo (>48h mas <40 dias) mostra texto "Tarifarios do cache" e classe "badge-warn" no HTML
    - Test badge_no_data: Dashboard sem comparacoes mostra texto "Sem dados de comparacao" e classe "badge-stale"
    - Test badge_in_htmx_fragment: GET /local/{id}/dashboard retorna fragmento HTML que contem o badge (frescura_badge dentro de dashboard_content.html)
  </behavior>
  <action>
    **1. Escrever testes PRIMEIRO em tests/test_web_dashboard.py:**

    Acrescentar ao final do ficheiro existente. Usar a fixture `web_client_sqlite` que ja tem dados de comparacao, ou criar inserts directos com `db_engine_test`. Verificar como a fixture cria os dados para escolher a abordagem.

    ```python
    def test_badge_fresh(web_client_sqlite):
        """Badge mostra 'actualizados' quando cached_at < 48h."""
        # web_client_sqlite tem comparacoes com cached_at recente
        resp = web_client_sqlite.get("/")
        assert resp.status_code == 200
        assert "badge-ok" in resp.text
        # Texto conforme UI-SPEC copywriting contract
        assert "actualizados em" in resp.text.lower() or "Tarifarios actualizados" in resp.text

    def test_badge_no_data_shows_sem_dados(web_client):
        """Badge mostra 'Sem dados de comparacao' quando sem comparacoes."""
        # web_client base nao tem comparacoes SQLite — fixture usa CSV
        resp = web_client.get("/")
        assert resp.status_code == 200
        # O badge deve conter texto de sem dados OU de dados CSV
        # (depende de como _load_location_data resolve freshness)

    def test_badge_in_htmx_fragment(web_client_sqlite):
        """GET /local/{id}/dashboard retorna fragmento com badge."""
        resp = web_client_sqlite.get("/local/teste-sqlite/dashboard")
        assert resp.status_code == 200
        assert "badge" in resp.text
        # Badge deve estar no fragmento HTMX (dashboard_content.html)
        assert "frescura" in resp.text.lower() or "actualizados" in resp.text.lower() or "Sem dados" in resp.text
    ```

    NOTA: Ajustar os asserts apos ler a fixture `web_client_sqlite` para entender os dados de seed exactos. Se `cached_at` no seed for recente, o badge sera "fresh". Se antigo, sera "cache".

    Correr testes — devem FALHAR (RED) porque o template actual nao tem `freshness.source` nem esta em `dashboard_content.html`.

    **2. Adicionar classe CSS `.badge-warn` em style.css:**

    Adicionar apos a linha `.badge-stale { background: #fff3cd; color: #856404; }`:
    ```css
    .badge-warn { background: #fff3cd; color: #856404; }
    ```

    NOTA: badge-warn usa as mesmas cores que badge-stale (amarelo). A distinccao e semantica (warn = cache disponivel, stale = dados muito antigos). Se no futuro quiser diferenciar visualmente, mudar aqui.

    **3. Reescrever frescura_badge.html com 3 estados:**

    Substituir o conteudo completo de `src/web/templates/partials/frescura_badge.html` por:

    ```html
    {% if freshness.source == "fresh" %}
    <span class="badge badge-ok">
      Tarifarios actualizados em {{ freshness.generated_at[:10] }}
      {% if freshness.days_ago is not none and freshness.days_ago > 0 %} (ha {{ freshness.days_ago }} dias){% endif %}
    </span>
    {% elif freshness.source == "cache" %}
    <span class="badge badge-warn">
      Tarifarios do cache — ultima consulta: {{ freshness.generated_at[:10] }}
      {% if freshness.days_ago is not none %} (ha {{ freshness.days_ago }} dias){% endif %}
      {% if freshness.is_stale %} — Dados desactualizados{% endif %}
    </span>
    {% else %}
    <span class="badge badge-stale">Sem dados de comparacao</span>
    {% endif %}
    ```

    Copywriting conforme 06-UI-SPEC.md Copywriting Contract.

    **4. Mover badge para dashboard_content.html:**

    Em `src/web/templates/partials/dashboard_content.html`, adicionar `{% include "partials/frescura_badge.html" %}` como PRIMEIRA linha (antes de `{% include "partials/recomendacao_banner.html" %}`). Resultado:

    ```html
    {% include "partials/frescura_badge.html" %}
    {% include "partials/recomendacao_banner.html" %}
    <div class="grid-2">
      {% include "partials/consumo_chart.html" %}
      <div id="custo-section">
        {% include "partials/custo_section.html" %}
      </div>
    </div>
    {% include "partials/upload_xlsx.html" %}
    {% include "partials/upload_pdf.html" %}
    <div id="ranking-section">
      {% include "partials/ranking_table.html" %}
    </div>
    ```

    **5. Remover badge do header em dashboard.html:**

    Em `src/web/templates/dashboard.html`, remover a linha `{% include "partials/frescura_badge.html" %}` do bloco header. O div `.form-inline` que continha o badge pode ser removido se ficar vazio. Resultado do header:

    ```html
    <div class="header">
      <h1>Monitorizacao Eletricidade</h1>
    </div>
    ```

    **6. Verificar compatibilidade com get_freshness_info (fallback CSV):**

    A funcao `get_freshness_info` (usada em fallback CSV no dashboard.py, linha 58) NAO retorna campo `source`. Para que o badge nao quebre em locais com pipeline CSV, adicionar `"source": "cache"` como default nos returns de `get_freshness_info` em `data_loader.py`:

    - Return de status recente: adicionar `"source": "fresh" if days_ago <= 2 else "cache"`
    - Return de status None/missing: adicionar `"source": "none"`
    - Return de exception: adicionar `"source": "none"`

    Isto garante que o template frescura_badge.html funciona independentemente de a fonte ser SQLite ou CSV/JSON.

    Correr testes — devem PASSAR (GREEN).
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python -m pytest tests/test_web_dashboard.py tests/test_web_data_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/web/templates/partials/frescura_badge.html contem `freshness.source == "fresh"`
    - src/web/templates/partials/frescura_badge.html contem `freshness.source == "cache"`
    - src/web/templates/partials/frescura_badge.html contem `Sem dados de comparacao`
    - src/web/templates/partials/frescura_badge.html contem `badge-warn`
    - src/web/templates/partials/frescura_badge.html contem `Tarifarios actualizados`
    - src/web/templates/partials/frescura_badge.html contem `Tarifarios do cache`
    - src/web/templates/partials/dashboard_content.html contem `frescura_badge.html`
    - src/web/templates/dashboard.html NAO contem `frescura_badge.html`
    - src/web/static/style.css contem `badge-warn`
    - src/web/services/data_loader.py funcao get_freshness_info retorna campo "source"
    - tests/test_web_dashboard.py contem `test_badge_fresh` ou `test_badge`
    - `python -m pytest tests/ -x -q` retorna exit code 0
  </acceptance_criteria>
  <done>
    Badge de frescura mostra 3 estados visuais distintos (fresh verde, cache amarelo, sem dados amarelo). Badge actualiza ao mudar de local via HTMX selector. CSS badge-warn adicionado. get_freshness_info tambem retorna campo source para compatibilidade com locais CSV. Todos os testes passam.
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -q
</verification>

<success_criteria>
- Badge de frescura mostra "Tarifarios actualizados em [data]" quando source=fresh (badge-ok verde)
- Badge de frescura mostra "Tarifarios do cache" quando source=cache (badge-warn amarelo)
- Badge de frescura mostra "Sem dados de comparacao" quando source=none (badge-stale amarelo)
- Badge actualiza ao mudar de local (esta em dashboard_content.html, dentro do HTMX swap zone)
- Badge removido do header de dashboard.html
- Suite completa de testes passa sem regressoes
</success_criteria>

<output>
After completion, create `.planning/phases/10-cache-tiagofelicia-pt-integra-o-compara-o/10-02-SUMMARY.md`
</output>
