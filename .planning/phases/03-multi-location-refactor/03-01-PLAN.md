---
phase: 03-multi-location-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/system.json
  - tests/conftest.py
  - tests/test_multi_location_config.py
  - tests/test_cpe_routing.py
  - .gitignore
autonomous: true
requirements: [MULTI-01, MULTI-02]

must_haves:
  truths:
    - "config/system.json has a locations array with casa entry containing CPE PT0002000084968079SX"
    - "config/system.json has a locations array with apartamento entry containing placeholder CPE"
    - "config/system.json no longer has top-level current_contract or pipeline sections"
    - "conftest.py provides multi_location_config fixture with two locations in tmp_path"
    - "CPE extraction from filename works for real E-REDES naming pattern"
    - ".gitignore covers nested data paths (data/casa/, data/apartamento/)"
  artifacts:
    - path: "config/system.json"
      provides: "Multi-location config schema with locations array"
      contains: '"locations"'
    - path: "tests/conftest.py"
      provides: "multi_location_config fixture for all phase 03 tests"
      contains: "multi_location_config"
    - path: "tests/test_multi_location_config.py"
      provides: "Tests for config loading and directory structure"
      exports: ["test_config_has_locations_array", "test_directory_structure"]
    - path: "tests/test_cpe_routing.py"
      provides: "Tests for CPE extraction and location lookup"
      exports: ["test_extract_cpe_from_real_filename", "test_find_location_by_cpe"]
  key_links:
    - from: "tests/conftest.py"
      to: "config/system.json"
      via: "multi_location_config fixture mirrors real schema"
      pattern: '"locations".*"id".*"casa"'
---

<objective>
Create the multi-location config schema, test infrastructure, and CPE routing utilities that all subsequent plans depend on.

Purpose: Establish the `locations` array contract in `config/system.json`, provide test fixtures for the entire phase, and implement the CPE extraction function needed by Plan 02.
Output: Updated `config/system.json`, new test files, updated `conftest.py`, updated `.gitignore`.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-location-refactor/03-RESEARCH.md

<interfaces>
<!-- Current config/system.json schema (to be replaced): -->
```json
{
  "current_contract": { "supplier": "...", "current_plan_contains": "...", "power_label": "..." },
  "eredes": { "storage_state_path": "...", "download_dir": "...", ... },
  "pipeline": { "processed_csv_path": "...", "report_dir": "...", ... },
  "schedule": { ... },
  "watcher": { ... }
}
```

<!-- Current conftest.py fixtures: -->
```python
@pytest.fixture
def test_config(tmp_path, sample_csv, sample_tariffs, sample_contract) -> Path:
    # Returns path to config/system.json with old schema
```

<!-- Existing test files (must remain green): -->
- tests/test_tiagofelicia_fallback.py (uses test_config fixture)
- tests/test_supplier_missing.py (uses test_config fixture)
- tests/test_xlsx_validation.py (standalone, no config fixture)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update config/system.json to locations schema + test fixtures + CPE routing tests</name>
  <files>config/system.json, tests/conftest.py, tests/test_multi_location_config.py, tests/test_cpe_routing.py</files>
  <read_first>
    - config/system.json
    - tests/conftest.py
    - tests/test_tiagofelicia_fallback.py
    - tests/test_supplier_missing.py
    - .planning/phases/03-multi-location-refactor/03-RESEARCH.md (Architecture Patterns section for exact schema)
  </read_first>
  <behavior>
    - test_config_has_locations_array: load config/system.json, assert "locations" key exists, assert len >= 1, assert locations[0]["id"] == "casa"
    - test_location_has_required_keys: each location has keys id, name, cpe, current_contract, pipeline
    - test_casa_cpe_matches: locations[0]["cpe"] == "PT0002000084968079SX"
    - test_directory_structure: given multi_location_config fixture, assert data/casa/raw/eredes/, data/casa/processed/, data/casa/reports/, state/casa/ all exist in tmp_path
    - test_extract_cpe_from_real_filename: extract_cpe_from_filename("Consumos_PT0002000084968079SX_2026-02-07_2026-03-26_20260326043814.xlsx") == "PT0002000084968079SX"
    - test_extract_cpe_short_format: extract_cpe_from_filename("Consumos_PT0002000084968079SX_20260326042940.xlsx") == "PT0002000084968079SX"
    - test_extract_cpe_no_match: extract_cpe_from_filename("random_file.xlsx") is None
    - test_find_location_by_cpe: find_location_by_cpe(locations, "PT0002000084968079SX") returns the casa location dict
    - test_find_location_by_cpe_unknown: find_location_by_cpe(locations, "PT9999999999999999XX") returns None
  </behavior>
  <action>
    **Step 1 (RED): Create test files first.**

    Create `tests/test_multi_location_config.py` with tests listed in behavior block. The tests should:
    - Import `json` and `pathlib.Path`
    - Load config from the real `config/system.json` (using `project_root` fixture from conftest)
    - Use `multi_location_config` fixture (to be created) for directory structure tests

    Create `tests/test_cpe_routing.py` with CPE extraction and lookup tests. These tests should import `extract_cpe_from_filename` and `find_location_by_cpe` from a new module `src/backend/cpe_routing.py`.

    **Step 2 (GREEN): Implement.**

    2a. Create `src/backend/cpe_routing.py` with:
    ```python
    import re
    from pathlib import Path

    CPE_PATTERN = re.compile(r"Consumos_(PT\w+?)_")

    def extract_cpe_from_filename(filename: str) -> str | None:
        m = CPE_PATTERN.search(Path(filename).name)
        return m.group(1) if m else None

    def find_location_by_cpe(locations: list[dict], cpe: str) -> dict | None:
        for loc in locations:
            if loc.get("cpe") == cpe:
                return loc
        return None
    ```

    2b. Update `config/system.json` to the new schema with `"locations"` array. Use the exact schema from RESEARCH.md Architecture Patterns section. Key changes:
    - Remove top-level `"current_contract"` and `"pipeline"` sections
    - Add `"locations": [...]` with casa (CPE `PT0002000084968079SX`) and apartamento (CPE `PT000200XXXXXXXXXX` placeholder)
    - Each location has `id`, `name`, `cpe`, `current_contract`, `pipeline` (with nested paths like `data/casa/raw/eredes` etc.)
    - Keep `eredes`, `schedule`, `watcher` sections at top level (shared)
    - In `eredes` section, rename `download_dir` to `download_dir_base` with value `data/{location_id}/raw/eredes`

    2c. Update `tests/conftest.py`:
    - Keep existing fixtures (`project_root`, `sample_csv`, `sample_tariffs`, `sample_contract`, `test_config`) unchanged so existing tests remain green
    - Add new `multi_location_config` fixture that creates a `config/system.json` in `tmp_path` with the new locations schema (two locations: casa + apartamento_mock), creates all expected directories (`data/casa/raw/eredes/`, `data/casa/processed/`, `data/casa/reports/`, `state/casa/`, and same for apartamento), and returns the config file path
    - The `test_config` fixture MUST keep the old schema (top-level `current_contract` and `pipeline`) so that existing Phase 02 tests continue to pass. The old fixture is a compatibility shim — it will be updated when those tests are migrated in Plan 02.

    **Step 3: Verify existing tests still pass** by running the full suite.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - config/system.json contains `"locations"` key at top level
    - config/system.json contains `"id": "casa"` and `"cpe": "PT0002000084968079SX"`
    - config/system.json contains `"id": "apartamento"` and `"cpe": "PT000200XXXXXXXXXX"`
    - config/system.json does NOT contain top-level `"current_contract"` key (grep returns empty)
    - config/system.json does NOT contain top-level `"pipeline"` key (grep returns empty)
    - tests/test_multi_location_config.py exists with at least 4 test functions
    - tests/test_cpe_routing.py exists with at least 5 test functions
    - src/backend/cpe_routing.py exists with `def extract_cpe_from_filename` and `def find_location_by_cpe`
    - tests/conftest.py contains `def multi_location_config`
    - tests/conftest.py still contains `def test_config` (backward compat)
    - `python3 -m pytest tests/ -x -q` exits 0 (all tests pass including old Phase 02 tests)
  </acceptance_criteria>
  <done>Config schema migrated to locations array, CPE routing module created, test infrastructure ready for phase, all existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore for nested data paths</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
    Update `.gitignore` to ensure nested location paths are covered. The current rules `data/raw/`, `data/processed/`, `data/reports/` only match flat structure. Add explicit rules for nested paths:

    Replace the three data sections with:
    ```
    # Dados brutos com CPE exposto (flat e nested)
    data/raw/
    data/*/raw/

    # Dados processados (gerados pelo pipeline)
    data/processed/
    data/*/processed/

    # Relatorios gerados
    data/reports/
    data/*/reports/
    ```

    Also add state per-location exclusions:
    ```
    # Estado per-location
    state/*/monthly_status.json
    state/*/last_processed_download.json
    ```

    Keep all other existing rules unchanged.

    After writing, run `git status` to verify no data files appear in staging.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && git status --porcelain | grep -c "^?? data/" || echo "0 untracked data files"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains `data/*/raw/`
    - .gitignore contains `data/*/processed/`
    - .gitignore contains `data/*/reports/`
    - .gitignore contains `state/*/monthly_status.json`
    - .gitignore contains `state/*/last_processed_download.json`
    - `git status` shows no files under data/ as untracked
  </acceptance_criteria>
  <done>.gitignore covers both flat (legacy) and nested (multi-location) data paths.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -v` — full suite green (existing + new tests)
- `python3 -c "import json; c=json.load(open('config/system.json')); assert 'locations' in c; assert len(c['locations']) == 2"` — config schema valid
- `git status` — no data files staged
</verification>

<success_criteria>
- config/system.json uses locations array schema with casa and apartamento
- CPE routing module exists with extract + lookup functions
- Test infrastructure ready: conftest.py has multi_location_config fixture
- All 11+ existing tests still pass
- .gitignore covers nested paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-location-refactor/03-01-SUMMARY.md`
</output>
