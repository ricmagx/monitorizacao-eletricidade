---
phase: 08-upload-pdf-extrac-o-de-faturas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-docker.txt
  - src/web/services/extrator_pdf.py
  - tests/test_extrator_pdf.py
autonomous: true
requirements:
  - UPLD-03
  - UPLD-04
must_haves:
  truths:
    - "pdfplumber esta disponivel no container Docker"
    - "Texto de fatura Meo Energia produz total_eur e year_month correctos"
    - "Texto de fatura Endesa produz total_eur e year_month correctos (so electricidade)"
    - "Texto de fatura Endesa com gas nao inclui valores de gas no total"
    - "PDF nao reconhecido retorna erro claro sem crash"
    - "CPE com espaco no texto e normalizado antes do lookup"
  artifacts:
    - path: "requirements-docker.txt"
      provides: "pdfplumber dependency for Docker"
      contains: "pdfplumber"
    - path: "src/web/services/extrator_pdf.py"
      provides: "PDF extraction service (Meo Energia + Endesa)"
      exports: ["extrair_fatura"]
      min_lines: 60
    - path: "tests/test_extrator_pdf.py"
      provides: "Unit tests for PDF extraction"
      min_lines: 80
  key_links:
    - from: "src/web/services/extrator_pdf.py"
      to: "pdfplumber"
      via: "import pdfplumber; pdfplumber.open(BytesIO(...))"
      pattern: "pdfplumber\\.open"
    - from: "src/web/services/extrator_pdf.py"
      to: "src/web/services/locais_service.py"
      via: "get_local_by_cpe for CPE lookup"
      pattern: "get_local_by_cpe"
---

<objective>
Adicionar pdfplumber ao Docker e implementar o servico de extraccao de faturas PDF (Meo Energia + Endesa) com testes unitarios completos.

Purpose: Criar a logica core de extraccao de dados de faturas PDF — total pago em EUR e periodo de faturacao — para os dois formatos conhecidos, com deteccao de formato por texto, CPE lookup, e filtragem explicita de gas nas faturas Endesa. Este servico e consumido pelo endpoint de upload no Plan 02.
Output: `src/web/services/extrator_pdf.py` funcional com testes verdes, pdfplumber em requirements-docker.txt.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upload-pdf-extrac-o-de-faturas/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/schema.py:
```python
custos_reais = Table(
    "custos_reais", metadata,
    Column("id", Integer, primary_key=True),
    Column("location_id", String(64), nullable=False),
    Column("year_month", String(7), nullable=False),  # "YYYY-MM"
    Column("custo_eur", Float, nullable=False),
    Column("source", String(64)),  # usar "pdf_upload"
    Column("created_at", DateTime, default=lambda: datetime.now(timezone.utc)),
    UniqueConstraint("location_id", "year_month", name="uq_custos_loc_month"),
)
```

From src/web/services/locais_service.py:
```python
def get_local_by_cpe(cpe: str, engine: Engine) -> dict | None:
    """Retorna um local pelo CPE ou None."""
```

From src/web/services/ingestao_xlsx.py (pattern to replicate):
```python
def ingerir_xlsx(tmp_path: Path, filename: str, engine: Engine) -> dict:
    """Returns dict com keys: erro (str|None), location_id, location_name, cpe, ..."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pdfplumber dependency + test stubs com fixtures sinteticas</name>
  <files>requirements-docker.txt, tests/test_extrator_pdf.py</files>
  <read_first>
    - requirements-docker.txt (estado actual das dependencias)
    - tests/conftest.py (fixtures existentes: db_engine_test, web_client)
    - tests/test_ingestao_xlsx.py (pattern de testes a replicar)
    - .planning/phases/08-upload-pdf-extrac-o-de-faturas/08-RESEARCH.md (regex candidatos e pitfalls)
  </read_first>
  <behavior>
    - test_extrair_meo_energia: texto sintetico Meo Energia com "Total a pagar: 45,32 EUR" e periodo "01-01-2026 a 31-01-2026" e CPE "PT0002000084968079SX" -> retorna {"erro": None, "formato": "meo_energia", "cpe": "PT0002000084968079SX", "year_month": "2026-01", "custo_eur": 45.32}
    - test_extrair_endesa: texto sintetico Endesa com secccao Electricidade, total e periodo -> retorna formato "endesa" com custo_eur e year_month correctos
    - test_endesa_ignora_gas: texto sintetico Endesa com seccoes Electricidade (30.00 EUR) E Gas Natural (15.00 EUR) -> custo_eur == 30.00, nao 45.00
    - test_formato_desconhecido: texto sem keywords de Meo ou Endesa -> retorna {"erro": "Formato de fatura nao reconhecido...", ...}
    - test_cpe_com_espaco: texto com CPE "PT000200003982208 2NT" (com espaco) -> cpe retornado e "PT0002000039822082NT" (normalizado sem espaco)
    - test_extrair_texto_pdf_bytesio: pdfplumber.open(BytesIO(bytes)) extrai texto nao-vazio de PDF real minimo
    - test_ingestao_pdf_escreve_sqlite: extrair_fatura + escrita em custos_reais com engine in-memory -> row existe com location_id, year_month, custo_eur, source="pdf_upload"
    - test_ingestao_pdf_idempotencia: mesma fatura ingerida 2x -> apenas 1 row em custos_reais
  </behavior>
  <action>
    1. Adicionar `pdfplumber==0.11.7` a `requirements-docker.txt` (depois da linha `openpyxl==3.1.5`, antes de `requests`).

    2. Criar `tests/test_extrator_pdf.py` com:

    a) Constantes de texto sintetico no topo do ficheiro:

    ```python
    TEXTO_MEO_ENERGIA = """
    Meo Energia
    Fatura de Eletricidade
    CPE: PT0002000084968079SX
    Periodo de faturacao: 01-01-2026 a 31-01-2026
    Consumo em vazio: 200.5 kWh
    Consumo fora de vazio: 350.2 kWh
    Total a pagar: 45,32 EUR
    """

    TEXTO_ENDESA = """
    Endesa Energia
    Fatura Eletricidade + Gas
    CPE: PT0002000084968079SX
    Eletricidade
    Periodo de Consumo: 01/02/2026 a 28/02/2026
    Potencia contratada: 10.35 kVA
    Total Eletricidade: 38,50 EUR
    Gas Natural
    Consumo: 150 m3
    Total Gas: 22,10 EUR
    Total Fatura: 60,60 EUR
    """

    TEXTO_ENDESA_SO_ELETRICIDADE = """
    Endesa Energia
    CPE: PT0002000084968079SX
    Eletricidade
    Periodo de Consumo: 01/03/2026 a 31/03/2026
    Total Eletricidade: 42,75 EUR
    """

    TEXTO_DESCONHECIDO = """
    Documento Qualquer
    Sem formato reconhecido
    Valor: 100.00 EUR
    """

    TEXTO_CPE_COM_ESPACO = """
    Endesa Energia
    CPE: PT000200003982208 2NT
    Eletricidade
    Periodo de Consumo: 01/01/2026 a 31/01/2026
    Total Eletricidade: 29,90 EUR
    """
    ```

    b) Testes que importam `from src.web.services.extrator_pdf import extrair_fatura` e validam o dict retornado. Cada teste chama `extrair_fatura(texto)` onde `texto` e a string sintetica (a funcao aceita texto ja extraido para testabilidade — a leitura do PDF com pdfplumber e feita pela camada de cima).

    c) Teste `test_extrair_texto_pdf_bytesio` que cria um PDF minimo com reportlab ou fpdf, e verifica que `pdfplumber.open(BytesIO(pdf_bytes))` extrai texto. Se reportlab/fpdf nao estiver disponivel, usar `pytest.importorskip("fpdf")` ou criar PDF binario minimo hardcoded.

    d) Testes `test_ingestao_pdf_escreve_sqlite` e `test_ingestao_pdf_idempotencia` que usam fixture `db_engine_test` do conftest.py. Inserem um local na tabela `locais` com CPE "PT0002000084968079SX", chamam funcao de ingestao que combina extrair_fatura + escrita em custos_reais, e verificam o conteudo da tabela.

    3. Todos os testes devem FALHAR inicialmente (RED) porque `extrator_pdf.py` nao existe ainda.
  </action>
  <verify>
    <automated>grep pdfplumber requirements-docker.txt && python3 -m pytest tests/test_extrator_pdf.py --collect-only -q 2>&1 | grep "test session starts\|tests collected\|error"</automated>
  </verify>
  <acceptance_criteria>
    - requirements-docker.txt contem linha "pdfplumber==0.11.7"
    - tests/test_extrator_pdf.py existe com pelo menos 8 funcoes test_*
    - `pytest tests/test_extrator_pdf.py --collect-only` lista pelo menos 8 testes
    - Cada teste tem assert explicito (nao apenas pass)
    - Fixtures de texto sintetico incluem keywords "Meo Energia", "Endesa", "Eletricidade", "Gas Natural"
  </acceptance_criteria>
  <done>pdfplumber em requirements-docker.txt, 8+ testes colectaveis em test_extrator_pdf.py que falham (RED) por falta do modulo extrator_pdf</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar extrator_pdf.py — parsers Meo Energia + Endesa + ingestao SQLite</name>
  <files>src/web/services/extrator_pdf.py</files>
  <read_first>
    - tests/test_extrator_pdf.py (testes RED da Task 1 — estes definem o contrato)
    - src/web/services/ingestao_xlsx.py (pattern de ingestao a replicar)
    - src/web/services/locais_service.py (get_local_by_cpe signature)
    - src/db/schema.py (custos_reais table definition)
    - .planning/phases/08-upload-pdf-extrac-o-de-faturas/08-RESEARCH.md (regex candidatos, pitfalls, patterns)
  </read_first>
  <behavior>
    - extrair_fatura(texto: str) retorna dict com keys: erro, formato, cpe, year_month, custo_eur
    - Detecta formato por keywords: "Meo Energia" -> meo_energia, "Endesa" -> endesa
    - Extrai CPE com regex tolerante a espacos: `PT\d{16,18}[\w ]{1,4}` normalizado (remove espacos)
    - Meo Energia: regex para "Total a pagar: XX,XX" e periodo "DD-MM-YYYY a DD-MM-YYYY"
    - Endesa: regex para "Total Eletricidade: XX,XX" (NAO "Total Fatura" nem "Total Gas")
    - Endesa periodo: "Periodo de Consumo: DD/MM/YYYY a DD/MM/YYYY"
    - Formato desconhecido: retorna {"erro": "Formato de fatura nao reconhecido. Formatos suportados: Meo Energia, Endesa."}
    - ingerir_pdf(pdf_bytes: bytes, engine: Engine) -> dict: extrai texto com pdfplumber, chama extrair_fatura, faz lookup CPE, escreve custos_reais
  </behavior>
  <action>
    Criar `src/web/services/extrator_pdf.py` com:

    1. **Imports:**
    ```python
    import re
    from io import BytesIO
    import pdfplumber
    from sqlalchemy import Engine
    from sqlalchemy.dialects.sqlite import insert as sqlite_insert
    from src.db.schema import custos_reais
    from src.web.services.locais_service import get_local_by_cpe
    ```

    2. **Constantes regex** (multiplos candidatos por campo para robustez):
    ```python
    # CPE — tolerante a espacos
    CPE_PATTERN = re.compile(r"(PT\d{16,18}[\w ]{1,4})")

    # Meo Energia — total pago
    TOTAL_MEO_PATTERNS = [
        re.compile(r"Total\s+a\s+pagar[:\s]+(\d+[,\.]\d{2})\s*(?:EUR|€)", re.IGNORECASE),
        re.compile(r"Total[:\s]+(\d+[,\.]\d{2})\s*(?:EUR|€)", re.IGNORECASE),
    ]

    # Meo Energia — periodo (DD-MM-YYYY a DD-MM-YYYY)
    PERIODO_MEO_PATTERNS = [
        re.compile(r"(\d{2}-\d{2}-\d{4})\s+a\s+(\d{2}-\d{2}-\d{4})"),
        re.compile(r"(\d{2}/\d{2}/\d{4})\s+a\s+(\d{2}/\d{2}/\d{4})"),
    ]

    # Endesa — total ELECTRICIDADE (nunca "Total Fatura" nem "Total Gas")
    TOTAL_ENDESA_PATTERNS = [
        re.compile(r"Total\s+Ele[ct]ricidade[:\s]+(\d+[,\.]\d{2})\s*(?:EUR|€)", re.IGNORECASE),
    ]

    # Endesa — periodo
    PERIODO_ENDESA_PATTERNS = [
        re.compile(r"Per[ií]odo\s+de\s+Consumo[:\s]+(\d{2}/\d{2}/\d{4})\s+a\s+(\d{2}/\d{2}/\d{4})", re.IGNORECASE),
        re.compile(r"(\d{2}/\d{2}/\d{4})\s+a\s+(\d{2}/\d{2}/\d{4})"),
    ]
    ```

    3. **Funcao `_normalizar_texto(texto: str) -> str`**: aplica `re.sub(r"\s+", " ", texto)` para lidar com quebras de linha no meio de valores.

    4. **Funcao `_extrair_com_patterns(texto: str, patterns: list) -> re.Match | None`**: itera sobre lista de regex, retorna primeiro match.

    5. **Funcao `_parse_valor_eur(valor_str: str) -> float`**: converte "45,32" para 45.32 (replace virgula por ponto).

    6. **Funcao `_parse_periodo_para_year_month(data_inicio: str) -> str`**: converte "01-01-2026" ou "01/01/2026" para "2026-01".

    7. **Funcao `extrair_fatura(texto: str) -> dict`**:
       - Normalizar texto com `_normalizar_texto`
       - Extrair CPE com `CPE_PATTERN`, normalizar removendo espacos
       - Detectar formato: `"Meo Energia"` no texto -> meo_energia; `"Endesa"` no texto -> endesa
       - Se nenhum formato detectado: retornar `{"erro": "Formato de fatura nao reconhecido. Formatos suportados: Meo Energia, Endesa.", "formato": None, "cpe": cpe_extraido, "year_month": None, "custo_eur": None}`
       - Conforme formato, aplicar TOTAL e PERIODO patterns correctos
       - Se total nao encontrado: retornar erro "Total nao encontrado na fatura {formato}"
       - Se periodo nao encontrado: retornar erro "Periodo nao encontrado na fatura {formato}"
       - Retornar `{"erro": None, "formato": formato, "cpe": cpe, "year_month": year_month, "custo_eur": custo_eur}`

    8. **Funcao `extrair_texto_pdf(pdf_bytes: bytes) -> str`**:
       - `pdfplumber.open(BytesIO(pdf_bytes))` — extrai texto de todas as paginas concatenado
       - Retorna texto normalizado

    9. **Funcao `ingerir_pdf(pdf_bytes: bytes, engine: Engine) -> dict`**:
       - Chama `extrair_texto_pdf(pdf_bytes)` para obter texto
       - Chama `extrair_fatura(texto)` para extrair dados
       - Se erro na extraccao: retorna dict com erro
       - Se CPE extraido: chama `get_local_by_cpe(cpe, engine)` para resolver local
       - Se local nao encontrado: retorna erro "CPE {cpe} nao corresponde a nenhum local configurado."
       - Escreve em `custos_reais` com `sqlite_insert(...).on_conflict_do_nothing(index_elements=["location_id", "year_month"])`
       - Retorna `{"erro": None, "formato": ..., "location_id": ..., "location_name": ..., "cpe": ..., "year_month": ..., "custo_eur": ..., "inserido": True/False}`

    Executar testes apos implementacao: `python3 -m pytest tests/test_extrator_pdf.py -x -v`
    Todos os 8+ testes devem passar (GREEN).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_extrator_pdf.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/web/services/extrator_pdf.py existe com funcoes: extrair_fatura, extrair_texto_pdf, ingerir_pdf
    - `grep "def extrair_fatura" src/web/services/extrator_pdf.py` retorna match
    - `grep "def ingerir_pdf" src/web/services/extrator_pdf.py` retorna match
    - `grep "def extrair_texto_pdf" src/web/services/extrator_pdf.py` retorna match
    - `grep "pdfplumber.open" src/web/services/extrator_pdf.py` retorna match
    - `grep "on_conflict_do_nothing" src/web/services/extrator_pdf.py` retorna match
    - `grep "get_local_by_cpe" src/web/services/extrator_pdf.py` retorna match
    - `grep "Meo Energia" src/web/services/extrator_pdf.py` retorna match (deteccao de formato)
    - `grep "Endesa" src/web/services/extrator_pdf.py` retorna match (deteccao de formato)
    - `grep "Total Ele" src/web/services/extrator_pdf.py` retorna match (regex Endesa electricidade, NAO "Total Fatura")
    - `python3 -m pytest tests/test_extrator_pdf.py -x -v` retorna 0 (todos os testes passam)
    - Suite completa nao tem regressoes: `python3 -m pytest tests/ -x -q` retorna 0
  </acceptance_criteria>
  <done>extrator_pdf.py implementado com parsers Meo Energia e Endesa, ingestao em custos_reais com idempotencia, todos os testes verdes, suite completa sem regressoes</done>
</task>

</tasks>

<verification>
1. `grep pdfplumber requirements-docker.txt` — confirma dependencia
2. `python3 -m pytest tests/test_extrator_pdf.py -v` — todos os testes passam
3. `python3 -m pytest tests/ -x -q` — suite completa sem regressoes
</verification>

<success_criteria>
- pdfplumber==0.11.7 em requirements-docker.txt
- extrator_pdf.py com funcoes extrair_fatura(), extrair_texto_pdf(), ingerir_pdf()
- 8+ testes unitarios verdes cobrindo: Meo Energia, Endesa, gas ignorado, formato desconhecido, CPE com espaco, escrita SQLite, idempotencia
- Suite de testes completa sem regressoes
</success_criteria>

<output>
After completion, create `.planning/phases/08-upload-pdf-extrac-o-de-faturas/08-01-SUMMARY.md`
</output>
