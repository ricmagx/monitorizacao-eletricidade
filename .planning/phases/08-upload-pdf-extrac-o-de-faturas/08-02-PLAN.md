---
phase: 08-upload-pdf-extrac-o-de-faturas
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/web/routes/upload.py
  - src/web/templates/partials/upload_pdf.html
  - src/web/templates/partials/upload_pdf_confirmacao.html
  - tests/test_upload_endpoint.py
autonomous: true
requirements:
  - UPLD-03
  - UPLD-04
must_haves:
  truths:
    - "O utilizador pode fazer upload de PDF via formulario web"
    - "Upload de PDF Meo Energia mostra total pago e periodo na confirmacao"
    - "Upload de PDF Endesa mostra total pago e periodo na confirmacao"
    - "PDF nao reconhecido mostra mensagem de erro clara sem crash"
    - "O custo extraido fica gravado em custos_reais SQLite e tambem no custos_reais.json para que o dashboard o exiba"
  artifacts:
    - path: "src/web/routes/upload.py"
      provides: "POST /upload/pdf endpoint"
      contains: "upload_pdf"
    - path: "src/web/templates/partials/upload_pdf.html"
      provides: "Upload PDF form partial"
      contains: "upload/pdf"
    - path: "src/web/templates/partials/upload_pdf_confirmacao.html"
      provides: "Upload PDF confirmation partial"
      contains: "custo_eur"
    - path: "tests/test_upload_endpoint.py"
      provides: "Integration test for upload PDF endpoint"
      contains: "test_upload_pdf"
  key_links:
    - from: "src/web/routes/upload.py"
      to: "src/web/services/extrator_pdf.py"
      via: "from src.web.services.extrator_pdf import ingerir_pdf"
      pattern: "from src\\.web\\.services\\.extrator_pdf import ingerir_pdf"
    - from: "src/web/routes/upload.py"
      to: "src/web/services/data_loader.py"
      via: "save_custo_real writes custos_reais.json for dashboard visibility"
      pattern: "save_custo_real"
    - from: "src/web/routes/upload.py"
      to: "src/web/templates/partials/upload_pdf_confirmacao.html"
      via: "TemplateResponse"
      pattern: "upload_pdf_confirmacao"
    - from: "src/web/templates/partials/upload_pdf.html"
      to: "POST /upload/pdf"
      via: "hx-post form submission"
      pattern: "hx-post.*/upload/pdf"
---

<objective>
Ligar o servico de extraccao PDF ao endpoint FastAPI e ao template HTML, completando o fluxo de upload de PDF de faturas via browser. Garantir que o custo extraido fica visivel no dashboard (escrita em custos_reais.json alem do SQLite).

Purpose: O utilizador faz upload de um PDF de fatura via browser, o sistema extrai automaticamente o total pago e periodo, e mostra confirmacao (ou erro claro se formato nao reconhecido). O custo fica gravado em custos_reais SQLite E em custos_reais.json para que o dashboard o exiba imediatamente.
Output: Endpoint POST /upload/pdf funcional com formulario HTML, template de confirmacao, e teste de integracao do endpoint.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upload-pdf-extrac-o-de-faturas/08-RESEARCH.md
@.planning/phases/08-upload-pdf-extrac-o-de-faturas/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 -->

From src/web/services/extrator_pdf.py (created in Plan 01):
```python
def extrair_fatura(texto: str) -> dict:
    """Returns dict: erro (str|None), formato (str|None), cpe (str|None),
    year_month (str|None), custo_eur (float|None)"""

def extrair_texto_pdf(pdf_bytes: bytes) -> str:
    """Extracts text from PDF bytes using pdfplumber (lazy import)."""

def ingerir_pdf(pdf_bytes: bytes, engine: Engine) -> dict:
    """Returns dict: erro (str|None), formato, location_id, location_name,
    cpe, year_month, custo_eur, inserido (bool)"""
```

From src/web/routes/upload.py (existing XLSX pattern to replicate):
```python
@router.post("/upload/xlsx", response_class=HTMLResponse)
async def upload_xlsx(request: Request, background_tasks: BackgroundTasks,
                      ficheiro: UploadFile = File(...)):
    templates = request.app.state.templates
    engine = request.app.state.db_engine
    # ... read content, call service, return template response
```

From src/web/services/data_loader.py (dashboard custos_reais integration):
```python
def save_custo_real(custos_path: Path, year_month: str, custo_eur: float) -> None:
    """Adiciona/actualiza entry em custos_reais.json. Cria ficheiro se nao existe."""

def load_custos_reais(custos_path: Path) -> dict:
    """Le custos_reais.json e retorna entries dict. {} se nao existe."""
```

NOTA CRITICA: O dashboard le custos_reais de ficheiros JSON (data/{local_id}/custos_reais.json),
NAO da tabela SQLite. Para que o custo extraido do PDF seja visivel no dashboard, o endpoint
DEVE tambem chamar save_custo_real() para escrever o JSON alem da escrita SQLite feita por ingerir_pdf().

From src/web/templates/partials/upload_xlsx.html (form pattern):
<!-- hx-post="/upload/xlsx" hx-target="#upload-resultado" hx-encoding="multipart/form-data" -->

From src/web/templates/partials/upload_confirmacao.html (confirmation pattern):
<!-- Shows erro or resultado with periodo, local, meses info -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint POST /upload/pdf + templates HTML + escrita custos_reais.json</name>
  <files>src/web/routes/upload.py, src/web/templates/partials/upload_pdf.html, src/web/templates/partials/upload_pdf_confirmacao.html</files>
  <read_first>
    - src/web/routes/upload.py (endpoint XLSX existente — replicar pattern para PDF)
    - src/web/templates/partials/upload_xlsx.html (formulario XLSX — replicar para PDF)
    - src/web/templates/partials/upload_confirmacao.html (confirmacao XLSX — replicar para PDF)
    - src/web/services/extrator_pdf.py (API de ingerir_pdf criada no Plan 01)
    - src/web/services/data_loader.py (save_custo_real — necessario para escrita JSON)
    - src/web/routes/custos_reais.py (exemplo de como save_custo_real e chamado com custos_path)
    - src/web/app.py (verificar que upload_router ja esta registado — nao precisa de alteracao)
    - requirements.txt (confirmar que pdfplumber esta presente — adicionado no Plan 01)
  </read_first>
  <action>
    1. **Modificar `src/web/routes/upload.py`** — adicionar imports e endpoint PDF:

    a) Adicionar imports no topo (junto aos imports existentes):
    ```python
    from src.web.services.extrator_pdf import ingerir_pdf
    from src.web.services.data_loader import save_custo_real
    ```

    NOTA sobre pdfplumber: O import de `ingerir_pdf` e seguro porque extrator_pdf.py usa lazy import para pdfplumber (dentro de `extrair_texto_pdf()`, nao no topo do modulo). Portanto este import NAO causa ImportError mesmo que pdfplumber nao esteja instalado — so falha quando se tenta processar um PDF.

    b) Adicionar endpoint apos o existente `upload_xlsx`:
    ```python
    @router.post("/upload/pdf", response_class=HTMLResponse)
    async def upload_pdf(
        request: Request,
        ficheiro: UploadFile = File(...),
    ):
        """Recebe PDF de fatura, extrai total pago e periodo, grava em custos_reais."""
        templates = request.app.state.templates
        engine = request.app.state.db_engine

        content = await ficheiro.read()  # BytesIO — sem ficheiro temporario

        resultado = ingerir_pdf(content, engine)

        if resultado["erro"]:
            return templates.TemplateResponse(
                request=request,
                name="partials/upload_pdf_confirmacao.html",
                context={"erro": resultado["erro"], "resultado": None},
            )

        # Escrever tambem no custos_reais.json para que o dashboard mostre o valor.
        # O dashboard le de data/{local_id}/custos_reais.json, nao da tabela SQLite.
        project_root = request.app.state.project_root
        custos_path = project_root / "data" / resultado["location_id"] / "custos_reais.json"
        save_custo_real(custos_path, resultado["year_month"], resultado["custo_eur"])

        return templates.TemplateResponse(
            request=request,
            name="partials/upload_pdf_confirmacao.html",
            context={"erro": None, "resultado": resultado},
        )
    ```

    Nota: NAO e necessario `BackgroundTasks` — o PDF nao dispara consulta tiagofelicia (diferente do XLSX). NAO e necessario `tempfile` — pdfplumber aceita BytesIO, nao precisa de ficheiro no disco.

    2. **Criar `src/web/templates/partials/upload_pdf.html`**:
    ```html
    <div id="upload-pdf-form">
      <h3>Upload Fatura PDF</h3>
      <p>Formatos suportados: Meo Energia, Endesa</p>
      <form hx-post="/upload/pdf"
            hx-target="#upload-pdf-resultado"
            hx-encoding="multipart/form-data"
            hx-swap="innerHTML">
        <input type="file" name="ficheiro" accept=".pdf" required>
        <button type="submit">Enviar PDF</button>
      </form>
      <div id="upload-pdf-resultado"></div>
    </div>
    ```

    3. **Criar `src/web/templates/partials/upload_pdf_confirmacao.html`**:
    ```html
    {% if erro %}
    <div class="alert alert-error">
      <strong>Erro:</strong> {{ erro }}
    </div>
    {% else %}
    <div class="alert alert-success">
      <strong>Fatura importada com sucesso!</strong>
      <ul>
        <li>Formato: {{ resultado.formato }}</li>
        <li>Local: {{ resultado.location_name }} (CPE: {{ resultado.cpe }})</li>
        <li>Periodo: {{ resultado.year_month }}</li>
        <li>Total pago: {{ "%.2f"|format(resultado.custo_eur) }} EUR</li>
        {% if not resultado.inserido %}
        <li><em>Nota: ja existia custo para este periodo — dados nao duplicados.</em></li>
        {% endif %}
      </ul>
    </div>
    {% endif %}
    ```

    4. O router `upload_router` ja esta registado em `app.py` (linhas 96-102) — NAO modificar app.py. O novo endpoint `/upload/pdf` fica automaticamente disponivel porque esta no mesmo router.
  </action>
  <verify>
    <automated>python3 -c "from src.web.routes.upload import router; routes = [r.path for r in router.routes]; assert '/upload/pdf' in routes, f'Missing /upload/pdf in {routes}'; print('OK: /upload/pdf route registered')" && grep "save_custo_real" src/web/routes/upload.py && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "upload_pdf" src/web/routes/upload.py` retorna match (funcao do endpoint)
    - `grep "ingerir_pdf" src/web/routes/upload.py` retorna match (import do servico)
    - `grep "save_custo_real" src/web/routes/upload.py` retorna match (escrita JSON para dashboard)
    - `grep "upload_pdf_confirmacao" src/web/routes/upload.py` retorna match (template response)
    - `grep "hx-post.*upload/pdf" src/web/templates/partials/upload_pdf.html` retorna match
    - `grep "accept.*pdf" src/web/templates/partials/upload_pdf.html` retorna match (filtro ficheiro)
    - `grep "custo_eur" src/web/templates/partials/upload_pdf_confirmacao.html` retorna match
    - `grep "location_name" src/web/templates/partials/upload_pdf_confirmacao.html` retorna match
    - `grep "formato" src/web/templates/partials/upload_pdf_confirmacao.html` retorna match
    - `grep "inserido" src/web/templates/partials/upload_pdf_confirmacao.html` retorna match (nota de idempotencia)
    - NAO existe "BackgroundTasks" no endpoint upload_pdf (diferente do XLSX)
    - NAO existe "tempfile" no endpoint upload_pdf (pdfplumber usa BytesIO)
    - `python3 -m pytest tests/ -x -q` retorna 0 (suite completa sem regressoes)
  </acceptance_criteria>
  <done>POST /upload/pdf endpoint funcional, formulario HTML com hx-post e filtro .pdf, template de confirmacao mostra formato/local/periodo/total ou mensagem de erro, custo escrito em custos_reais.json para visibilidade no dashboard, suite de testes completa passa</done>
</task>

<task type="auto">
  <name>Task 2: Teste de integracao do endpoint POST /upload/pdf</name>
  <files>tests/test_upload_endpoint.py</files>
  <read_first>
    - tests/conftest.py (fixture web_client existente)
    - tests/test_web_upload.py (pattern de testes de upload XLSX existente, se existir)
    - src/web/routes/upload.py (endpoint PDF criado na Task 1)
    - src/web/services/extrator_pdf.py (ingerir_pdf signature e retorno)
  </read_first>
  <behavior>
    - test_upload_pdf_returns_200: POST /upload/pdf com PDF sintetico (bytes minimos de um PDF valido) retorna HTTP 200
    - test_upload_pdf_erro_formato: POST /upload/pdf com bytes que nao sao PDF valido ou PDF sem formato reconhecido retorna HTTP 200 com mensagem de erro no HTML
    - test_upload_pdf_renders_confirmacao: POST /upload/pdf com mock de ingerir_pdf retornando sucesso -> response HTML contem "Fatura importada com sucesso"
  </behavior>
  <action>
    Criar `tests/test_upload_endpoint.py` com testes de integracao para o endpoint POST /upload/pdf:

    1. Importar o `web_client` fixture do conftest.py (httpx.AsyncClient ou TestClient conforme o pattern do projecto).

    2. **test_upload_pdf_returns_200**: Envia POST /upload/pdf com um ficheiro PDF sintetico (pode ser bytes minimos validos ou mock). Verifica que o status code e 200. Se pdfplumber nao conseguir extrair texto, o endpoint deve retornar 200 com mensagem de erro (nao 500).

    3. **test_upload_pdf_erro_formato**: Envia POST /upload/pdf com bytes que produzem texto sem formato reconhecido. Verifica que a resposta HTML contem "Formato de fatura nao reconhecido" ou "Erro".

    4. **test_upload_pdf_renders_confirmacao**: Usa `unittest.mock.patch` para mockar `ingerir_pdf` retornando um resultado de sucesso (erro=None, formato="meo_energia", location_id="casa", location_name="Casa", cpe="PT0002000084968079SX", year_month="2026-01", custo_eur=45.32, inserido=True). Verifica que a resposta HTML contem "Fatura importada com sucesso" e "45.32".

    Nota: Tambem mockar `save_custo_real` para evitar escrita em disco durante testes.

    Pattern de envio multipart com httpx/TestClient:
    ```python
    response = client.post(
        "/upload/pdf",
        files={"ficheiro": ("fatura.pdf", pdf_bytes, "application/pdf")},
    )
    ```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_upload_endpoint.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_upload_endpoint.py existe com pelo menos 3 testes
    - `python3 -m pytest tests/test_upload_endpoint.py -x -v` retorna 0 (todos passam)
    - `python3 -m pytest tests/ -x -q` retorna 0 (suite completa sem regressoes)
  </acceptance_criteria>
  <done>tests/test_upload_endpoint.py com 3+ testes de integracao verdes cobrindo: HTTP 200, erro de formato, e confirmacao com mock</done>
</task>

</tasks>

<verification>
1. `python3 -c "from src.web.routes.upload import router; print([r.path for r in router.routes])"` — lista /upload/xlsx e /upload/pdf
2. `ls src/web/templates/partials/upload_pdf*.html` — ambos ficheiros existem
3. `grep "save_custo_real" src/web/routes/upload.py` — confirma escrita JSON para dashboard
4. `python3 -m pytest tests/test_upload_endpoint.py -v` — testes de integracao passam
5. `python3 -m pytest tests/ -x -q` — suite completa sem regressoes
</verification>

<success_criteria>
- POST /upload/pdf endpoint registado no router
- Formulario HTML upload_pdf.html com hx-post e accept=".pdf"
- Template confirmacao mostra formato, local, periodo, total EUR, e nota de idempotencia
- Erro claro para formatos nao reconhecidos (sem crash)
- Custo escrito em custos_reais.json (via save_custo_real) para visibilidade no dashboard
- tests/test_upload_endpoint.py com 3+ testes de integracao verdes
- Suite de testes completa passa sem regressoes
</success_criteria>

<output>
After completion, create `.planning/phases/08-upload-pdf-extrac-o-de-faturas/08-02-SUMMARY.md`
</output>
