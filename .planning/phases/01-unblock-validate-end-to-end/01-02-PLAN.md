---
phase: 01-unblock-validate-end-to-end
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [FIX-03, VAL-01, VAL-02]

must_haves:
  truths:
    - "Sessao E-REDES tem JWT valido (exp > now)"
    - "Parser XLSX produz CSV com kWh entre 30-1000 para os 3 ficheiros"
    - "Pipeline completo gera relatorio Markdown com ranking de fornecedores"
    - "Relatorio mostra mais de 1 mes analisado"
  artifacts:
    - path: "state/eredes_storage_state.json"
      provides: "Sessao E-REDES renovada com JWT valido"
      contains: "aat"
    - path: "/tmp/test_parse_2025.csv"
      provides: "Output do parser para XLSX de 2025"
      contains: "year_month,total_kwh"
    - path: "/tmp/test_parse_2026v1.csv"
      provides: "Output do parser para XLSX de 2026 v1"
      contains: "year_month,total_kwh"
    - path: "/tmp/test_parse_2026v2.csv"
      provides: "Output do parser para XLSX de 2026 v2"
      contains: "year_month,total_kwh"
    - path: "data/reports/relatorio_eletricidade_*.md"
      provides: "Relatorio final do pipeline"
      contains: "Ranking"
  key_links:
    - from: "src/backend/eredes_to_monthly_csv.py"
      to: "data/raw/eredes/*.xlsx"
      via: "openpyxl parse"
      pattern: "convert_xlsx_to_monthly_csv"
    - from: "src/backend/monthly_workflow.py"
      to: "data/reports/relatorio_eletricidade_*.md"
      via: "run_workflow"
      pattern: "relatorio_eletricidade"
---

<objective>
Renovar a sessao E-REDES (bootstrap manual), validar o parser XLSX contra os 3 ficheiros disponiveis, e correr o pipeline completo end-to-end para confirmar que gera um relatorio correcto.

Purpose: Confirmar que o pipeline backend funciona com dados reais antes de avancar para resiliencia e multi-local.
Output: Sessao E-REDES valida, 3 CSVs de validacao do parser, relatorio final gerado pelo pipeline.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md
@.planning/phases/01-unblock-validate-end-to-end/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap sessao E-REDES (FIX-03)</name>
  <files>state/eredes_storage_state.json, state/eredes_bootstrap_context.json</files>
  <read_first>
    - .planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md (seccao "Bootstrap de Sessao E-REDES" para o fluxo exacto e pitfall 2 sobre navegar ate pagina de consumos)
    - state/eredes_storage_state.json (confirmar que JWT esta expirado antes de pedir bootstrap)
  </read_first>
  <action>
Este passo requer login interactivo do utilizador no portal E-REDES. Claude nao pode fazer login porque o portal tem reCAPTCHA/2FA.

Antes de pedir ao utilizador, verificar que o JWT esta de facto expirado:
```bash
/usr/local/opt/python@3.11/libexec/bin/python3 -c "
import json, base64
from datetime import datetime
state = json.loads(open('state/eredes_storage_state.json').read())
for origin in state.get('origins', []):
    for item in origin.get('localStorage', []):
        if item.get('name') == 'aat':
            payload = item['value'].split('.')[1]
            payload += '=' * (4 - len(payload) % 4)
            claims = json.loads(base64.b64decode(payload))
            exp = claims.get('exp', 0)
            print(f'Expirado: {datetime.now().timestamp() > exp}')
"
```

Se expirado, apresentar ao utilizador as instrucoes de bootstrap (ver how-to-verify).
  </action>
  <what-built>Nada — este passo requer login interactivo do utilizador no portal E-REDES.</what-built>
  <how-to-verify>
O utilizador deve executar o seguinte comando e completar o login interactivo:

```bash
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade
/usr/local/opt/python@3.11/libexec/bin/python3 src/backend/eredes_bootstrap_session.py \
  --storage-state state/eredes_storage_state.json \
  --context-output state/eredes_bootstrap_context.json
```

Passos no browser que abre:
1. Fazer login no portal E-REDES (balcaodigital.e-redes.pt)
2. Navegar ate a pagina de historico de consumos (balcaodigital.e-redes.pt/consumptions/history)
3. Confirmar que a tabela de consumos carregou
4. Voltar ao terminal e pressionar Enter

IMPORTANTE: Nao fazer o bootstrap com antecedencia — a sessao expira em ~90 minutos. Fazer imediatamente antes de continuar para Task 2/3.

Apos completar, verificar que o JWT e valido:
```bash
/usr/local/opt/python@3.11/libexec/bin/python3 -c "
import json, base64
from datetime import datetime
state = json.loads(open('state/eredes_storage_state.json').read())
for origin in state.get('origins', []):
    for item in origin.get('localStorage', []):
        if item.get('name') == 'aat':
            payload = item['value'].split('.')[1]
            payload += '=' * (4 - len(payload) % 4)
            claims = json.loads(base64.b64decode(payload))
            exp = claims.get('exp', 0)
            print(f'JWT exp: {datetime.fromtimestamp(exp).isoformat()}')
            print(f'Valido: {datetime.now().timestamp() < exp}')
"
```
Deve imprimir `Valido: True`.
  </how-to-verify>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && /usr/local/opt/python@3.11/libexec/bin/python3 -c "
import json, base64; from datetime import datetime
state = json.loads(open('state/eredes_storage_state.json').read())
for o in state.get('origins', []):
  for i in o.get('localStorage', []):
    if i.get('name') == 'aat':
      p = i['value'].split('.')[1]; p += '=' * (4 - len(p) % 4)
      c = json.loads(base64.b64decode(p))
      assert datetime.now().timestamp() < c.get('exp', 0), 'JWT expirado'
      print('PASS: JWT valido')
"</automated>
  </verify>
  <acceptance_criteria>
    - `state/eredes_storage_state.json` foi actualizado (timestamp do ficheiro e recente)
    - O script de verificacao JWT imprime `Valido: True`
    - `state/eredes_bootstrap_context.json` existe e contem `current_url` com `e-redes`
  </acceptance_criteria>
  <done>Sessao E-REDES renovada com JWT valido, state files actualizados</done>
  <resume-signal>Type "bootstrap done" quando o login estiver completo e o JWT verificado como valido</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar parser XLSX contra os 3 ficheiros disponiveis (VAL-02)</name>
  <files>/tmp/test_parse_2025.csv, /tmp/test_parse_2026v1.csv, /tmp/test_parse_2026v2.csv</files>
  <read_first>
    - src/backend/eredes_to_monthly_csv.py (entender a interface CLI: --input, --output, --drop-partial-last-month, --allow-partial-last-month)
    - .planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md (seccao "Sequencia de teste VAL-02" para os 3 comandos exactos e criterio de 30-1000 kWh)
  </read_first>
  <action>
Correr o parser XLSX individualmente para cada um dos 3 ficheiros disponiveis em `data/raw/eredes/`:

```bash
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade

# Ficheiro 2025 (11 meses, formato data longa):
/usr/local/opt/python@3.11/libexec/bin/python3 src/backend/eredes_to_monthly_csv.py \
  --input data/raw/eredes/Consumos_PT0002000084968079SX_2025-01-01_2025-11-26_20251126115627.xlsx \
  --output /tmp/test_parse_2025.csv \
  --drop-partial-last-month

# Ficheiro 2026 v1:
/usr/local/opt/python@3.11/libexec/bin/python3 src/backend/eredes_to_monthly_csv.py \
  --input data/raw/eredes/Consumos_PT0002000084968079SX_20260326042940.xlsx \
  --output /tmp/test_parse_2026v1.csv \
  --drop-partial-last-month

# Ficheiro 2026 v2:
/usr/local/opt/python@3.11/libexec/bin/python3 src/backend/eredes_to_monthly_csv.py \
  --input data/raw/eredes/Consumos_PT0002000084968079SX_20260326043825.xlsx \
  --output /tmp/test_parse_2026v2.csv \
  --drop-partial-last-month
```

Para cada CSV gerado, verificar:
1. Tem header com colunas `year_month`, `total_kwh`, `vazio_kwh`, `fora_vazio_kwh`
2. Cada valor de `total_kwh` esta entre 30 e 1000 kWh
3. O ficheiro de 2025 deve ter ~10 meses (Nov incompleto, descartado com --drop-partial-last-month)
4. O valor de referencia: Fevereiro 2026 deve ser ~721 kWh (conforme relatorio existente)

Se algum ficheiro falhar ou produzir valores fora dos limites, registar o problema no SUMMARY mas NAO alterar o parser (correcoes de parser sao para fases futuras). O objectivo e diagnosticar, nao corrigir.
  </action>
  <verify>
    <automated>test -f /tmp/test_parse_2025.csv && test -f /tmp/test_parse_2026v1.csv && test -f /tmp/test_parse_2026v2.csv && head -1 /tmp/test_parse_2025.csv && wc -l /tmp/test_parse_2025.csv /tmp/test_parse_2026v1.csv /tmp/test_parse_2026v2.csv</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/test_parse_2025.csv` existe e tem mais de 1 linha (header + dados)
    - `/tmp/test_parse_2026v1.csv` existe e tem mais de 1 linha
    - `/tmp/test_parse_2026v2.csv` existe e tem mais de 1 linha
    - Header de cada CSV contem `total_kwh`
    - Nenhum comando de parse terminou com exit code != 0
    - Valores de total_kwh inspeccionados estao entre 30 e 1000 kWh
  </acceptance_criteria>
  <done>Os 3 ficheiros XLSX sao parseados com sucesso, CSVs gerados com valores de kWh dentro dos limites plausiveis (30-1000), diagnostico de qualquer variacao de formato registado</done>
</task>

<task type="auto">
  <name>Task 3: Correr pipeline completo end-to-end com XLSX real (VAL-01)</name>
  <files>data/reports/relatorio_eletricidade_*.md</files>
  <read_first>
    - src/backend/monthly_workflow.py (entender a interface CLI: --config, --input-xlsx, --allow-partial-last-month)
    - config/system.json (ver configuracao actual do pipeline)
    - .planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md (seccao "Validacao End-to-End VAL-01" para o comando exacto e pitfall 4 sobre historico insuficiente)
  </read_first>
  <action>
Correr o pipeline completo com o XLSX de 2025 (maximo de historico disponivel) para gerar um relatorio com analise multi-mes:

```bash
cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade

/usr/local/opt/python@3.11/libexec/bin/python3 src/backend/monthly_workflow.py \
  --config config/system.json \
  --input-xlsx data/raw/eredes/Consumos_PT0002000084968079SX_2025-01-01_2025-11-26_20251126115627.xlsx \
  --allow-partial-last-month
```

Apos execucao, verificar:
1. Existe um ficheiro `data/reports/relatorio_eletricidade_YYYY-MM-DD.md` com a data de hoje
2. O relatorio contem ranking de fornecedores (nao apenas 1 fornecedor)
3. O relatorio mostra mais de 1 mes analisado (evitar o problema do relatorio anterior com "Meses analisados: 1")
4. O relatorio contem recomendacao mono/bihorario

Se o pipeline falhar com erro de sessao E-REDES (porque tenta fazer download alem do --input-xlsx), registar o comportamento exacto. O modo --input-xlsx deve saltar o download.

Se o pipeline falhar por outro motivo, capturar o traceback completo para diagnostico no SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && ls -la data/reports/relatorio_eletricidade_*.md 2>/dev/null && grep -ci "ranking\|fornecedor" data/reports/relatorio_eletricidade_$(date +%Y-%m-%d).md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Ficheiro `data/reports/relatorio_eletricidade_YYYY-MM-DD.md` existe com data de hoje
    - `grep -i "ranking\|fornecedor" data/reports/relatorio_eletricidade_*.md` retorna pelo menos 2 matches
    - O relatorio nao contem "Meses analisados: 1" (deve ter multiplos meses)
    - O comando `monthly_workflow.py` terminou com exit code 0
  </acceptance_criteria>
  <done>Pipeline completo executado com sucesso usando XLSX real, relatorio gerado com ranking de fornecedores e analise multi-mes, confirmando que o backend esta funcional end-to-end</done>
</task>

</tasks>

<verification>
1. `state/eredes_storage_state.json` tem JWT com exp > now (sessao valida)
2. 3 CSVs parseados com valores de kWh entre 30-1000
3. Relatorio final existe em `data/reports/` com ranking e recomendacao
4. Nenhum erro TCC nos logs apos fix do Plan 01
</verification>

<success_criteria>
- Sessao E-REDES renovada com JWT valido
- Parser XLSX validado contra os 3 ficheiros disponiveis sem erros
- Pipeline completo gera relatorio com ranking de multiplos fornecedores e mais de 1 mes de analise
- Zero erros de execucao no pipeline end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-unblock-validate-end-to-end/01-02-SUMMARY.md`
</output>
