---
phase: 01-unblock-validate-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist
  - launchd/com.ricmag.monitorizacao-eletricidade.plist
autonomous: true
requirements: [FIX-01, FIX-02, FIX-04]

must_haves:
  truths:
    - "state/eredes_storage_state.json nao aparece em git status"
    - "__pycache__/ nao aparece em git status"
    - "pip install -r requirements.txt corre sem erros"
    - "Ambos os plists usam o path absoluto do Homebrew Python"
    - "launchd agents recarregados e activos"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusao de ficheiros sensiveis e gerados do repositorio"
      contains: "state/eredes_storage_state.json"
    - path: "requirements.txt"
      provides: "Declaracao de dependencias do projecto"
      contains: "playwright==1.58.0"
    - path: "launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist"
      provides: "Plist do watcher com Python path correcto"
      contains: "/usr/local/opt/python@3.11/libexec/bin/python3"
    - path: "launchd/com.ricmag.monitorizacao-eletricidade.plist"
      provides: "Plist do reminder com Python path correcto"
      contains: "/usr/local/opt/python@3.11/libexec/bin/python3"
  key_links:
    - from: "launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist"
      to: "~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.process-latest.plist"
      via: "cp e launchctl reload"
      pattern: "launchctl load"
    - from: "launchd/com.ricmag.monitorizacao-eletricidade.plist"
      to: "~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.plist"
      via: "cp e launchctl reload"
      pattern: "launchctl load"
---

<objective>
Corrigir os tres bloqueadores de configuracao: .gitignore em falta (credenciais expostas), requirements.txt em falta (dependencias nao declaradas), e path Python errado nos plists launchd (TCC permission error).

Purpose: Desbloquear o pipeline para que o watcher launchd funcione e o projecto esteja seguro para operacoes git.
Output: .gitignore, requirements.txt, dois plists corrigidos, launchd agents recarregados.
</objective>

<execution_context>
@/Users/ricmag/Documents/AI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ricmag/Documents/AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .gitignore e requirements.txt (FIX-02, FIX-04)</name>
  <files>.gitignore, requirements.txt</files>
  <read_first>
    - .planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md (seccao "Fix .gitignore" para conteudo exacto, seccao "requirements.txt" para packages e versoes)
  </read_first>
  <action>
Criar `.gitignore` na raiz do projecto com o seguinte conteudo exacto (per FIX-02):

```gitignore
# Sessoes e credenciais
state/eredes_storage_state.json
state/eredes_bootstrap_context.json

# Dados brutos com CPE exposto
data/raw/

# Dados processados (gerados pelo pipeline)
data/processed/

# Relatorios gerados
data/reports/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Ficheiros temporarios do pipeline
config/_runtime_partial_override.json

# Logs de estado do launchd
state/launchd.process.stdout.log
state/launchd.process.stderr.log
state/launchd.stdout.log
state/launchd.stderr.log
```

Criar `requirements.txt` na raiz do projecto com o seguinte conteudo exacto (per FIX-04):

```
playwright==1.58.0
openpyxl==3.1.5
tf-playwright-stealth==1.2.0
requests==2.32.5
```

Validar com: `git status` (ficheiros sensiveis nao devem aparecer) e `pip install --dry-run -r requirements.txt`.
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && test -f .gitignore && test -f requirements.txt && grep -q "state/eredes_storage_state.json" .gitignore && grep -q "playwright==1.58.0" requirements.txt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` existe na raiz do projecto
    - `grep "state/eredes_storage_state.json" .gitignore` retorna match
    - `grep "state/eredes_bootstrap_context.json" .gitignore` retorna match
    - `grep "data/raw/" .gitignore` retorna match
    - `grep "__pycache__/" .gitignore` retorna match
    - `requirements.txt` existe na raiz do projecto
    - `grep "playwright==1.58.0" requirements.txt` retorna match
    - `grep "openpyxl==3.1.5" requirements.txt` retorna match
    - `grep "tf-playwright-stealth==1.2.0" requirements.txt` retorna match
    - `grep "requests==2.32.5" requirements.txt` retorna match
    - `git status` nao mostra `state/eredes_storage_state.json` como untracked
  </acceptance_criteria>
  <done>Ambos os ficheiros existem, .gitignore exclui ficheiros sensiveis do git status, requirements.txt lista as 4 dependencias com versoes fixas</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir path Python nos plists launchd e recarregar agents (FIX-01)</name>
  <files>launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist, launchd/com.ricmag.monitorizacao-eletricidade.plist</files>
  <read_first>
    - launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist (ver estado actual — linha 9 tem `python3` bare)
    - launchd/com.ricmag.monitorizacao-eletricidade.plist (ver estado actual — linha 9 tem `python3` bare)
    - .planning/phases/01-unblock-validate-end-to-end/01-RESEARCH.md (seccao "Fix launchd TCC" para path correcto e sequencia de reload)
  </read_first>
  <action>
Em AMBOS os ficheiros plist, substituir a linha 9:

```xml
<!-- ANTES (errado): -->
<string>python3</string>

<!-- DEPOIS (correcto): -->
<string>/usr/local/opt/python@3.11/libexec/bin/python3</string>
```

Ficheiro 1: `launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist` — watcher de ~/Downloads
Ficheiro 2: `launchd/com.ricmag.monitorizacao-eletricidade.plist` — reminder do dia 1

Apos editar ambos, executar a sequencia de reload para CADA agent:

```bash
# Watcher
launchctl unload ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.process-latest.plist 2>/dev/null
cp launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.process-latest.plist

# Reminder
launchctl unload ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.plist 2>/dev/null
cp launchd/com.ricmag.monitorizacao-eletricidade.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.plist
```

Verificar que ambos os agents estao carregados:
```bash
launchctl list | grep monitorizacao-eletricidade
```

Deve retornar duas linhas (uma para cada agent).
  </action>
  <verify>
    <automated>cd /Users/ricmag/Documents/AI/3-hobbies/Casa/energia/monitorizacao-eletricidade && grep -c "/usr/local/opt/python@3.11/libexec/bin/python3" launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist launchd/com.ricmag.monitorizacao-eletricidade.plist && launchctl list | grep -c monitorizacao-eletricidade</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/usr/local/opt/python@3.11/libexec/bin/python3" launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist` retorna exactamente 1 match
    - `grep "/usr/local/opt/python@3.11/libexec/bin/python3" launchd/com.ricmag.monitorizacao-eletricidade.plist` retorna exactamente 1 match
    - `grep "python3</string>" launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist` retorna apenas a linha com path absoluto (nao "python3" bare)
    - `grep "python3</string>" launchd/com.ricmag.monitorizacao-eletricidade.plist` retorna apenas a linha com path absoluto (nao "python3" bare)
    - `launchctl list | grep monitorizacao-eletricidade` retorna 2 linhas
    - `diff launchd/com.ricmag.monitorizacao-eletricidade.process-latest.plist ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.process-latest.plist` retorna vazio (ficheiros identicos)
    - `diff launchd/com.ricmag.monitorizacao-eletricidade.plist ~/Library/LaunchAgents/com.ricmag.monitorizacao-eletricidade.plist` retorna vazio (ficheiros identicos)
  </acceptance_criteria>
  <done>Ambos os plists usam path absoluto do Homebrew Python 3.11, copias em ~/Library/LaunchAgents/ estao sincronizadas, ambos os agents estao carregados no launchctl</done>
</task>

</tasks>

<verification>
1. `git status` nao mostra `state/eredes_storage_state.json` nem `__pycache__/`
2. `cat requirements.txt` mostra 4 dependencias com versoes
3. `grep "/usr/local/opt/python@3.11" launchd/*.plist` retorna 2 matches
4. `launchctl list | grep monitorizacao` retorna 2 entries
</verification>

<success_criteria>
- .gitignore criado e exclui ficheiros sensiveis do git tracking
- requirements.txt criado com 4 dependencias pinadas
- Ambos os plists corrigidos com path absoluto do Python
- Plists copiados para ~/Library/LaunchAgents/ e agents recarregados
</success_criteria>

<output>
After completion, create `.planning/phases/01-unblock-validate-end-to-end/01-01-SUMMARY.md`
</output>
